# 如何更新这个网站的内容

> 写给站主自己看的。不需要懂前端，也不需要碰终端。

## 一条铁律

**所有内容都是文件，改文件就等于改网站。** 没有后台、没有数据库、没有"登录管理页面"这种东西。
你改一个文件 → 提交 → Vercel 自动重新构建 → 一两分钟后线上就变了。

---

## 三种改法，挑顺手的

### 方法一：跟 Claude 说（最省事）

在这个项目文件夹开个终端窗口，直接说人话：

- 「我写了篇文章，标题叫 XXX，内容是……」
- 「工具页把 Figma 删掉，加上 Cursor」
- 「首页那句引导语改成……」

Claude 会改文件、跑构建、提交推送，一条龙。**改错了也能撤回**（git 有完整历史）。

### 方法二：GitHub 网页版改（不用装任何东西，手机也能改）

你已经用这个办法建过定时任务的文件了，就是那套：

1. 打开 https://github.com/leomfu/WL---
2. 点进 `content` 文件夹，找到要改的文件
3. 点文件右上角的**铅笔图标**（Edit this file）
4. 改完拉到底 → 绿色 **Commit changes**
5. **等一两分钟，线上自动更新** —— 什么都不用再做

新增文件就用 **Add file → Create new file**，文件名要带完整路径，比如
`content/posts/我的第一篇.md`。

⚠️ 唯一的例外：`.github/workflows/` 下的文件必须在网页版改（本机 token 没权限）。

### 方法三：本地改（你熟悉之后）

用编辑器改 `content/` 下的文件 → 终端里 `git add -A && git commit -m "说明" && git push`。
想先看效果就跑 `npm run dev`，浏览器打开 http://localhost:3000。

---

## 内容对照表：想改哪儿，改哪个文件

> 2026-09-17 全站改版之后，站上只剩：首页、项目、视频、博客、摄影、工具、关于、联系，外加唱片（音乐）。
> 文案的定稿在 `docs/design/文案定稿.md`。**`content/` 目前只有中文**，英文页面直接显示中文内容；
> 界面上的短文案（按钮、标题、标签）在 `messages/` 里，中英两份。

| 你想改的东西 | 文件 |
|---|---|
| **写新文章** | 在 `content/posts/` 新建一个 `.md`，格式见下 |
| 首页 Hero 那句引导语 | `content/home/intro.zh.md` |
| 首页大标题、按钮、「在做的」旁边的月份、「爱好」两行的说明 | `messages/zh.json` + `en.json` 的 `home.*` |
| 首页拍立得头像 | 换 `public/images/hero/poster.webp`（同名覆盖即可，组件不用动；新图请先转成 webp、宽 800px 左右，别直接放几 MB 的原图） |
| 关于页正文 | `content/about/about.zh.md` |
| 关于页右边那张「现在」大卡 | `messages/zh.json` + `en.json` 的 `about.*` |
| 项目列表（首页「在做的」也读它：`featured: true` 的那几个，`summary` / `featuredStack` 是首页卡片上的短版） | `content/projects/projects.json` |
| 视频列表（`summary` 是首页「最近拍的」那句短说明） | `content/videos.json` |
| **摄影**（加照片、新开一辑） | `public/images/photos/` 放图 + 改 `content/photos/*.json`。**照着 `docs/如何添加照片.md` 做** |
| 工具页清单 | `content/tools.json`（新图标要往 `src/components/icons/ToolIcon.tsx` 加 path） |
| 联系页右边三行的说明、页脚那两行大字和版权 | `messages/zh.json` + `en.json` 的 `contact.socials.*` / `footer.*` |
| **「按心情听」歌单**（加歌/删歌/改场景名） | `scripts/music-chart.txt` → 跑 `npm run chart` → 自动写进 `content/music/chart.json`。**照着 `docs/如何加歌.md` 做** |
| 名字、邮箱、社交链接 | `site.config.ts` 最上面 |
| 其他界面上的字 | `messages/zh.json` + `messages/en.json`（**两个都要改，键要对齐**） |

## 写一篇新文章（最常用）

在 `content/posts/` 新建一个文件，比如 `第一次做网站.md`。**文件名就是网址**
（`/blog/第一次做网站`），建议用英文或拼音，比如 `first-website.md`。

内容长这样 —— 上面那段 `---` 之间的叫 front-matter，是给系统看的；下面才是正文：

```markdown
---
title: 第一次做一个网站
date: 2026-08-28
type: blog
tags: [记录, 前端]
lang: zh
summary: 从零到上线，以及中途所有卡住的地方。
---

正文从这里开始，直接写就行。

## 小标题这样写

段落之间空一行。**加粗**用两个星号，`代码`用反引号。

- 列表用减号开头
- 第二条
```

字段说明：

- `type`：`blog`（随笔）/ `essay`（长文）/ `thought`（想法），决定它在文章页的哪个筛选下
- `lang`：这篇的原文语言，写 `zh` 就行
- `title_en` / `summary_en`（可选）：英文版页面上显示的，**不写的话英文页会显示中文原文**
- `minutes`（可选）：列表上显示的阅读时长（分钟）。不写就按字数自动估
- `date`：影响排序，格式 `YYYY-MM-DD`

**文章里插图**：图片放 `public/images/posts/`，正文里写 `![说明文字](/images/posts/文件名.png)`。
正文图片默认是 washed（去饱和、抬亮），鼠标移上去恢复原色；如果这张图**本身就是内容**（截图、摄影作品），
在路径后面加一个 `"原色"` 就保持原色：

```markdown
![终端截图](/images/posts/xxx.png "原色")
```

**写完就提交**，文章列表、RSS、sitemap 全都自动更新，不用改任何别的地方。

📌 现在 `content/posts/` 里除了「使用 AI 的这一年」，另外三篇的正文还是占位示例，写好真文章后替换掉。

---

## 加一个视频

`content/videos.json` 里照着现有的那条往数组里加：

```json
{
  "platform": "bilibili",
  "id": "BV1WHup6fEyA",
  "title": "视频标题",
  "desc": "一句话介绍",
  "summary": "首页「最近拍的」卡片上那句更短的说明（可选）",
  "date": "2026-08-01",
  "cover": "/images/videos/xxx.jpg"
}
```

B 站填 BV 号，YouTube 填视频 id。封面图放 `public/images/videos/` 下，不填就用深色渐变占位。

---

## 改完怎么确认成功了

1. GitHub 上能看到你那次提交
2. Vercel 会自动开始构建（https://vercel.com 里能看到）
3. **一两分钟后刷新网站** —— 变了就是成了
4. 没变？可能是构建失败了。JSON 少个逗号、front-matter 格式错了都会导致失败。
   打开 Vercel 看那次构建的日志，或者直接把报错截图发给 Claude。

## 最容易踩的三个坑

1. **JSON 文件里少逗号 / 多逗号** —— 整个构建会失败。改 `.json` 前先想清楚，
   或者干脆让 Claude 改。markdown 文件没这个问题，随便改。
2. **只改了中文没改英文** —— `messages/` 下两个文件的键必须一一对应，缺一个会报错。
   内容类的（文章、about）没这个限制，英文缺了就显示中文。
3. **front-matter 的引号** —— 标题里有冒号 `:` 或方括号时必须用引号包起来，
   比如 `title: "记录：第一天"`。
