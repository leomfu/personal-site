# 项目进度

> 每个阶段完工后由当班终端更新本文件。

- [x] 规划：需求确认、PLAN.md、分工提示词（2026-08-24，Fable 规划会话完成）
- [x] 阶段 0：UI 视觉稿（2026-08-24 用 Opus + /design 完成，用户已确认，见 docs/design/ 五个画板）
- [x] 阶段 1：脚手架 + 设计系统 + 双语框架 + git + Hello 开场页（2026-08-24 完成）
- [x] 阶段 2：左侧边栏导航 + 首页（含"现在是"板块）+ About + Projects + 视频作品区 + 工具页 + Contact（2026-08-24 完成）
- [x] 阶段 3：Markdown 博客系统（列表/详情/标签/RSS）+ 书影音页 + ⌘K 搜索 + giscus 评论/留言板（2026-08-24 完成）
- [x] 阶段 4：放松区（沉浸模式 + 氛围音自托管 + 网易云歌单嵌入 + 播客嵌入）（2026-08-24 完成）
- [x] 阶段 5：SEO + 打磨 + 访问统计（2026-08-24 完成）+ 部署上线（2026-08-25，Vercel）

## 交接备注

（各阶段完工后在此追加：做了什么 / 遗留问题 / 给下一阶段的提醒）

- 规划会话：项目文件夹为空仓库，尚未 git init（阶段 1 负责）。素材均未到位，全部先用占位。
- 规划补充（2026-08-24）：用户确认新增功能——视频作品区、Now 页、书影音页、giscus 评论+留言板、⌘K 搜索、访问统计；放松区音乐改为网易云歌单嵌入（氛围音仍自托管，见 PLAN.md §6）。giscus 要求 GitHub 仓库 public + 开 Discussions。
- 阶段 0 完成（2026-08-24）：视觉稿定稿于 docs/design/（Intro/Main/BlogContact/Lounge/Tools 五画板，Logo 为 wl-logo.png）。设计相对原计划的变化已同步进 PLAN.md：布局改为左侧暗色侧边栏 + 浅色内容区；Now 并入首页"现在是"板块（/now 页取消）；新增 /tools 工具页；开场页为深空星点风格（滚动/回车/点击进入）；标题用 Noto Serif SC 细衬线；不做全局暗色模式切换。设计目录下的 *.py 是画板生成脚本，仅作留档，开发不依赖它们。

- 阶段 1 完成（2026-08-24）：

  **做了什么**
  - Next.js 15.5.23（App Router）+ TypeScript + Tailwind v4 + motion 13 + next-intl 4 + gray-matter。
    `create-next-app` 默认装的是 Next 16，已按 CLAUDE.md 的锁定版本降回 15。
  - `next.config.ts` 开了 `output: "export"` + `trailingSlash` + `images.unoptimized`，
    从第一天起就是纯静态产物（`out/`）。**因此全站不能用 middleware**——next-intl 走
    `[locale]` 段 + `generateStaticParams`，两种语言在构建时各生成一份。
  - 设计系统 tokens 全在 `src/app/globals.css` 的 `@theme` 里（Tailwind v4 是 CSS 优先配置，
    没有 tailwind.config.js）。亮侧/暗侧灰阶、字体栈、布局尺寸（`--spacing-sidebar: 264px`、
    `--spacing-column: 700px`、`--spacing-rail: 64px`）、七个动效关键帧、
    `prefers-reduced-motion` 全局兜底，都在这一个文件。
    另有两个 `@utility`：`bg-content`（内容区浅灰渐变）、`link-underline`（正文链接）、`bg-rule`（开场页分割线）。
  - `messages/zh.json` + `messages/en.json`，键结构两边已校对一致。
  - `content/` 按 PLAN §7 建好，`content/README.md` 里写死了各类 front-matter 字段约定。
    `site.config.ts` 在项目根目录，字段留全（社交 / 网易云 / 播客 / giscus / umami），值先占位。
  - Hello 开场页（`src/components/intro/`）：四层质感（辉光 dcGlow / 46 颗星点 dcTwinkle /
    SVG feTurbulence 噪点 / 暗角）、Logo 反色 + dcFloat 浮动 + dcSheen 光泽扫过、
    分割线 dcWiden 划开、入场按视觉稿节奏 stagger（260→1600ms）、
    滚动/回车/空格/触摸/点击五种方式进站、过场淡出+微放大+模糊 620ms 后跳 /home。

  **几个实现上的决定（跟视觉稿有出入的地方）**
  - **根路径 `/`**：静态导出没有 middleware 和 redirect()，所以 `src/app/page.tsx` 输出一张
    最小跳转页（meta refresh + `location.replace` + noscript 链接，底色 `#060606` 不闪白），
    跳到 `/zh/`。`src/app/layout.tsx` 因此只 `return children`，真正的 `<html lang>` 骨架
    在 `src/app/[locale]/layout.tsx` 里（lang 要跟着语言走）。
  - **左上角只留了一个语言切换圆钮**，视觉稿上那个「声音」图标没做——开场页本来就没有声音，
    放个点了没反应的按钮不如不放。声音控件留到阶段 4 的放松区。
  - **进入按钮是 `<Link>` 不是 `<button>`**：禁用 JS 也能进站，有 JS 时 preventDefault 拦下来播过场动画。
  - **回访不闪开场页**：`[locale]/page.tsx` 里内联了一小段脚本，在 HTML 解析阶段就读
    sessionStorage 跳走（早于首次绘制）；组件里的 useEffect 是客户端路由跳回来时的兜底。

  **遗留问题 / 给阶段 2 的提醒**
  - **衬线字体还没自托管**。`--font-serif` 目前是 `"Noto Serif SC", "Source Han Serif SC",
    "Songti SC", Georgia, serif` 的回退栈——你的 Mac 上会落到宋体，效果接近视觉稿；
    但 Windows 访客会掉到别的字体。Google Fonts 在国内还被墙，所以正解是阶段 5
    自己子集化思源宋体放 `public/fonts/`。英文 Inter 已经用 `next/font/google` 自托管，没这问题。
  - **首页 `/[locale]/home` 目前是占位页**，阶段 2 换成真正的首页 + 左侧边栏。
    侧边栏建议抽成 `src/components/shell/`，因为除开场页外所有页面都要用。
  - **社交平台图标还没做**。画板里六个平台的 SVG 路径在 `docs/design/_parts.py` 的 `BRAND` 字典里，
    阶段 2 直接抄成 React 组件即可。注意小红书和抖音那两个是我手绘的近似版，
    有条件换成官方 SVG。
  - **GitHub 仓库没建**：这台机器上没装 `gh` CLI。本地 git 仓库已初始化并完成首次提交
    （54 个文件）。要接远程的话：装 `gh`（`brew install gh` 然后 `gh auth login`）后跟当班终端说一声，
    或者自己在网页上建空仓库再 `git remote add origin ... && git push -u origin main`。
    ⚠️ 阶段 3 的 giscus 要求这个仓库是 **public** 并开启 **Discussions**。
  - **`npm audit` 有 3 条 high**，全部来自 Next 15 的传递依赖（postcss、sharp），
    修复方案是升级到 Next 16，跟 CLAUDE.md 的版本锁定冲突，所以没动。
    这些是构建期工具，静态产物里不含它们，先记着。

- 阶段 2 完成（2026-08-24）：

  **做了什么**
  - `src/lib/` 分成四个文件，边界是「能不能进浏览器包」：
    `types.ts`（类型）+ `format.ts`（双语取值/日期/阅读时长，纯函数）→ 客户端组件也能引；
    `content.ts`（有 node:fs，只在服务端跑）+ `markdown.ts`（marked + shiki）。
    一开始把类型和格式化都放在 content.ts 里，客户端的 VideoCard 一引就把 node:fs 拖进浏览器包，
    Turbopack 直接 panic。要在客户端组件里用内容类型，记得从 `@/lib/types` 和 `@/lib/format` 引。
  - 左侧暗色侧边栏 `src/components/shell/Sidebar.tsx`（对照 Main 画板左栏）：
    Logo 圆徽 + 名字 + 定位、八项导航（当前项 `#EDEDED` 底反色）、连接/CONNECT 七个社交、
    底部 中/EN + ⌘K。桌面端点 ‹ 收成 64px 图标条（记 localStorage，键 `sidebar-collapsed`）；
    移动端（<lg）收成 56px 顶部条 + 全屏抽屉，复用同一份 panel。
    语言切换保持当前路径（`/zh/projects/` → `/en/projects/`）。
    社交链接为空时渲染成不可点的灰字，不给死链。
  - 路由分组 `src/app/[locale]/(site)/`：这一组套 SiteShell（侧栏 + 700px 内容列）和 PageFade
    （换页淡入）。开场页在组外，放松区（阶段 4）也在组外，各有整屏布局。
    原 `[locale]/home/page.tsx` 占位页已删除，首页现在是 `(site)/home/page.tsx`。
  - 六个页面：/home（名字 + 介绍 + 现在是 + 我在做的 + 我写的 + 页脚）、/about（读 md）、
    /projects（年份左列的列表）、/videos（点击才插 iframe 的懒加载卡片）、
    /tools（双列卡片，hover 亮品牌色）、/contact（衬线大字邮箱 + 写邮件/复制 + 在别处双列）。
  - 图标全部抄成 React 组件：`icons/NavIcon.tsx`（8 个导航）、`icons/SocialIcon.tsx`（7 个平台）、
    `icons/ToolIcon.tsx`（8 个工具；2026-08-25 已全部换成 Simple Icons 官方 path，见下方备注）。
  - `.prose-bw` 排版类进了 globals.css：markdown 渲染出来的正文、标题、引用、列表、表格、
    代码块全在这里定样式。代码高亮用 shiki + `src/lib/markdown.ts` 里手写的**灰阶主题**
    （靠深浅/粗细/斜体区分 token，不用颜色，免得破了黑白设计）。
  - 内容占位补齐：`content/home/intro.{zh,en}.md`（首页两段介绍，含站内链接）、
    `content/projects/projects.json`（3 个项目）、`content/videos.json`（2 条）、
    `content/about/about.{zh,en}.md`（带小标题的多段占位）、tools.json 补上 8 个品牌色。
  - markdown 里的站内链接写 `/projects` 即可，渲染时自动补成 `/zh/projects/`（见 markdown.ts）。
  - messages 两份字典按全站页面重排过一遍（pageMeta / nav / home / projects / videos / tools /
    about / contact / blog / library / guestbook / lounge / palette），键结构已脚本校对一致。
  - 顺手把每页的双语 metadata（title/description/canonical/hreflang/OG）一起做了
    （`src/lib/metadata.ts`），本属阶段 5，写页面时一并落笔省一轮改动。

  **两处跟视觉稿的偏差（有意的）**
  - 画板是 1440px 桌面稿。移动端未画，按 PLAN §4 末尾自行推导：顶部条 + 全屏抽屉，
    大标题 46px → 34px，内容区左右留白 24px。
  - 侧栏折叠态视觉稿只画了 ‹ 按钮没画折叠后的样子，这里实现成 64px 图标条，
    和放松区的收窄图标条同一套尺寸（`--spacing-rail`），保持一致。

  **给阶段 3 的提醒**
  - 首页「我写的」已经在读 `content/posts/`，现在没文章所以显示占位句；阶段 3 放进 md 就自动接上。
  - 侧栏底部和移动端顶部的 ⌘K 按钮已经在 `window.dispatchEvent(new CustomEvent("open-command-palette"))`，
    阶段 3 的命令面板只要监听这个事件 + 自己接 ⌘K 键盘事件即可。
  - `src/lib/nav.ts` 里 `EXTRA_PAGES` 留了 /library 和 /guestbook（不进侧栏，走页脚和 ⌘K）。

- 阶段 3 完成（2026-08-24）：

  **做了什么**
  - `content/posts/` 放了 3 篇占位文章（blog / essay / thought 各一篇，其中第一篇带 h2/h3 和
    一个 ts 代码块，用来验证目录和高亮）。`content/library/library.json` 放了 4 条书影音占位。
  - /blog 列表页（对照 BlogContact 画板上半）：全部/博客/长文/想法 四个筛选是纯前端的，
    年份变化时插一条虚线分隔行。筛选要交互所以 `BlogList` 是客户端组件 ——
    只把标题/摘要/日期/标签/时长传过去，**正文不进浏览器包**。
  - /blog/[slug] 详情页：两种语言 × 所有文章构建时全量生成；长文（h2/h3 ≥ 3 个）在内容列
    右侧留白里挂一个 sticky 目录（xl 以上才出现，`left-[calc(100%+56px)]`），当前小节靠
    IntersectionObserver 高亮；底部上一篇/下一篇 + 回列表 + giscus 评论。
  - 双语策略按 PLAN 落地：front-matter 的 `lang` 标原文语言，只有单语版本的文章在另一种语言下
    照常显示，正文上方标一行「原文为中文，暂无英文版本」。标题/摘要有 `_en` 字段就用译文。
  - 阅读时长：中文 350 字/分钟 + 英文 200 词/分钟（`src/lib/format.ts` 的 `readingMinutes`）。
  - markdown 的 h2/h3 渲染时挂 id（`slugifyHeading`，中文原样保留，浏览器锚点支持 UTF-8），
    目录靠 `extractHeadings` 只走 lexer 抽出来，不重复渲染。
  - RSS：`src/app/rss.xml/route.ts`，`export const dynamic = "force-static"`，构建时写成
    `out/rss.xml`（静态导出下 route handler 只能是静态 GET，这是唯一可行的写法）。
    一篇文章只发一条，链接用原文语言的路径。（2026-08-25 起侧边栏不再展示 RSS 入口，
    路由和文件都保留，订阅地址就是 /rss.xml。）
  - /library 书影音：书/影/音三类切换，评分用 ★☆ 而不是彩色条，保持黑白。
  - ⌘K 命令面板（`components/search/CommandPalette.tsx`）：⌘K / Ctrl+K 开关、esc 关、↑↓ 选、
    ↵ 打开；搜文章标题与摘要、跳全部 10 个页面、切换语言。索引在 `(site)/layout.tsx` 里
    构建时生成后当 prop 传进去，纯前端过滤，不请求任何接口。移动端顶部的放大镜按钮走同一个事件。
  - /guestbook 留言板 + 文章页评论都挂 `components/comments/Giscus.tsx`。

  **需要你操作的（giscus 还没通）**
  - 评论和留言板现在显示一行说明文字，不会插脚本 —— 因为 `site.config.ts` 里 giscus 的
    四个字段还是空的。要开通：① 仓库推到 GitHub 并设为 **public**；② 仓库 Settings →
    General → Features 勾上 **Discussions**；③ 到 https://github.com/apps/giscus 安装 giscus app
    到这个仓库；④ 打开 https://giscus.app 填仓库名，页面会给出 `data-repo-id` 和
    `data-category-id`，把它们连同 repo、category 填进 `site.config.ts` 的 giscus 段。
  - `public/giscus.css` 是给 giscus 用的黑白主题，但 giscus 的 iframe 在 giscus.app 域下，
    只认**绝对地址**。所以现在 `giscus.theme` 先填的 "light"；等域名定了，把它改成
    `https://你的域名/giscus.css` 就能用上黑白版（前提是托管平台允许跨域读取该文件）。

  **两个说明**
  - 命令面板里没有「切换暗色模式」这一项：PLAN.md §5 明确整站是「暗侧栏 + 亮内容」的定稿设计，
    不做全局明暗切换。面板里只有语言切换。
  - 代码块高亮是 shiki + 手写灰阶主题（阶段 2 就位），彩色一律不进来。

- 阶段 4 完成（2026-08-24）：

  **做了什么**
  - `/lounge` 不套 (site) 骨架，自己占满一屏（对照 Lounge 画板）：左侧 64px 图标条
    （Logo + 八个图标 + 底部竖排「ESC 退出沉浸」），鼠标移到最左边 24px 或按 ESC
    展开成 264px 完整侧栏；顶部居中一行弱提示；中间三层错相呼吸的同心圆环 + 衬线场景名。
  - 四个氛围场景的背景**全部由 CSS 生成，一张图不加载**（`SceneBackdrop.tsx`）：
    雨夜是两层不同角度/速度的斜纹雨丝，海浪是两层反向横向漂移的横带，
    篝火是底部径向辉光 + dcFlicker 不规则跳动，深空复用了开场页的星点层。
    每个场景之上共用噪点 + 暗角。换场景时整层交叉淡入 1.2s。
  - 氛围音引擎 `useAmbient.ts`：每帧把各轨音量推向目标值实现交叉淡入淡出；
    某个场景第一次被选中才创建 `<audio>`（懒加载）；音轨接进 WebAudio 的 AnalyserNode，
    **呼吸圆环跟着实时音量律动**，没有 analyser 或没在放时退回 CSS 的固定节奏呼吸。
  - 场景和音量记在 localStorage。这次用的是新写的 `src/lib/useStoredState.ts`
    （useSyncExternalStore），不是「effect 里读一次再 setState」——顺手也把侧栏折叠状态和
    开场页的 sessionStorage 换成了它。
  - 音乐层：网易云外链播放器 iframe 收进黑白相框（`grayscale(1)`，鼠标移上去才还原颜色），
    支持多个歌单切换（chips）。播客层同理。两者都懒加载。
  - iOS 需要用户手势才出声，所以进页面先盖一层「轻点开始」。切到音乐/播客标签时氛围音自动停，
    切回来续上，两路声音不打架。

  **素材还没到位的部分（不阻塞，页面照常能用）**
  - 氛围音四个文件还没有。路径已经预填在 `site.config.ts` 的 `lounge.scenes` 里，
    **把文件命名成 `rain.mp3 / waves.mp3 / fire.mp3 / space.mp3` 丢进 `public/audio/ambient/`
    就自动生效，不用改代码**（该目录 README 里写了选素材的要求和 CC0 来源）。
    文件不在时底部会显示一行说明，圆环退回固定节奏呼吸。
  - 网易云歌单 id 填进 `siteConfig.lounge.neteasePlaylists`（数组，可放多个）；
    播客嵌入地址填进 `lounge.podcastEmbeds`。没填时那两个标签页显示一行说明。
    ⚠️ 网易云有版权限制的歌外链放不出来，建好歌单后先用
    `https://music.163.com/outchain/player?type=0&id=歌单ID` 自测一下。

  **顺手修的**
  - `npm run lint` 之前是坏的：Next 16 删了 `next lint`，且 eslint-config-next 16 直接导出
    flat config，旧的 FlatCompat 写法会报 circular structure。已改成
    `eslint.config.mjs` 直接引 `eslint-config-next/core-web-vitals` + `/typescript`，
    `package.json` 的 lint 脚本改成 `eslint .`。
  - lint 通了之后暴露出 8 条 react-hooks 新规则的报错（effect 里同步 setState、
    渲染中改外部变量），已全部按规则本意改掉，不是加 disable：
    列表的年份分隔行改成 useMemo 预算好、命令面板的分组标题同理、
    抽屉关闭改成点击事件触发、持久化状态统一走 useStoredState。

- 阶段 5 完成（除部署外，2026-08-24）：

  **SEO**
  - 每页双语 metadata 在阶段 2 就随页面写好了（title / description / canonical / hreflang / OG）。
  - 分享图：`public/og.png`，由 `npm run og`（`scripts/generate-og.mjs`）生成，1200×630 黑白。
    **没用 app/opengraph-image.tsx 的约定**：静态导出下它产出的文件叫 `out/zh/opengraph-image`，
    没有扩展名，GitHub Pages 这类纯静态托管会用错的 Content-Type 发它，爬虫不认。
    图上只有拉丁字符（satori 要画中文得喂 ttf/otf，而站点中文字体是 woff2）。
    **改了名字或一句话定位之后记得重跑 `npm run og`。**
  - `src/app/sitemap.ts` + `src/app/robots.ts`，都要 `export const dynamic = "force-static"`，
    否则静态导出直接报错。产出 `out/sitemap.xml` / `out/robots.txt`，每页两种语言互指 hreflang。
  - favicon 用 Logo：`src/app/icon.png`（脚手架默认的 favicon.ico 已删）。

  **字体（阶段 1 遗留问题已解决）**
  - 标题的思源宋体改用 `next/font/google` 的 `Noto_Serif_SC`：**构建时下载、跟着站点一起发**，
    访客不需要连 Google（国内连不上），所以这就是自托管，不用自己子集化。
    中文字体按 unicode-range 切成上百个 woff2 小块，浏览器只下当前页面用得到的那几块；
    `out/` 因此变成 14MB（字体占绝大部分），但单页实际加载量很小。
  - ⚠️ 构建时需要能连 fonts.gstatic.com。第一次构建有几个块下载失败（只是 warning，不中断），
    再跑一次就补齐了（会缓存）。部署平台的构建机在墙外，不会有这问题。

  **其他**
  - 访问统计：`components/analytics/Analytics.tsx`，只有 `site.config.ts` 的 `analytics` 填了
    umami 的 src 和 website id 才插脚本，不填就完全不加载。推荐 https://cloud.umami.is 免费版
    （无 cookie、不采集个人信息）；如果最后部署在 Vercel，也可以换成 Vercel Analytics。
  - 404：`src/app/not-found.tsx` → `out/404.html`，深色、中英双语两个入口。
    它自带 html/body（根 layout 不渲染骨架）。
  - 暗色模式按 PLAN.md §5 明确不做：整站就是「暗侧栏 + 亮内容」的定稿设计。

  **部署（等你拍板）**
  - 产物是纯静态的 `out/`，三个平台都能上。建议顺序：
    ① **Cloudflare Pages** —— 国内访问最稳，免费额度大，构建命令 `npm run build`、输出目录 `out`；
    ② **Vercel** —— 接 GitHub 最省事、预览部署好用，但国内直连时快时慢（绑自己的域名会好些）；
    ③ **GitHub Pages** —— 免费且够用，但要写一个 Actions workflow，且仓库必须 public
       （giscus 本来也要求 public，所以不冲突）。
  - 定了之后要做三件事：把 `site.config.ts` 的 `url` 换成真实域名 → 重跑 `npm run og` →
    在平台上接好仓库。日常流程就是：写完 markdown → `git push` → 平台自动构建上线。

- 真实素材落地 + 审核问题修复（2026-08-25）：

  **填进去的真实信息**
  - `site.config.ts`：名字（伟良 / Weiliang）、一句话定位（中英）、邮箱 weiliang99520@gmail.com、
    四个社交链接（X / GitHub / 哔哩哔哩 / 小红书，B 站和小红书的跟踪参数已去掉）。
    **YouTube 和抖音仍留空**——用户给的分别是 youtube 首页和 `douyin.com/user/self`
    （后者只对登录本人生效），不是可分享的主页地址，等正确链接（见素材清单）。
    `url` 仍是 `https://example.com`，等定了部署平台再换。
  - `messages/{zh,en}.json` 的 `meta` / `pageMeta.home` / `intro` 里的 `[你的名字]`、
    `[一句话定位]` 占位全部换成真实内容，两份字典键结构仍一致（已脚本校对）。
  - 内容：`content/about/about.{zh,en}.md`（关于页全文，中文按用户原稿整理，
    修掉了「我是伟良，，」那个重复逗号，加了「在做什么 / 想去哪」两个小标题；英文是翻译版）、
    `content/home/intro.{zh,en}.md`（首页两段）、`content/now/now.{zh,en}.md`（`updated` 改 2026-08-25）。
  - `content/projects/projects.json`：三个占位换成三个真实项目
    （AI 知识库 / Claude-Anki / 本站），三个都 featured，首页三个全显示。
    ①② 用户还没给 repo 和线上地址，`link` / `repo` 先留空；③ 的 repo 指向本站仓库。

  **一处设计上的取舍：一句话定位有长短两版**
  - 用户的定位是完整一句话（31 个汉字）。侧边栏那一行是单行 + `truncate`（约 13 字），
    分享图 og.png 那行也是单行不换行——整句进去会被截断/溢出画布。
  - 所以 `site.config.ts` 新增了 `taglineShort` / `taglineShortEn`（「把问题查到根上，再写下来」/
    "Root causes, written down"），**只有侧边栏和 og 脚本用短版**；
    开场页大字、RSS 描述、各页 meta description 用的仍是完整句。
    改了短版记得重跑 `npm run og`。

  **修掉的审核问题（docs/review/2026-08-25-审核报告.md 的唯一一条【建议修】）**
  - 首页「我在做的」里硬编码的 `" —— "` 分隔符改走 messages：新增
    `home.projectSeparator`（中 ` —— ` / 英 ` · `）。顺手把同一行硬编码的技术栈句号也
    一起改成 `home.stackSuffix`（中 `。` / 英 `.`），否则英文版会冒出一个中文句号。

  **GitHub 远程**
  - `origin` 已经存在（https://github.com/leomfu/WL---.git），远程 main 停在
    `d6e893f`（阶段 1 完工那次提交），**是本地历史的祖先**，所以这次是干净的快进推送，
    没有覆盖任何东西，也没用到 force。用户说的「上传了一半」就是这个状态。
  - 仓库还得是 **public** 且开 Discussions，giscus 才能通（见素材清单第四节）。

  **还差的素材**：YouTube / 抖音正确链接、①② 项目的仓库地址、真实文章（现在还是 3 篇 `[示例]`）、
  视频清单、工具清单、书影音、四个氛围音文件、网易云歌单、播客嵌入、giscus 四个 id、部署平台 + 域名。

- 打磨轮（2026-08-25，规划会话并行调度两个 Opus 代理完成）：
  - 开场页中心视觉重做：删掉 Logo 大图 + dcSheen 光泽层（灰方块真凶），新增 `intro/TimeDial.tsx`
    代码绘制时间之钟（SSR 首帧固定 10:09:36 避免 hydration mismatch，挂载后 rAF 平滑走真实时间）；
    进站过场 = 指针倒转 + 星点拉丝 warp + 辉光，总时长 820ms，reduced-motion 全降级。
  - 开场页文案重写：按钮「向下滚动 · 走进我的世界」，页脚「写字、做东西、发呆」，中英双语。
  - 侧边栏：Logo 圆徽（原生 <a> 整页跳转）→ `/{locale}/?replay=1` 重看开场页（两道拦截均已放行
    replay 参数）；名字块 → /home。移动端顶部条行为一致。
  - 工具页 8 个图标换 Simple Icons 官方 path（claude/github/figma/notion/vercel/spotify 取自
    v16.28.0，OpenAI 取自 v13、VS Code 取自 v9——新版已按品牌方要求移除这两家）；brandColor
    换官方 hex（用户拍板：ChatGPT 用 OpenAI 紫 #412991，Notion/Vercel 保持官方纯黑）。
    simple-icons 只临时安装取数据，package.json 零改动。
  - 视频页录入第一条真实视频：B 站 BV1WHup6fEyA（Claude-Anki 使用介绍），日期暂填 2026-08-01 可改。
  - 仍待素材：YouTube/抖音正确链接、两个项目 repo 地址、真实文章、氛围音、网易云歌单、播客、
    giscus 配置（仓库需 public + Discussions）、部署平台。

- 收尾小改（2026-08-25，规划会话起头、代理接手完成）：视频卡片支持 `cover` 封面
  （`Video.cover` + `public/images/videos/claude-anki.jpg`，next/image 懒加载 + 压暗层，
  无封面时回退原来的深色渐变占位）；侧边栏与联系页去掉 RSS 一项（`SocialKey` 的 "rss"
  和 SocialIcon 的 rss 图标一并删掉，`/rss.xml` 路由保留）；YouTube 补齐真实主页
  @WEILIANGFU-s7q。仍待素材只剩抖音主页链接（YouTube 已划掉）。

- 部署上线（2026-08-25，www 会话接手 plan 会话未完成的部署）：
  - **平台：Vercel**，项目 `weiliang`（scope：weiliang99520-a11y's projects / Hobby），
    已接 GitHub 仓库 `leomfu/WL---` 的 `main` 分支，**以后 push 即自动构建上线**。
    线上地址 **https://weiliang-blush.vercel.app** ——
    `weiliang.vercel.app` 这个子域被别的账号占了，Vercel 自动分配了带后缀的这个，`url` 按它填。
    想要更好看的地址：项目 → Settings → Domains 加一个没被占的 `xxx.vercel.app`（或自己的域名），
    加完把 `site.config.ts` 的 `url` 改过去 → `npm run og` → 提交推送。
  - `site.config.ts` 的 `url` 从 `https://example.com` 换成了线上地址，并重跑 `npm run og`
    重生成分享图。构建产物已核对：sitemap / robots / canonical / og:image 全部指向新域名，
    全站再无 example.com。
  - 构建设置用 Vercel 自动识别的 Next.js 预设即可（`output: "export"` 下 Next 16 产出 `out/`，
    Vercel 直接当静态站点发）。没有加 `vercel.json`。
  - ⚠️ **有一个多余的项目 `weiliang-site` 要你去删**：plan 会话建 `weiliang` 时 MCP 的读接口
    读不到（团队 slug 与写接口作用域不一致，`list_projects` 只在用 `weiliang99520-a11y`
    这个 slug 时才返回全部项目），我误以为没建成又建了一个，两个都连着同一个仓库，
    所以每次 push 会各构建一次。到 Vercel → weiliang-site → Settings → 最下方 Delete Project 删掉即可
    （MCP 没有删项目的接口，只能你点）。留着也不影响 `weiliang` 那个正式站，只是浪费构建额度。
  - 后续要绑自定义域名：Vercel 项目 → Settings → Domains 添加，然后把 `site.config.ts` 的 `url`
    改成新域名 → `npm run og` → 提交推送。

- 放松区补齐（2026-08-25）：
  - **音乐层**：网易云歌单 `7044876104`（用户给的是主页 uid，用网易云公开接口
    `music.163.com/api/user/playlist?uid=...` 反查到歌单 id）。`lounge.neteasePlaylists`
    的类型从 `string[]` 改成 `{ id, label, labelEn }[]` —— chip 上要显示歌单名而不是「歌单 1」，
    名字得双语。只有一个歌单时不渲染 chip（一个孤零零的按钮没意义）。
  - **氛围层**：四个 mp3 由新脚本 `scripts/generate-ambient.sh`（ffmpeg 噪声源 + 滤波器）合成，
    不是下载的素材。理由：授权干净、能无缝循环（调制周期 10s，90s 整除）、每个 1.2MB。
    四段统一 −23 LUFS + alimiter。要换真实录音直接同名覆盖。
  - **三层标签按有无内容显示**：`TABS` 改成过滤后的 `tabs`，播客没配就不显示那一层。
    原来的空态提示写的是「把 id 填进 site.config.ts」这种开发者话术，**会被访客看到**，
    已删掉 `musicEmpty` / `podcastEmpty` / `playlist` 三个 message 键，
    `audioMissing` 改成面向访客的说法。
  - 已知小事：mp3 首尾有编码器补的静音，浏览器 `loop` 时可能有几十毫秒缝，
    噪声类听不出来，深空那种持续音理论上会有；真听出来再换无缝格式。

- 音乐层重做成时间盘播放器（2026-08-25，用户看到网易云 iframe 后要求重来）：
  - **砍掉网易云外链 iframe**。两个原因：① 跨域 iframe 白底红标，样式改不了，跟黑白站冲突；
    ② 网易云对**没登录的访客只放出歌单前 10 首**（那个 232 首的歌单在播放器里只显示 6 首）。
  - 新组件 `components/lounge/MusicDial.tsx`：全屏时间盘，视觉语言抄开场页的 `intro/TimeDial`
    （60 刻度 / 锥形指针 / #EDEDED），但指针走的是**这首歌的时间**，转满一圈 = 放完；
    进度弧用 `pathLength={1}` + dasharray，指针角度逐帧由 ref 写不走 React。
    点圆盘任意角度 = 跳到那个进度；空格播放暂停、←→ ±5 秒、L 开关曲目清单。
  - **两组曲目**（`lib/content.ts` 的 `getMusic`）：
    · 常驻 `content/music/resident.json` —— 自托管的 4 首肖邦（Musopen，CC0/公共领域），
      文件在 `public/audio/music/`，统一 112k / −20 LUFS，共 16MB；
    · 我在听 `content/music/netease.json` —— 由 `npm run music`（`scripts/fetch-netease.mjs`）
      从 `scripts/netease-songs.txt` 生成：查 id → 查时长 → **逐首验证外链能不能真出声**，
      只把能放的写进 json。**构建时不请求网易云**，部署机连不上国内也没关系。
  - 运行时某首放不出来 → 标灰跳下一首；整组都挂 → 自动退回常驻。所以直链哪天被封，页面不会空。
  - **没接 AnalyserNode**：把 `<audio>` 接进 WebAudio 图之后，跨域无 CORS 的音源会被静音，
    网易云那条链路赌不起。氛围层是自托管的，那边照旧跟着实时音量呼吸。
  - `site.config.ts` 的 `lounge.neteasePlaylists` 已删（曲目改由 content/ 管），
    messages 新增 `lounge.music.*` 一组。
  - ⚠️ 待验证：网易云直链会 302 到 **http** 的 CDN，Chrome 会自动升级成 https（实测 CDN 支持 https），
    但别的浏览器可能拦。用户实测「我在听」那组出不出声，出不来就只留常驻。
  - ⚠️ 已知：能外链播放的网易云曲目里，不少是**翻唱/独立版本**而不是原唱
    （原唱基本都被版权锁了），比如 Shallow 是 Jada Facer 的翻唱版。

- 留言板/评论完善（2026-08-25）：
  - `giscus.repoId` 已填 **R_kgDOUCPA3g**（不用登录也能拿：`api.github.com/repos/<owner>/<repo>`
    返回的 `node_id` 就是 giscus 要的 repo-id）。`categoryId` 仍空 —— 它必须等仓库开了
    Discussions **且**装好 giscus app 才拿得到，之后 `curl "https://giscus.app/api/discussions/categories?repo=..."`
    一条命令就有，不用去 giscus.app 网页上复制。
  - 主题从 `light` 换成本站那份黑白 `https://weiliang-blush.vercel.app/giscus.css`
    （giscus 的 iframe 在别的域，只认绝对地址；**换自定义域名后这里要跟着改**）。
    css 里补了：头像默认去色 hover 还原、去圆角、输入框聚焦不出蓝框。
  - **评论改成点一下才加载**（和视频卡片同一个做法）：不点就不向 GitHub 发任何请求，
    也不会因为 iframe 撑高导致页面跳动；占位 min-h-280px。
  - 未配置时给访客看的文案从「把四个 id 填进 site.config.ts」改成「留言区还没开放」。
  - 留言板加了**「没有 GitHub 账号？」兜底块**：说明为什么要 GitHub 登录（静态站没后端），
    并给出写邮件的入口（复用 `EmailActions`）。
  - ⚠️ 剩下两步只能用户点：仓库 Settings → 勾 Discussions；装 https://github.com/apps/giscus。
  - **用户拍板（2026-08-25）：留言方案就保持 giscus + 邮件兜底**，不引入 Waline/Twikoo
    这类需要后端的评论系统，也暂时不接 Formspree/Tally 表单。
    理由是零成本零维护，先上线跑一阵看真有没有人想留言。**以后别再翻这笔账**，
    除非用户主动提。CLAUDE.md 里「不引入数据库/后端」那条继续有效。
  - **2026-08-25 收尾：giscus 全部配好了**。用户开了 Discussions + 装了 giscus app 之后，
    `curl "https://giscus.app/api/discussions/categories?repo=leomfu/WL---"`
    直接给出分类 id，填了 Announcements 那个：`DIC_kwDOUCPA3s4DEK0W`
    （选 Announcements 是因为它只允许维护者开新帖，讨论串由 giscus 代建，避免被灌水开帖）。
    四个值齐了，文章页和留言板的评论区同时生效。

- 新闻页定稿 + 定时任务上线（2026-08-28）：
  - 上一版（含美股/加密/海外工具/X 精选）已 `git revert`，重做成**两个板块**：
    世界新闻（BBC 中文/BBC World/卫报/半岛/DW/NYT/NPR 七家头条 + 九家报社入口）
    和 AI 更新解读（官方更新 + 中文解读，解读在 `content/news/digests.json`）。
  - 顶部两个筛选标签（`components/news/NewsTabs.tsx`）：两块内容仍在服务端渲染，
    以 children 传进客户端组件，只由它管显隐；选择存 localStorage。
  - **定时任务已装好**：用户在 GitHub 网页版建了 `.github/workflows/news.yml`，
    首跑成功并自动提交了 `每日新闻：2026-08-28`，Vercel 已构建上线。全链路验证通过。
    ⚠️ 本机 token 无 workflow 权限，**这个文件只能在网页版改**，本地碰了下次 push 全被拒。
  - **解读由 Claude 手写**，用户明确不接 API、不付费。流程写在 CLAUDE.md「更新新闻页」。


- 换正式域名 weiliang.dev（2026-08-28）：
  - 域名 **weiliang.dev**，2026-08-28 **在 Vercel 买的**（不是之前设想的 Cloudflare），
    到期 **2027-08-28**。DNS 用 Vercel 自家的 `ns1/ns2.vercel-dns.com`，域名和项目在同一个账号下，
    所以在项目 → Settings → Domains 里加一下就完事，**不用手配 A / CNAME 记录**。
  - **裸域是主地址**：`https://weiliang.dev` 直接 200，`https://www.weiliang.dev` 308 永久跳回裸域。
    所以 `site.config.ts` 的 `url` 只写裸域，写 www 会白白多一跳。
  - ⚠️ `.dev` 整个顶级域在浏览器的 **HSTS 预加载名单**里，浏览器强制走 HTTPS，
    **没有 http 兜底**。以后要是证书出问题，页面是直接打不开，不会降级到 http，排查时别往那个方向想。
  - 代码里只有 `site.config.ts` 两处硬编码地址，都已改：`url`，以及 `giscus.theme` 那个
    `https://weiliang.dev/giscus.css`。⚠️ **giscus 那处最容易漏** —— 它的 iframe 跑在 giscus.app 域下，
    只认绝对地址，忘了改评论区会掉回默认白底，把黑白设计系统破坏掉。
  - `npm run og` 重跑过了，但 **og.png 的内容其实没变**（生成脚本 `scripts/generate-og.mjs`
    只从 config 里正则取 `nameEn` / `taglineShortEn` / `since` 三个字段，图上根本没印站点地址），
    所以 git 看不到这个文件的改动，属正常。真正跟着域名变的是 meta 里 `og:image` 的绝对地址。
  - 产物已抽查：`grep -rl "weiliang-blush" out/` 为空；canonical / og:url / og:image /
    sitemap.xml / rss.xml / robots.txt 全部是 `https://weiliang.dev`。
  - **之前那个「Vercel MCP 一会儿报 409 已存在、一会儿报 404 不存在」的矛盾，真凶查清楚了**：
    不是 Vercel 的 bug，也不是团队 slug 的问题 —— 是当初连 Vercel MCP 时**授权范围只勾了
    `learn-wuwu` 一个项目**，`weiliang` 等其余项目没授权，MCP 自然读不到。项目一直是好好的。
    以后再遇到 MCP「看不见明明存在的资源」，先去查连接时的授权范围，别怀疑平台。
  - **大陆访问实测通过（2026-08-28）**：用户找了一位在中国大陆的朋友实际打开
    `https://weiliang.dev`，**能正常访问，没有问题**。原来的部署计划把「Vercel 在大陆是否可达」
    列为主要风险，还预备了两套备选方案，现在这条风险可以先结掉。
    ⚠️ 但这只是**单点实测** —— 一个人、一个网络、一个时间点，不等于长期稳定；
    大陆对境外 CDN 的可达性是会变的。**备选方案先留档别删**，以后真有人反馈打不开就翻出来用：
    ① 把 DNS 从 Vercel 转到 Cloudflare，开橙云代理（proxied），SSL 模式设 **Full**；
    ② 整站迁 Cloudflare Pages —— 项目本来就是 `output: "export"` 纯静态导出，迁过去无痛。
  - **遗留待办 ①**：Vercel 账号下**有两个项目在构建同一个仓库** —— `weiliang`
    （weiliang-blush.vercel.app，正在用的，weiliang.dev 绑在它身上）和 `weiliang-site`
    （weiliang-site.vercel.app，重复的）。每次 push 会白跑两遍构建。建议删掉 `weiliang-site`，
    但**删除不可逆**，所以等 weiliang.dev 跑稳几天，再由用户到控制台
    → weiliang-site → Settings → 最下方 Delete Project。⚠️ 别删错成 `weiliang`。
  - **遗留待办 ②**：Vercel 账号有个「付款方式的账单地址不完整」的警告，用户需要去补全，
    否则明年（2027-08-28）自动续费可能失败，域名有掉的风险。

### GitHub 用户名改了：weiliang99520-a11y → leomfu（2026-08-29）

- 是**改名**不是新号：旧的 `api.github.com/users/weiliang99520-a11y` 已经 404，
  仓库 `leomfu/WL---` 的 node_id 仍是 `R_kgDOUCPA3g`（和改名前一样）。
  所以 **giscus 的 `repoId` / `categoryId` 不用动**，只有 `giscus.repo` 那个
  `owner/name` 字符串要跟着改 —— 已改。
- 跟着改过的地方：`site.config.ts`（GitHub 社交链接 + handle + `giscus.repo`）、
  `content/projects/projects.json`（个人网站仓库链接）、
  `docs/如何更新内容.md`、`docs/素材清单.md`、本文件里的仓库地址。
- ⚠️ **本地 git remote 还是旧地址**（GitHub 会 301 重定向，所以 push/pull 照常能用，
  但最好改掉）：`git remote set-url origin https://github.com/leomfu/WL---.git`
- 上面 Vercel 那段里的 scope 名 `weiliang99520-a11y's projects` 是 **Vercel 的**账号名，
  不是 GitHub 的，没动。Vercel 接的是 GitHub App，仓库改名它自己会跟上，不用重连。
- 顺手把 Claude-Anki 的仓库地址补进了项目页：https://github.com/leomfu/claude-Anki

### 放松区第三层重做：Spotify 嵌入 → 时刻表，并新增番茄钟（2026-08-29）

**起因**：用户在 weiliang.dev 上看到音乐层里一整块白色错误页（Chrome 自己的
「网址可能暂时无法连接」），因为大陆连不上 open.spotify.com。先做了一版可达性探测 +
说明卡兜底（commit 9730265），但用户看了之后要求**根本上不要嵌入**。

**查证结论（别再走回头路）**：想用站内自己的 `<audio>` 播放，就必须有能直接下载的音频直链，
而正版流媒体在授权层面就不给：
- **Spotify Web Playback SDK**：能自定义界面，但要求**每个听众**都有 Premium 并登录你的站，
  内核仍是他们的播放器，大陆照样不通；
- **Apple MusicKit JS**：$99/年开发者账号 + JWT（最长 6 个月）+ 每个听众要 Apple Music 订阅；
- **YouTube IFrame API**：可以藏起 iframe 自建界面，但条款限制外观改动，大陆也不通。
三条都不是「不用嵌入」，只是更麻烦的嵌入。

**网易云直链对华语流行基本无效（实测）**：`song/media/outer/url` 的规则是「网易云能免费试听的
才有直链」。拿张震岳、周杰伦各 10 首实测：张震岳 0/10；周杰伦看着中了 8 首，
**查歌手后发现原唱一首都没有** —— 全是翻唱、假名上传的 remix（`周杰伦-/Montagem`）、
或搜岔的同名歌。真实数字 **0/20**。所以「扩充我在听那一组」这条路对他的口味是死的，
现有 9 首能放是因为那些是没有版权限制的老歌/独立音乐。

**做了什么**：
- 删掉 `SpotifyEmbed.tsx` 和所有 Spotify 嵌入；`site.config.ts` 的
  `spotifyPlaylists` + `podcastEmbeds` 合并成 **`departures`**（一行一个去处，加行即可扩展）。
- 「播客」层 → **「时刻表」层**（`Departures.tsx`）：中心一块走真实时间的站台钟
  （时/分/秒针 + 等宽数字读数），下面是外链列表。理由：这一层的作用就是**离开这个网站**，
  时刻表既是「时间的表格」又是「从这里去哪儿」，和它的处境严丝合缝。
  和音乐层形成对照 —— 那边的指针走「歌的时间」，这边走「现实的时间」。
- 新增 **番茄钟层**（`PomodoroDial.tsx` + `usePomodoro.ts`），和音乐层同一张钟面，
  转满一圈 = 这一段走完。弧线画**剩余**（与音乐层的「已播进度」相反，是故意的）。
  - 状态存的是**绝对结束时刻 `endsAt`**，剩余时间由 `Date.now()` 现算 ——
    后台标签页降频、电脑睡眠都不会少算。**不要改成每 tick 减一秒。**
  - 状态放在 `LoungeStage`（不是番茄钟组件里），切层照走；整个序列化进 localStorage，刷新不丢。
  - 只在阶段结束时更新一次 React 状态，走针和数字用 ref 写 DOM，不带着呼吸圆环一起每秒重画。
  - 结束提示音用 WebAudio 现合成两声（不引入音频文件），可开关，存 localStorage。
  - 跑着时把剩余时间写进标签页标题。
- 抽出 `dial.tsx` 共用表盘几何（刻度/指针/盘心/clock），音乐层、番茄钟、站台钟三处共用。
- 音乐层顺带变干净：选择器从两簇缩回一簇「我在听 9 │ 常驻 4」。
- **氛围音行为改了**：原来切走就停，现在只有**音乐层**会掐掉氛围音，
  番茄钟层和时刻表层让雨声接着放 —— 一边下雨一边专注本来就该这样。

**用户已定的两件事**：① 曲库先不动，现有 13 首够用；
② 自托管喜欢的歌（加口令只给自己听）这条路**用户没选**，别自作主张去做。

**遗留**：`content/posts/` 里三篇 `[示例]` 占位还在，等第一篇真文章。

### 氛围音整个撤掉，番茄钟时长改成可自定义（2026-08-29）

**氛围音**：用户要求去掉。删了 `public/audio/ambient/`（4.8MB，四个合成的 mp3）、
`scripts/generate-ambient.sh`（90 行 ffmpeg 合成脚本）、`useAmbient.ts`（207 行音频 hook）。
**别再加回来** —— 素材清单里那条已经标成 ❌。

保留的是四套**背景画面**（`SceneBackdrop.tsx`，纯 CSS 渐变+噪点+动画，不需要任何素材）：
不再单独占一层，下放成整个放松区的全局背景，底部一排小字随时换。
localStorage 的 key 仍是 `lounge-scene`，老用户上次选的场景不会丢。

**呼吸圆环一并去掉了**：它画的是氛围音的实时音量，没有音量就没有它要表达的东西，
留着只会和番茄钟、音乐层那两张表盘抢视觉。
**「轻点开始」遮罩也去掉了**：它存在的唯一理由是浏览器要一次手势才允许出声，
现在进来不自动放任何声音，音乐层和番茄钟各自有播放/开始按钮。

放松区现在是三层：**音乐 / 番茄钟 / 时刻表**，默认停在音乐。

**番茄钟时长可自定义**：`site.config.ts` 的 `lounge.pomodoro` 从此只是**出厂默认值**，
用户在界面上改过之后以 localStorage（`lounge-pomodoro-conf`）为准。
- 专注 1–180 / 短休 1–60 / 长休 1–120 分钟，长休间隔 2–8 个专注（`LIMITS` 里）。
- 数字能直接打字改（要 50 分钟点 25 下加号太蠢），输入过程存草稿字符串，
  失焦或回车才夹到范围内提交 —— 否则刚打下 "5" 就被夹成合法值，第二个 "0" 永远打不进去。
- **正在跑的那一段不会被打断**：改时长只在停着的时候立刻重排，跑着就等下一段生效。

⚠️ React 19 的 `react-hooks/set-state-in-effect` 这次又撞了一回（标签落回第一个那段）。
解法一样：**在渲染时派生，别在 effect 里 setState**。

### 放松区加第四层「手记」：备忘 + 博客草稿（2026-08-29）

用户要在放松区写博客和备忘。**前提先说清楚**：静态导出没有后端，浏览器里写的东西
只能存 localStorage，不会自己变成文章。所以这个功能的重点不是编辑器，
而是**从「写完」到「站上有这篇」那条路有多短**。

**查证到的关键事实**：GitHub 的新建文件页支持用 URL 参数预填 ——
`github.com/<owner>/<repo>/new/main?filename=...&value=...`，
点过去编辑框里已经躺着写好的文章，只要按 Commit，**不需要任何令牌**。
上限是 GitHub 服务端的 8191 字节 URL 限制，超了直接 414。
实测（scripts 之外的一次性验证）：编码后一个汉字 9 字节，**约 880 汉字是分界线** ——
880 字 → 8110 字节能跳，900 字 → 8290 字节超。所以按钮会按长度自动切换成「复制后去 GitHub」。

**用户已定**：不存 GitHub 令牌（明文放 localStorage 的风险不值得），先走无令牌这条路；
番茄钟在跑时手记层右上角挂剩余时间（要）。

**做了什么**：
- `useNotes.ts` 存储层（一叠便签存 localStorage）、`noteExport.ts` 导出层
  （字数、阅读时长、front-matter 生成、GitHub URL 组装）、`Notes.tsx` 界面。
- 备忘 / 草稿一个开关切，不做成两套系统。备忘没有标题也没有导出按钮 ——
  **备忘就是备忘，不该有「发布」这种东西**。
- 时间主题落在三处真数据上：便签的时间戳（列表左侧那列点/竖线是把表盘 60 道刻度竖过来）、
  「约 N 分钟读完」、番茄钟剩余时间。
- 底部常驻「导出全部」+ 一句如实的说明（只在这台电脑、不同步、不加密）。
  **这不是装饰，是这个架构的必需品**，别以后精简掉。

**两个实现上的坑**：
- ⚠️ `Notes.tsx` 和 `notes.ts` 在 macOS（大小写不敏感的文件系统）上会撞，
  TS 报 TS1149。工具模块因此叫 `noteExport.ts`。**别再改回 notes.ts**。
- ⚠️ React 19 的 `react-hooks/refs`（不能在渲染期间写 ref）这次撞了一回，
  加上之前的 `set-state-in-effect`，这两条规则本项目已经踩过三次。
  正文用局部 state 缓冲、停手 400ms 才 flush 进 localStorage（每敲一个字就把整叠便签
  JSON 化写一遍太浪费，还会连累整个放松区重渲染），换便签靠 `key` 重新挂载而不是 effect 同步。

### 「时刻表」的 Spotify 链接点不开 —— 是代理分流，不是网站（2026-08-29）

用户反馈跳转不了。**先排除了代码**：把默认层临时改成 departures 重新构建，
导出的 HTML 里是标准的 `<a href="https://open.spotify.com/playlist/..." target="_blank">`，
URL 在构建产物里也完好无损。

**决定性证据**（同一台机器同一时刻）：
```
open.spotify.com   HTTP 000   5.1s 超时
music.163.com      HTTP 200   1.4s
www.bbc.com        HTTP 200   2.2s   ← 同样被墙，却能打开
```
BBC 通而 Spotify 不通 ⇒ **代理是工作的，只是没覆盖 open.spotify.com**。
很多 Clash 类规则集把 Spotify 单独分一组走直连（因为要求账号区域匹配），
所以网页版打不开。这不是站点能修的东西。

**做了什么**：`departures` 加了可选的 `appUrl`（`spotify:playlist:<id>`），
行尾多一个小小的「App」，直接把请求交给 Spotify 桌面/手机 App ——
App 自己的连接和浏览器是两条路，大陆网络下这条往往是唯一通的。
下面补了一句说明告诉用户为什么。

⚠️ 以后再遇到「链接点不开」，**先跑这三条 curl 对比**再改代码。
只测目标站会误判成"墙了没办法"，加一个同样被墙但能通的对照站（BBC）才能分清
是墙、是代理没覆盖、还是站点自己的问题。

### 开场页背景重做：上海外滩夜景 + 深度图 3D 视差（2026-08-30）

**做了什么**
- 背景从「深空星点」换成**一张外滩夜景照片 + 基于深度图的视差**。
  新组件 `src/components/intro/CityDepth.tsx`：一张全屏 canvas，**手写 WebGL1 shader，
  没有引入 three.js**（整站零新增依赖）。fragment shader 同时采样原图和深度图，
  按深度做 UV 位移 —— 近处跟着指针动得多、远处动得少，平面照片于是有了纵深。
- 素材（`public/images/intro/`）：
  · `shanghai.webp` 1672px / **136KB**（原图 2.0MB 的 png 没进仓库）
  · `shanghai-depth.jpg` 960×540 / 12KB
- ⚠️ **深度图是程序近似生成的，不是真的深度估计**。生成脚本
  `scripts/generate-intro-assets.mjs`（一次性，留档，日常不用跑）按构图手写了一条
  按 y 走的分层剖面 + 男孩和两根路灯杆两处局部，最后整体高斯模糊
  （硬边在视差位移里会撕成锯齿，糊一点反而对）。
  **约定：越亮 = 越近。** 想升级：把原图丢给能出 depth map 的模型，
  导出灰度图（近白远黑）同名覆盖 `shanghai-depth.jpg` 即可，**代码一行都不用改**。

**关于「全站只用黑白灰」这条规则**
- 开场页的**默认态仍然是纯黑白**（shader 里 uColor=0，`mix(vec3(luma), rgb, uColor)`）。
  彩色只出现在「穿梭时间」那不到两秒的进站动效里，而且是**从画面深处先亮起、向近处涌来**
  （上色锋面由深度驱动），做的是「时间倒流回有颜色的那一刻」。**这是刻意设计，不是破例**。
  CLAUDE.md 没改，那条规则继续管着全站其余部分。

**进站时序**（两处要一起改：`IntroScreen` 的 `EXIT` + `CityDepth` 的 `TL`）
- 0ms 触发 → 指针倒转（TimeDial，700ms）+ 颜色涌回（760ms）
- 240ms 起镜头沿深度轴推进（uDolly，1120ms，近处向两侧滑开、远处向中心聚）
- 950ms 起整屏淡出（500ms）→ **1600ms 跳转**。replay（`?replay=1`）走同一套。

**降级链（三层，都验过）**
1. 正常：静态 `<img>` 打底 → canvas 就绪后淡入盖上。
   ⚠️ img 上有 `transform: scale(1.06)`，必须和 shader 里的 `OVERSCAN` 保持一致，
   否则 canvas 淡入时画面会「跳」一下。
2. `prefers-reduced-motion`：canvas 根本不挂，只剩那张 CSS `grayscale(1)` 的静态图，
   进站只做交叉淡出。
3. 拿不到 WebGL 上下文 / 纹理加载失败 / `webglcontextlost`：同上，退回静态图。
- SSR 首帧一律只有静态图（学 TimeDial 首帧固定 10:09:36 的思路），不会 hydration 闪烁。

**移动端**：**不请求陀螺仪权限**（iOS 要弹窗，不值得）。改成一条极慢的 lissajous
自主漂移让画面保持活着；桌面端也叠了一层很小的同款漂移，指针不动时画面不会死。

**版式上的两处调整（照片当底之后必须做的）**
- 主体内容整体上移，只占屏幕上面那一段，把江面、堤岸和**那个坐着的背影让出来**——
  文字压在人身上会两败俱伤。时间之钟因此落在陆家嘴上空，东方明珠从表盘中间穿过去
  （塔尖留在盘外，没挡死），盘后垫了一块圆形玻璃（暗一点 + 2px 背景模糊）保证白色表盘可读。
- 首尾两块用了 `mt-auto` / `mb-auto` 的**安全居中**：空间够时等同 `justify-center`，
  空间不够时 auto 边距收成 0，内容只往下溢出 —— **绝不会把顶上那行 eyebrow 裁掉**
  （用 `h-[65%]` + `justify-center` 那版在 810px 高的窗口上就是这么裁没的）。
- 叠在照片上的字统一加了一层 `text-shadow`，比把整张照片压暗划算；
  几个最弱的灰（`shell-faint` #5a5a5a / `shell-mute` #4a4a4a）在开场页就地提亮到 #8A~#A0 ——
  那几个 token 是为 #060606 纯色底调的，压在夜景上会直接消失。**只在开场页改，token 没动。**

**Starfield 没删**：开场页不再用它，但放松区的「深空」背景
（`components/lounge/SceneBackdrop.tsx`）还在引，删了会连带炸掉那一层。

**自查记录**：`npm run build` 通过；用 headless Chrome 在 1440×910 / 1440×710 / 390×844 三档
截图核对过版式，并用一个临时的时间戳注入验证了进站动效的 0 / 380 / 760 / 1360ms 四帧
（验完已撤掉，不在提交里）。

### 开场页六处调整：钟悬到塔尖之上、夜光、上色放慢（2026-08-30）

用户看过外滩实景那一版之后逐项拍板的六件事，全部落地。

**① 钟缩小抬高，悬在塔尖上方 —— 这一条逼出了一个背景改动**
- 先量清楚了才动手：照片自己的天空在东方明珠塔尖之上**只剩 3%**，
  满屏 cover 之后塔尖就顶在屏幕最上沿（1440×900 实测 y≈4px）。
  也就是说「钟悬在塔尖之上」在原来的构图里**几何上做不到** ——
  不管钟放多高、多小，塔尖都在它上面。
- 解法：`CityDepth` 新增 `SKY_STRIP = 0.09`，把整张照片压进屏幕下面 91% 那块
  （**底边不动，江边那个人还坐在原地**），顶上让出一条夜空带。
  带子里的画面 = 把接缝处那一行天空**镜像回去再压扁 `SKY_SQUEEZE = 0.02`**，
  等于把那一行竖着抹开；接缝一阶连续，看不出横线。
  shader 和静态图两条路走同一组数（CSS 那边是 `scaleY(-1/SKY_SQUEEZE)`），
  **改了任何一个常数，两边都要跟着改**。
- ⚠️ 这里踩了两个坑，都已修，别再走回去：
  · **镜像不压扁**会把塔尖整根翻上去，变成一小截倒挂在屏幕顶上的针；
  · **宽扁窗口（16:9 这类）**下照片改成按宽度 cover、上下被裁，
    接缝那一行正好落在天线上，一抹就把天线拉到屏幕顶 —— 又是一根竖线。
    所以 shader 里夜空带越往上越 `mix` 到「照片第 0.006 行」（那一行永远是纯天空），
    静态图那条路用顶行的平均灰 `rgb(50,50,50)` 做同样的渐隐。
    另外夜空带里的采样点被 `cap` 钉住不许越过接缝那一行，否则视差一晃又会抹出塔尖。
- 表盘从 272px 缩到 `min(136px,15vh)`（手机 `min(120px,15vh)`），
  用固定上边距 `max(72px,8vh)` 顶到眉行之下的空天里。1440×900 下钟占 y≈72–208、
  塔的可见部分从 y≈215 起，**不再重叠**。

**② 夜光感**：`TimeDial` 把刻度/指针/盘心整层套上两道 `drop-shadow`
（近的一层「线在发亮」+ 远的一层「空气里的散射」），克制在这个量级。
辉光挂在 Face **外面**那层 div 上 —— 挂在 Face 上会破掉它的 memo，指针属性会被写回首帧角度。
进站时整层辉光换成暖色版本（两个字符串函数序列一致，CSS 能逐项插值成一次平滑的变暖），
盘后再压一层暖色辉光淡入，钟跟着城市灯火一起亮起来。

**③ 横贯画面的细线**：删掉（原「下分割线」`bg-rule`）。
`globals.css` 里那个 `@utility bg-rule` 现在全站没人用了，留着不生成 CSS，先没删。

**④ 顶部眉行**：从主体那一列里拿出来，改成页面级绝对定位（`top-10 sm:top-12`），
两侧长横线去掉，只留文字。绝对定位顺带保证它永远不会被挤掉。

**⑤ 上色放慢**：`TL.colorMs` 0.76s → **1.2s**，缓动从 easeOut 换成 easeInOut
（「慢慢亮起来」而不是前重后轻）；dolly 420ms 起走 1.75s；
`EXIT` 淡出 1.85s 起 0.6s，**总时长 1.6s → 2.5s**；
`TimeDial.WARP_MS` 700 → 1100。**这四处必须一起改**，改一处就散架。

**⑥ 手机竖屏表盘被裁**：已修（缩小 + 固定上边距 + 眉行绝对定位），
390×844 下表盘完整且不与塔叠。

**顺手查实并修掉一个一直被误判的问题**
- 现象：无头 Chrome `--screenshot --virtual-time-budget` 截出来只有顶部眉行，
  中央标题和进入按钮全没有。
- 结论：**纯属截图方法的问题，页面本身没毛病**。motion 的 opacity/transform 走 WAAPI，
  跑在合成器线程上用的是真实时间，`--virtual-time-budget` 推不动它 ——
  预算再大也只能拍到入场动画最开头几百毫秒（延迟 ≤460ms 的元素出现了，
  780ms 之后的全没有，正好对上）。改用 CDP（`Page.navigate` → 真实 sleep →
  `Page.captureScreenshot`）之后全部正常渲染。**以后核对开场页别再用 virtual-time-budget。**
- 查这件事时顺带发现一个真 bug 并修了：`CityDepth` 的清理函数里调了
  `WEBGL_lose_context.loseContext()`。`canvas.getContext("webgl")` 对同一张 canvas
  永远返回同一个上下文，所以一旦在清理里把它弄丢，**下次挂载拿到的是个废掉的上下文**，
  编译/链接静默失败，canvas 再也不亮。开发模式下 React StrictMode 正好「挂载→清理→再挂载」，
  于是 **`npm run dev` 里永远只看得到那张静态降级图**（生产构建没这问题）。
  很容易被当成「这机器不支持 WebGL」错判 —— 之前大概就是这么错过去的。已去掉那行。

**自查记录**：`npm run build` + `npm run lint` 通过。
**在生产产物上**（`npx serve out`，不是 dev —— 见上一条）用 CDP 截图核对了
1440×900 / 1440×710 / 390×844 三档，以及 `--force-prefers-reduced-motion` 的降级路和 /en；
钟悬塔尖之上不重叠、夜光可见、无横线、眉行不贴顶、手机钟完整、夜空带无接缝无竖痕，
并截了进站过程中的几帧确认颜色是由远及近慢慢涌回（近处地面最后才上色）。

### 新增「摄影」板块 + 侧边栏导航分组（2026-08-30）

**摄影 /photos —— 两级组织**
- 列表页：上半部**专题**（大图卡片，`kind: "feature"`），下半部**档案**（按年份分组的清单，
  `kind: "archive"`）。每辑一行显示 地点 · 日期区间 · 帧数（信息密度参考 ungetsu.net，
  排版是本站自己的：年份 + 细线 + 右侧「N 辑」，条目左标题右元信息）。
- 单辑页 `/photos/[slug]/`：正方形接触印相式网格 → 点开看整帧，
  **← → 翻页、ESC 关闭**，点背景也关（点图片本身不关）；底部上一辑/下一辑。
- 数据：`content/photos/<slug>.json`，一辑一个。读取层 `src/lib/photos.ts`（有 node:fs），
  类型和纯函数在 `src/lib/photoTypes.ts` —— **这条边界是硬的**，
  放大态那个组件是客户端组件，引到带 node:fs 的模块会把 Turbopack 打崩（阶段 2 踩过）。

**图片流水线 `npm run photos`（scripts/build-photos.mjs）**
- `photos-src/<辑>/*.jpg`（**在 .gitignore 里，原图不进仓库**）
  → `public/images/photos/<辑>/xxx.webp`（长边 2000，放大看的）
  + `public/images/photos/<辑>/thumb/xxx.webp`（宽 600，网格里的）
  → 把照片清单和尺寸写进 `content/photos/<辑>.json`。
- 用 **sharp**（next 的传递依赖，和 `generate-intro-assets.mjs` 一样借用，**零新增依赖**）；
  沙盘里没有 sharp 会给出「跑 npm install」的明确报错，不静默失败。
- 增量：源文件比产物新才重压；`-- --force` 全重压，`-- <辑名>` 只处理一辑。
- 已填的 `title/location/date` 等字段**不会被覆盖**（只重写 photos 段，`caption` 按文件名保留）。
- sharp 默认丢掉 EXIF（只用 `rotate()` 把竖拍摆正），所以**拍摄地点等元数据不会跟着上网**。
- HEIC 读不了（sharp 没带 libheif），脚本会点名跳过的文件并提示导出成 JPEG。

**占位素材（用户手头只有手机里几张照片，先试水）**
- 两辑占位：`placeholder-series`（专题）+ `placeholder-archive`（档案），
  共 10 张**渐变色块**（不是伪造的摄影作品），文件名和标题都写着占位，总共 160KB。
  真照片到位后删掉两个 json + `public/images/photos/` 下两个同名目录即可。
- 站主用的说明书：**`docs/如何添加照片.md`**（照着做能从零加一辑），
  `docs/如何更新内容.md` 的对照表和 `docs/素材清单.md` 都加了指路。

**黑白规则写明确了（CLAUDE.md）**
- 原来只写了「工具页图标 hover」一个例外，但视频封面早就是既成事实的豁免。
  现在写清楚：**界面只用黑白灰；内容图片（摄影、视频封面、唱片封面）一律原色**，
  分界线是「包着图片的界面仍然只有黑白灰」。免得以后的会话反复纠结要不要给照片加 grayscale。

**侧边栏导航分组（`src/lib/nav.ts` + `Sidebar.tsx`）**
- 平铺 11 项 + 摄影 + 唱片 = 13 项太长，改成四组：
  **作品**（项目/摄影/视频）· **写作**（博客/新闻）· **嗜好**（唱片/书影音/放松区）·
  **关于**（工具/关于/留言板/联系）。首页不属于任何一组，单独留在最上面。
  组标题用 `--tracking-label` 的 10px 极淡灰；折叠成 64px 图标条时组标题换成一道 20px 短横线。
- 书影音和留言板**从此进侧边栏**（原先按 PLAN §4 只走页脚和 ⌘K），
  `EXTRA_PAGES` 因此删掉，`(site)/layout.tsx` 和 `sitemap.ts` 改成直接用 `NAV_ITEMS`。
- ⚠️ **用户列的分组里有「现在」，但站上没有 /now 页**（PLAN.md 明确 Now 是首页的一个板块，
  不单独成页），所以「写作」组只有博客和新闻两项，没有擅自新建页面。
  他的列表里也没提首页和联系，这两项按现状保留（首页置顶、联系归入「关于」组）。
- **侧栏结构改了一处**：分组之后这一列在 900px 高的窗口里装不下，
  原来整列一起滚会把底部的 中/EN · ⌘K 顶到视口外。现在改成
  **顶部 Logo 和底部那一行钉住、只有「导航 + 连接」中间那段滚动**。语言切换任何时候都点得到。
- 导航项高度 40 → 36px（13 项要装下）。英文的组名 `writing` 叫 **Words** 不叫 Writing ——
  组里正好有一项就叫 Writing，撞脸。英文的「N 帧 / N 辑」用了 ICU plural，不会出现 "1 albums"。
- 新增四个导航图标（photos/records/library/guestbook）在 `icons/NavIcon.tsx`，
  照画板那套笔法补的（线性、1.5 描边、20×20）；画板本身没画这几个。

**并行开发的分工**：唱片板块由另一个 Opus 会话同时在做（`/records/`），
**导航里的「唱片」这一项由本次加上，指向 `/records/`**，页面本身不归本次。
两边都动了 `messages/{zh,en}.json`，但键区不同（本次是 `nav.*` / `photos.*` / `pageMeta.photos`）。

**自查记录**：`npm run build` + `npx tsc --noEmit` + `npm run lint` 全过（43 个静态页）。
用 CDP 截图（**不是 --virtual-time-budget**，见前面那条教训）核对了
1440×900 和 390×844 两档的列表页、单辑页、放大态、移动端抽屉、折叠成 64px 的图标条，中英各一遍。

---

### 唱片板块 /records（2026-08-30，与摄影板块并行开发，commit ba23152）

参考 https://ungetsu.net/music/ 的形态，但**对方站其实没有播放器**（Apple Music Replay
榜单 + 外链），所以唱机是自己造的。用户明确要求不模仿其 UI。

**黑胶唱机（主角）**
- 唱片按真实 **33⅓ 转（1.8 秒一圈）**匀速转；暂停用 `animation-play-state: paused`，
  **停在原地**而不是弹回 0°。
- **唱臂角度由余弦定理解出**：「支点—盘心—针尖」三角形反算唱针落在半径 r 的沟槽上时
  唱臂该转多少度。所以「进度 = 唱针从外圈走到内圈」是真按半径线性走，不是角度硬插值。
- 点盘面任意一圈跳到那儿（外圈开头、内圈结尾）。唱片是 `role="slider"`，聚焦后
  空格播放暂停、←/→ ±5 秒、Home 回开头。**没有全局键盘监听**——这是会滚动的内容页，
  不能劫持空格。
- 中心标签是 WL 徽记 + 艺人名 + 33⅓ RPM · STEREO，跟着转；曲名放右侧信息栏（转起来才读得到）。

**播放内核抽成 `src/lib/useAudioPlayer.ts`，与放松区共用**
- 管换歌 / 播放意图 / 音量 / 外链失效标灰跳过 / 整组挂了退回常驻 / **同文档互斥**
  （谁开始播就把别人按停）。
- 放松区 `MusicDial.tsx` 从 183 行状态机缩到只剩界面，**时间表盘一个像素没动**
  （那是全站「时间」意象的一部分，用户要求保留）。已用真实 CDP 点击逐项回归：
  网易云直链出声、指针角度与进度弧更新、空格暂停、下一首、切「常驻」加载本地肖邦，
  全部与改造前一致。

**「我听的」专辑墙**
- 数据走 `content/music/records.json`，组件不写死。封面**下载到本地**
  `public/images/records/`（`scripts/fetch-record-covers.mjs`，`npm run records`），
  6 张 webp 共 136KB。
- 为什么不热链：网易云图床 `p1/p2.music.126.net` 实测**不校验 Referer**、
  `?param=WxH` 还能服务端缩图，技术上可行；但那是别人家的 CDN，链接带 hash，
  换图就整面墙开天窗，且静态导出 `images.unoptimized`，热链原图一张 3.7MB。
  没有封面的条目退化成**纯文字唱片套**（肖邦那条是活样本）。
- ⚠️ **7 条都基于可查证的事实**（赵传来自网易云歌单，周杰伦/张震岳来自 Spotify 歌单，
  4 张专辑是歌单里那几首歌的所属专辑，肖邦来自站上常驻曲库），**没有编造偏好**；
  但后 4 条更像「歌单里恰好能外链的歌」而非用户真正想展示的唱片，
  **待用户换成自己挑的**：改那个 json 后跑一次 `npm run records` 取封面。

**reduced-motion**：唱片不转，唱针位置照常表达进度，只是没有过渡动画。

**遗留 / 下一步**
- 第二轮待做（本轮为避免文件打架而后置）：**作品站内详情页**
  （大截图 + 功能介绍 + 技术栈 + 链接，让 claude-Anki 这种非网页项目也讲得清楚）
  与**首页加最新动态聚合**。
- 摄影照片仍是占位，等用户按 `docs/如何添加照片.md` 丢进 `photos-src/` 跑 `npm run photos`。

---

### 播放能力搬到全站层 + 右下角迷你播放器 + 放松区改名「专注」（2026-08-30）

**一、播放状态搬到导航不会卸载的层级**
- `<audio>` 和播放状态从唱片页搬到 `app/[locale]/layout.tsx` 的新 client Provider
  `src/components/player/PlayerProvider.tsx`——它是 `(site)` 和 `focus` 的共同祖先，
  客户端跳页这一层不会卸载，音乐不断。对外暴露 `usePlayer()`。
- `lib/useAudioPlayer.ts` 从「放松区 + 唱片页共用的内核」改造成「Provider 专用的内核」：
  删掉了原来为放松区音乐层准备的 `active`/`autoStart`/同文档互斥注册表（音乐层已撤，
  一个文档只可能有一个播放器）；新增网易云那组只放 **30 秒试听**（构建时算好起点，
  见 `lib/content.ts` 的 `previewStartOf`），常驻那组仍完整播放——**曲库持久留在全站层
  之后，让访客走到别的页面还在放别人整首没授权的歌，风险比放松区里"切走就停"时更大**，
  这是加试听窗口的直接原因。`Track` 类型新增可选的 `preview`/`platformUrl` 字段
  （`lib/types.ts`），`content/music/netease.json` 本身没有改动，脚本
  （`scripts/fetch-netease.mjs`）也没碰——那是并行的音乐数据层代理的地盘，
  可选的 `previewStart` 手填字段读不到就用默认公式，两边不会打架。
- SSR 安全：Provider 挂载时才渲染 `<audio>`，服务端渲染阶段没有播放状态可言，首帧不闪。

**二、右下角迷你播放器**（`src/components/player/MiniPlayer.tsx`）
- 小唱片（纯 CSS 材质，`.mini-vinyl` + 独立的 `.mini-vinyl-spin` 转动类——**没有复用**
  大唱片的 `.records-disc`，那条规则的 `transform-origin` 是按 SVG viewBox 坐标算的，
  套到 34px 的 div 上会绕着盘外一个点打转）+ 曲名/艺人 + 播放暂停 + 可拖动进度条
  （原生 `<input type=range>` 叠在自绘进度条上，键盘原生可操作）+ 关闭。
- **只在「碰过播放器」时出现**（`touched` = 播过或播过又暂停），没播过就不占地方、
  不拦点击；**唱片页和开场页不显示**（`HIDE_ON` 正则匹配 `/records` 和 locale 根路径）。
- 关闭 = `player.stop()`，真的停止播放并把状态清空（回到第一首、意图变回 `auto`），
  不是只隐藏卡片。
- 移动端 `bottom-[calc(...+env(safe-area-inset-bottom))]`，宽度 `min(300px, 100vw-1.5rem)`
  不会在小屏溢出。**没有注册全局空格键**——进度条只能点/拖/Tab 到再用方向键。
- `prefers-reduced-motion` 下小唱片不转（全局兜底规则 + 组件自己判断双重保险）。

**三、放松区改名「专注」（/lounge → /focus）**
- 中英文案、导航项（`lib/nav.ts`）、页面标题、`site.config.ts` 的 `lounge` 键全改成
  `focus`。**localStorage 键没有跟着改**（`lounge-notes`、番茄钟、场景选择那几个）——
  改名字不该让老用户已经写下的手记或番茄钟记录凭空消失。
- **音乐层（原 `MusicDial.tsx`）整个删除**，番茄钟、手记、时刻表三层像素级不动
  （`FocusStage.tsx` 只删了 `music` 这个 tab 分支，其余逻辑原样平移）。`clock()`
  这个 mm:ss 格式化函数从 `components/lounge/dial.tsx` 挪到新文件 `lib/clock.ts`——
  唱片页和迷你播放器也要用它，不该为一个字符串函数去 import 一整套表盘几何。
- `/lounge/` 留了跳转页（`app/[locale]/lounge/page.tsx`）：`<meta http-equiv="refresh">`
  立即跳到 `/focus/`，`robots: noindex`，外加一句人话和手动链接兜底。sitemap 只收
  `/focus/`（从 `NAV_ITEMS` 生成，那儿已经改了）。

**四、纪律相关的一处返工**

> ⚠️ **这一节后来被推翻了，见本文件下一节。** 当时判断「工作树里那份透视唱机视觉稿是
> 越界的野生改动」并撤回了它 —— 实际上那是**另一份并行任务书里点名要做的第一项**
> （用户拿了张黑胶实拍图，要求「区域再大一点、真实一点、斜放透视」）。两份任务书对
> 「唱片页视觉归谁做」的说法冲突，不是谁写错了代码。视觉那部分已经重新落回仓库并提交。

- 开工时发现工作树里已经有一份未提交的**唱片页视觉大改**（黑胶从平面 SVG 改成
  透视 3D、`Turntable.tsx` 776→973 行、`globals.css` 新增两百多行 `.vinyl-*`
  材质），与本次任务边界写明的"唱片页视觉后续代理重做，这次只改取状态的方式，
  别顺手重构"正面冲突。**已撤回**：`Turntable.tsx` 和 `globals.css` 都退回上一个
  commit（`5581666`）的版本，只在其上做最小的取数据方式改动（`usePlayer()` 替掉
  `useAudioPlayer()` + 自己的 `<audio>`，`useProgressPainter` 调用改成用窗口化的
  `windowStart`/`total`）。**那份 3D 视觉稿没有删**，存在
  `Turntable.uncommitted-3d.tsx` / `globals.uncommitted.css`（本机 scratchpad 里，
  没进仓库）——后续真要做透视唱机可以直接拿去参考，只是这次不让它跟着这次提交混进去。
  同理还发现 `scripts/fetch-netease.mjs` / `content/music/netease.json` 被动过
  （加了保留 `previewStart` 手填值的逻辑），**已退回**，因为这两个文件是并行音乐数据层
  代理的地盘。
- 顺手清理了 `messages/{zh,en}.json` 里跟着那份 3D 视觉稿一起加的孤儿翻译键
  （`records.player.sideA/labelPreview/labelFull/labelFoot/previewTag/fullVersion/previewNote`），
  以及站点级 `meta.description` 里残留的"放松区/lounge"字样。

**自查记录**：`npm run build` / `npx tsc --noEmit` / `npm run lint` 全过。
生产静态产物起本地服务器后用 CDP 做了真实交互（不是 `--virtual-time-budget`）：
唱片页点播放真的出声 → 点站内链接客户端跳到 `/about` → 音频 `currentTime` 连续没断
→ 右下角迷你卡片出现、文案正确、能暂停、能拖进度条 → 回到 `/records` 迷你卡片自动隐藏
→ 再跳出去点关闭按钮，`audio.paused` 变 `true` 且卡片消失。专注区确认三个 tab
只剩番茄钟/手记/时刻表，`/lounge/` 确认能跳到 `/focus/`。1440×900 和 390×844
两档、中英文都过了一遍，移动端迷你卡片不溢出视口。

**遗留**
- 未提交的透视唱机视觉稿（见上）留在本机 scratchpad，没有进仓库，
  想继续做的话要重新从那两个文件搬。
- `content/music/records.json` 那 7 条唱片墙数据还是「歌单里恰好能外链的歌」，
  上一阶段就记过了，仍待用户换成自己真正想展示的唱片。

---

### 唱片重做（斜放透视）+ 30 秒试听（2026-08-30，与上一节同一天、两个会话并行）

上一节的播放层搬迁（Provider / 迷你播放器 / 放松区改名「专注」）和这一节是**两个终端
同时在做的**，落到仓库里的是两边的并集：播放层用上一节那套，唱片页的视觉是这一节。
上一节 §四 那条「已撤回」因此作废 —— 详见那里加的更正框。

**一、黑胶从「正对着看的平面 SVG」改成「真的斜放在透视里」**

用户给了张黑胶实拍参考图，点名要学的是：沟槽的密纹与沿纹路扫过的高光、盘边的厚度、
奶白色标签、打在台面上的投影、暗调单侧光；并明确「区域再大一点」「真实一点」「斜放透视」。

- **透视怎么搭的**：`.vinyl-scene` 给 `perspective: 1500px`，里层 `.vinyl-plane` 整个
  `rotateX(25deg)` 并开 `preserve-3d`。**盘、盘的厚度、唱臂都是这个平面的孩子** ——
  所以唱臂天然和盘在同一个透视里，不会穿帮；唱片自转只是平面内的 `rotate`，
  投影出来自然就是椭圆轨迹（不用手写椭圆插值）。唱臂再 `translateZ(12px)` 浮在盘面上方，
  加一道 drop-shadow 落在盘上。
- **唱臂角度的余弦定理解法原样保留**，只是把结果画进了倾斜后的坐标系里。
  几何重排过：R=390 / 起播槽 372 / 收尾槽 168 / 标签 162 / 支点 (440,−300) / 臂长 575，
  唱针从外圈走到内圈扫过约 22°，静止时停在盘外 42 单位处的托架上。
- **点盘面跳进度要反投影**：屏幕坐标先解回平面坐标才知道点的是第几圈
  （`planeFromScreen`，见 Turntable.tsx 里的推导注释）。这依赖两个前提 ——
  perspective 的值、以及 `perspective-origin` 正好落在盘心。
  **改 globals.css 里 `.vinyl-scene` 的 perspective 必须同步改组件里的 `PERSPECTIVE`。**
- **材质全在 CSS**（globals.css 顶部有结构图）：沟槽是两层 `repeating-radial-gradient`
  （3px 一道密纹 + 27px 一道分轨亮环），导入槽/收尾槽两圈没有纹路的平面，
  一层**不跟着转**的 conic 单侧光（灯是不动的，转的是碟），外加一道 9 秒一圈的柔光慢扫。
  盘的厚度是同一张盘沿自己的法线 `translateZ(-6px / -13px)` 压下去两层 ——
  斜放之后它们落在盘的正下方，露出来的那一弯就是盘边。盘背后还有一小片光，
  黑盘挡住它的中间，挡出来的就是影子。
  ⚠️ 收尾槽那一环**不能盖到标签上**（标签在下面一层），盖上去奶白就变黑了 ——
  第一版就是这么翻的车。
- **尺寸**：唱机整块改成暗色台面（`bg-shell` + 左上单侧光），盘横跨整块面板的宽度。
  1440 视口下盘的直径从原来的约 300px 涨到约 500px（面积 2.8 倍），
  390 手机上盘完整可见不被裁。
- **标签印这张碟的曲目表**（参考图就是这样）：奶白纸底、WEILIANG 名号、碟名、
  中心孔右边 `SIDE A / 33⅓ RPM / STEREO`、左边一枚 WL 厂牌小徽记、5 行曲目、
  底下一行「共 N 首 · 试听 30 秒 / 完整播放」。曲目表跟着当前这首**滑动**（5 行的窗口），
  正在放的那首加粗 —— 这样转到哪儿标签上都看得见。静止时读得清，转起来自然模糊，
  这是真实的，没去补偿。当前曲名同时在侧边信息栏，转动时也能读。
- **盘小了就换一版标签**：用**容器查询**（`.vinyl-frame` 上的 `container-type`），
  盘所在那一栏窄于 430px 时曲目表让位给只印大字那一版 —— 那个尺寸下曲目表只是一片灰点。
  看的是栏宽不是视口宽，所以侧栏折叠、平板分栏都能自适应。
- `prefers-reduced-motion`：不转（全局兜底规则），但透视、材质、厚度全都在。

**二、网易云那组只放 30 秒（常驻那组不受限制，完整播放）**

- **为什么**：站内放的是别人的歌。播放能力搬到全站层之后，访客走到任何一页都还在放，
  放整首的风险比原来「切走就停」时大得多。所以网易云那组每首只放 30 秒，
  自托管的常驻古典乐是公共领域录音，版权干净，完整播放、也不显示任何试听文案。
- **从哪儿开始**：**不从 0 秒起**（多数歌前 30 秒是前奏，从头放等于放了个空）。
  默认取「时长的 30%」，但不超过 60 秒。构建时算好写进 `Track.preview`
  （`lib/content.ts` 的 `previewStartOf`）。站主想给某首手挑一段，
  在 `content/music/netease.json` 那首上加 `"previewStart": 秒数` 覆盖，
  文件里的 `previewNote` 写了怎么填；`scripts/fetch-netease.mjs` 重跑时会**按 id 保留**
  这些手填值（那个脚本会整份重写 json，不保留的话手调一次就没了）。
- **「丝滑」是硬指标**，做法：
  - 播放期间跑一个 rAF 的**音量包络**（不是 `timeupdate`—— 那玩意 250ms 才来一次，
    拿它做淡出会一格一格地掉）。从歌中间切进去要淡入（0.45 秒），
    到点前 **1.5 秒淡出到 0**，再自动换下一首 —— 听感上是「这段放完了」而不是「网页坏了」。
  - 没接 WebAudio 的 GainNode：一旦把 `<audio>` 接进 WebAudio 图，
    跨域且没有 CORS 的音源（网易云直链）会被静音。所以是直接改 `el.volume`。
  - 兜底：万一那一跳没跳成（音源不支持 Range 请求），3 秒后就地把窗口锚点改成
    「从现在算 30 秒」，免得播放头永远够不着窗口、既不出声也不往下走。
- **进度条表达的是这 30 秒**，不是整首歌：`elapsed`/`total`/`seek()` 全部窗口化
  （`useAudioPlayer` 里的 `window_`），唱针也走这 30 秒。曲目清单里给的仍是**整首歌**
  的长度 —— 清单说的是歌，不是片段。
- **去平台听完整版**：`Track.platformUrl` 指向该曲的网易云单曲页，
  唱机上（试听角标旁）和迷你卡片里各有一个入口，另有一句话说明「最后一秒半会自己淡出，
  不是播放器坏了」。

**自查记录**：`npm run build` / `npx tsc --noEmit` / `npm run lint` 全过。
生产静态产物（`out/`，起本地静态服务器）上用 CDP 做真实交互，不是 `--virtual-time-budget`：
- 1440×900 与 390×844 两档、静止态和播放态各截图核对了透视、沟槽、盘边厚度、
  唱臂落点与标签曲目表；手机上盘完整在视口内（308px 宽的场景，left 41 / right 349），
  标签自动换成大字版。
- 真实点击播放：网易云那首 `currentTime` 从 **60 秒**起播（= min(281×30%, 60)），
  duration 281 与清单一致；把播放头挪到窗口末尾，实测音量
  0.7 → 0.68 → 0.49 → 0.30 → 0.12 → 0 平滑淡出，30 秒到点自动换下一首并淡入 ✓。
- 常驻那组：`src` 是自托管的 mp3，从 0 秒起播、总时长显示 04:36（不是 00:30）、
  没有任何试听文案 ✓。
- 从唱片页客户端跳到 `/zh/blog/`：`audio` 元素全站只有 1 个、没断、右下角卡片出现，
  拖进度条到 80% → `currentTime` 精确落到 84 秒（60+0.8×30）✓，暂停 ✓，
  关闭后 `paused=true` 且卡片消失 ✓；回到唱片页卡片自动隐藏 ✓。
- 专注区三个 tab 只剩番茄钟/手记/时刻表、番茄钟表盘照旧；`/zh/lounge/` 跳到 `/zh/focus/` ✓；
  中英各走了一遍，没有漏翻的键。
- ⚠️ **headless Chrome 的坑（记一笔省得下次误判）**：不加 `--mute-audio` 时，
  媒体播放会在**六七秒后毫无征兆地 `pause`**（没有 error、没有 stalled、readyState 还是 4），
  自托管的本地文件也一样。这是无音频设备的 headless 假象，不是代码问题 ——
  加上 `--mute-audio` 就一路播下去。别据此去改播放逻辑。

**遗留 / 注意**
- **两个会话并行改同一批文件，来回覆盖过好几轮**（我这边的 `globals.css`、
  `Turntable.tsx`、`messages/*.json`、`netease.json`、`fetch-netease.mjs` 都被覆盖过，
  又都恢复了）。最终提交的是两边的并集，构建和真实交互都验过。
  **以后同一批文件不要并行开两个终端**（PLAN.md §8 的规则本来就写着串行）。
- 第三个会话已经提交了 `c126999`（音乐数据层改用 Apple 官方 30 秒预览、按心情场景重排歌单，
  产出 `content/music/chart.json`），但**那份数据还没接进 UI**（它自己也写了「UI 迁移留给
  后续开发」）。真接进来之后，**本节的试听窗口逻辑大部分可以退休**：
  Apple 的 previewUrl 本身就是 30 秒的独立文件，不需要 seek 到中间、也不需要到点淡出，
  只剩淡入和「去平台听完整版」的外链还有用。`Track.preview` 那套是为网易云整首直链设计的。
- 唱片墙 `content/music/records.json` 那 7 条仍待用户换成自己真正想展示的唱片（上一阶段就记过）。

---

### 榜单接进唱片页 + 唱机去掉深色容器、盘心换封面、信息栏重排（2026-08-31）

接手上一轮中断的半成品：6 个未提交改动（音源从网易云切到 Apple 官方 30 秒预览，`MusicLibrary`
从 `{resident, netease}` 改成 `{resident, scenes, residentCredit}`）先补完了唯一的编译错误
（`records/page.tsx` 还在读已经不存在的 `music.netease`），然后做完「把榜单接进唱片页」这项
上一轮标注为「后续开发处理」的工作，再叠加用户看了截图后追加的三处视觉修改。

**一、榜单接进唱片页，撤掉「我听的」专辑墙**
- 新组件 `src/components/records/Chart.tsx`：按 `content/music/chart.json` 的 4 个心情场景
  分组渲染，每行 = 封面缩略图（内容图片，原色）+ 歌名 + 艺人 + 时长；整行是一个 `<button>`，
  点击 `player.playAt(scene.key, index)` 装到唱机上播放；平台链接是行尾**单独一个 `<a>`**
  （没有嵌进 `<button>` 里——嵌套交互元素不合法，也分不清"试听"和"去平台"两个动作），
  桌面显示"在 Apple Music 打开 ↗"，手机窄屏收成一个"↗"图标省地方。正在播放的行有
  选中态（`aria-pressed` + 底色），外链失效的行标灰禁用（复用 `player.broken`）。
  上一首/下一首在当前场景组内流转是 `PlayerProvider` 的既有行为（`goto`/`jump` 都在"当前这组
  tracks"里取模），接榜单不需要改这部分。
- `Turntable.tsx` 自己的可展开曲目清单**只留了常驻组**（4 首肖邦）——场景组已经被 Chart
  更完整地覆盖（带封面、带外链），两处重复列同一批歌没有意义。
- **撤掉了「我听的」专辑墙**（`RecordShelf` + `getRecords()`）：`records/page.tsx` 不再导入
  也不再渲染它们。`RecordShelf.tsx`、`getRecords()`、`RecordItem` 类型、
  `content/music/records.json`、`npm run records` 脚本**全部保留不删**，想回退随时能接回来
  （docs/素材清单.md 已同步这条）。

**二、用户看了截图后要求的三处修改**
1. **去掉唱机的深色容器**。原来 `Turntable` 外层是 `<section className="border border-shell-line
   bg-shell text-shell-ink">`——`shell-*` 是给暗侧（侧边栏/开场页）用的深色系 tokens，套在
   这儿就把唱机连同浅灰内容区割裂成两块。现在唱片、唱臂直接坐在 `<main>` 的 `bg-content`
   浅色渐变上，`Turntable` 全文再没有一处 `shell-*`。
   - **投影重做**：新增 `globals.css` 的 `.vinyl-floor-shadow`——一层**不进 3D 透视平面**的
     扁平灰色模糊椭圆，垫在唱片正下方，这是让唱片"落在页面上"的关键；原来叫 `.vinyl-pool`
     那层贴着盘身的近影，深底时代是白色轮廓光，现在改成真的深灰接触阴影（`.vinyl-shadow`）。
   - **唱臂重新配色**：支点座、配重这几处落在盘外、直接坐在浅色页面背景上的部件，原来的
     近黑色（`#161616`/`#1E1E1E`/`#0A0A0A` 这个量级）是为了在深色台面上"隐进"背景，浅底上
     反而显得又重又突兀，统一调亮一档（`#2A2A2A`~`#4E4E4E` 这个量级，材质本身还是深色金属，
     不是变浅色）。
   - 顺手改了一处潜在的对比度 bug：唱片的 `focus-visible:outline` 原来用的是
     `outline-shell-ink`（浅色，在深底容器里才看得见），浅底上这个描边会几乎消失，
     已经改成 `outline-ink`。
2. **唱片中心从纸标签换成专辑封面**：`svg.vinyl-label` 现在按 `track.cover` 分支——有封面
   就用 `<image>` + `clipPath` 圆形裁切嵌进盘心（内容图片，彩色，豁免全站黑白规则），
   只留一个轴孔；换歌就换封面。**常驻那组没有 `cover` 字段，原样回退到奶白纸标签**
   （印 WEILIANG 名号 / SIDE A / 曲目表那版），一行代码没改，不开天窗。
3. **副标题文案换了**：`records.lead` 中文改成用户指定的「享受音乐的放松时刻，让自己慢下来
   感受自己寻找自己的内心深处。」；英文按意思意译成
   "A quiet moment for music — slow down, and listen your way back to yourself."
   （不是逐字直译，直译会很怪）。

**三、右侧信息栏重排 + 16 句歌曲描述（代拟）**
- `Turntable` 从"唱片在上、信息栏在下、prev/play/next 挤在标题右边"改成**两栏布局**
  （`sm:` 起 `grid-cols-[1.35fr_1fr]`，disc 在左、信息栏在右；窄屏堆叠），信息栏按参考图的
  层级重排：**分类小标（当前碟名，淡灰小字，用户拍板不用暖色）→ 大字歌名 → 艺人 → 专辑名 →
  一句描述 → 播放控件（prev/play/next）+ 去平台链接**。原来的"正在播放/唱针已抬起"状态字挪到
  时间行里，不再跟分类小标挤在一起变成两行重复的小灰字。
- `Track` 类型新增 `desc`/`descEn`（`src/lib/types.ts`），`content.ts` 的 `getMusic()` 透传；
  `content/music/chart.json` 的全部 16 首歌都加了这两个字段 + `descDraft: true`
  （**显式标明是 AI 代拟的初稿**，页面不读这个字段、不展示给访客，纯粹是给站主看的元数据）。
  描述只依据**编曲、乐器、节奏、公认的情绪基调**这些可查证的特征写（比如"R&B 底子的抒情
  流行曲，木吉他分解和弦托着慵懒的哼唱，副歌层层叠出清爽的夏日感"），**没有编造这些歌对
  站主的私人意义**——这是个人站，别人代写的感受不该冒充成他自己的，`docs/如何加歌.md`
  新增一节写清楚了这件事和站主该怎么换成自己的话。
- `scripts/fetch-music-chart.mjs` 重跑会整份重写 `chart.json`，为了不冲掉手改的描述，
  加了跟 `fetch-netease.mjs` 保留 `previewStart` 同一个做法：先读旧文件，按
  **场景 key + 歌曲原始 query**（不是网易云那种数字 id，chart 这边没有稳定 id，只有
  `music-chart.txt` 里那行原文）记一份 `desc`/`descEn`/`descDraft`，写新文件时贴回去。
  剪切歌到别的场景、或改了 `music-chart.txt` 里那行原文的话，这条"记忆"会对不上，
  重新生成时那首会没有描述——损失的只是那一首，重写一句就好。

**四、messages 清理**
- `records.lead`/`records.footnote` 更新；`records.player.chartNote` 补上了（这是上一轮
  半成品里就在用但消息文件里根本没有的 key，之前会在运行时报缺键——一并修了）；
  `records.player.fullVersion` 从"去网易云听完整版"改成"去 Apple Music 听完整版"（音源已经
  换了，文案没跟上）；删掉了三个不再使用的孤儿键（`groupNetease`/`neteaseNote`/`fullPlaylist`）；
  新增 `records.chart.*`（title/note/playRow/openPlatform）；`records.wall.*` 整段删掉
  （专辑墙撤了，这些字符串不会再被读到）。zh/en 两份字典键结构用脚本比对过，完全一致。

**五、真实交互自查（生产静态产物，headless Chrome + CDP，`--mute-audio`）**
用 Node 内置 `WebSocket`/`fetch` 手写了一个轻量 CDP 驱动（没装 puppeteer/playwright，
这台机器的沙盒连不上 npm registry 装新包），点击一律走 `Input.dispatchMouseEvent`
派发真实鼠标事件（不是 `el.click()`）——后者没有 user-gesture，会被浏览器的自动播放策略
拦下 `audio.play()`，之前一版用 `el.click()` 测出来"点了也不播"，换成真实鼠标事件后确认
是测试方法的问题，不是代码问题。
- **1440×900，全新浏览器 profile（没有 localStorage）首次访问**：默认停在"深度专注"组
  第一首《七里香》，封面正确显示在盘心，信息栏文案层级、播放控件、换碟 chips 全部符合预期。
- **深色方块排查**：沿唱机 section 的祖先链一路查 `background-color`，从 section 到 `<main>`
  全部是透明的，唯一的 `rgb(10,10,10)` 是最外层侧边栏容器（`SiteShell` 的 `bg-shell`）——
  那是侧边栏本来就该有的暗色，不是唱机的容器，确认深色方块已经不存在。
- **点击榜单一行（真实鼠标事件）**：`audio.paused` 变 `false`（播放意图正确触发），
  `coverHref` 与该曲 `chart.json` 里的 `cover` 一致，分类小标显示"深度专注"。
  ⚠️ **等待后 `currentTime` 停在 0**（`error` 为 `null`，请求像是被静默挂起）——
  用 `curl` 直连同一条 `audio-ssl.itunes.apple.com` 直链在这台机器上同样没有响应，
  说明**这个沙盒环境本身连不上 Apple 的 CDN**，和 docs 里早就记录的"未验证国内网络能否
  裸连"是同一类风险，不是这轮改动引入的问题——上线前仍建议站主在真实网络下自己点一下。
- **组内下一首**：点击后标题从《七里香》变成《Hold on...》，分类小标仍是"深度专注"——
  确认上一首/下一首在场景组内正确流转，没有跳组。
- **切到常驻**：`hasCoverImage` 变 `false`、`hasPaperLabelText` 变 `true`——确认盘心正确
  回退成纸标签。点大播放/暂停按钮（真实鼠标事件）后 `audio.paused=false`、
  `currentTime` 持续增长、`duration≈276.6s`（对应"夜曲 作品9第2号"整首时长，不是 30 秒）——
  常驻这组是本地 mp3，在这个沙盒里能连通，确认播放内核本身完全正常。
- **展开常驻曲目清单**：4 首肖邦，当前播放那行有 `▸` 标记和高亮。
- **跨页不断流**：常驻正在放的时候，真实点击侧边栏到 `/about` 的链接（客户端路由，不是
  整页刷新），`currentTime` 从 2.84s 走到 4.14s（连续没断），迷你播放器出现，文案/进度条
  正确；点迷你播放器的关闭按钮后 `audio.paused` 变 `true`。
- **移动端 390×844**：无横向溢出（`scrollWidth` 没超过 `clientWidth`），侧边栏收成顶部条，
  唱机单栏堆叠（不是挤压的两栏），Chart 的外链在窄屏收成"↗"图标。
- **英文页 /en/records/**：`lead` 是意译版英文，"Listen by Mood"、"RESIDENT"、
  "Open in Apple Music ↗"、chip 标签（"Deep Focus"/"On the Commute"/"Pomodoro Break"/
  "Late-Night Wind-down"）全部正确显示，没有漏翻的键。
- `npm run build` / `npx tsc --noEmit` / `npm run lint` 全过（lint 剩 2 条**上一轮半成品
  就带着的**警告，跟这轮改动无关，见下面「遗留问题」）。

**遗留问题（发现但没修）**
- `npm run lint` 有 2 条**警告**（不是错误，`npm run lint` 退出码是 0）：
  `scripts/fetch-music-chart.mjs` 里 `slugFromQuery` 定义了没用到；
  `src/components/player/PlayerProvider.tsx` 第 72 行的 `tracks` 三元表达式让 `useMemo`
  的依赖每次渲染都可能变。这两处都是接手时那 6 个未提交改动自带的，
  按任务边界"除非接榜单必须改，否则别动 `player/**`"没有碰 `PlayerProvider.tsx`；
  `fetch-music-chart.mjs` 虽然这轮改了（加保留描述的逻辑），但 `slugFromQuery` 是改动前
  就有的死代码，跟这轮功能无关，一并留给以后处理。
- Apple 预览直链在**这个开发沙盒**里连不上（见上面 QA 记录），无法在此环境验证真实出声，
  这是环境限制不是代码问题，`docs/如何加歌.md` 已经提醒过站主上线前自测。
- 唱片墙 `content/music/records.json` 那 7 条仍然是"歌单里恰好能外链的歌"而非用户精选
  （历史遗留问题，这轮没有改动它，只是页面不再渲染它）。

---

### 摄影板块换成真照片（2026-08-31）

用户给了 `~/摄影/个人网页上传的作品/`（旅行照片 + 成长照片/视频）。**两辑渐变色块占位已删**，
现在摄影板块是 **4 辑 / 21 张真照片**，全部 `kind: "archive"`：

| slug | 标题 | 日期 | 张数 | 内容 |
|---|---|---|---|---|
| `beijing-2023` | 北京 | 2023.05.01 — 05.03 | 7 | 天安门夜景、清晨列队、保和殿、宝座、畅音阁、豆汁焦圈、牛肉面 |
| `qingdao-2024` | 青岛 | 2024 | 4 | 五四广场《五月的风》、候船区万国旗、礁石、蓝调时刻 |
| `shanghai-2025` | 上海 | 2025 | 6 | 陆家嘴、武康大楼、宋庆龄故居、傍晚车流、UFC 上海、东方明珠 |
| `sport-2023-2024` | 格斗与马拉松 | 2023.10.02 — 2024.03 | 4 | TWF 竞武决合影、赛前地铁、冲线、赛后成绩短信 |

**几个决定**

- **成长素材并入摄影板块当一辑档案**（用户选的）。格斗 + 马拉松合成 `sport-2023-2024` 一辑，
  按时间排：擂台 → 赛前 → 冲线 → 成绩。那篇格斗自述（源目录里的 `2026-08-30.md`）
  **没有进 `content/posts/`**，它的内容浓缩进了这一辑的简介和第一张的图注。
- **每张都写了双语 caption**，写的时候只认图里看得见的东西：
  匾额上写着「壶天宣豫 / 导和怡泰」才敢叫畅音阁，「保和殿」那张有蓝底匾。
  拿不准的（那张宝座具体是哪座殿）只描述不点名。**站主自己知道的话直接改 json 就行。**
- **青岛和上海的 `date` 只填了年份**（`"2024"` / `"2025"`）。源文件夹名里只有年份，
  照片 EXIF 也被抹干净了（应该是从社交软件导出的），没法编一个日期出来。
  `albumDates()` 对年份字符串是安全的，页面显示成 `2024`；年份分组也照常。
- **文件名带序号**（`01-…` `02-…`），因为 `build-photos.mjs` 按文件名排序，
  而且输出的 webp 直接用这个名字。想调顺序改文件名重跑即可。
- 压完 4.6MB 进仓库（原图 8.8MB，最大一张压完 100KB 出头）。

**遗留**

- ⚠️ **格斗那两段视频没上站**（`741a53dd….mp4` 13 秒 / `90616eaf….mp4` 6 秒，竖屏 **HEVC**）。
  摄影板块只吃图片，`/videos` 是 B 站嵌入位、放不了自托管文件，所以没有去处。
  真要上：先 `ffmpeg -i in.mp4 -c:v libx264 …` 转 H.264（HEVC 在 Chrome/Firefox 上多半黑屏），
  再决定放哪 —— 最省事的是写成一篇博客把 `<video>` 内联进去。原文件还在源目录。
- **列表页上半部的「专题」现在是空的**（四辑都是 archive），所以摄影页首屏没有大图，
  是一张纯清单。想要大图卡片，把某一辑的 `"kind"` 改成 `"feature"`。
- ⚠️ **本次没有 commit，也没跑通 `npm run build`** —— 挡路的是**另一场会话未提交的音乐改动**：
  `src/lib/types.ts` 里 `MusicLibrary` 去掉了 `netease`，但 `src/app/[locale]/(site)/records/page.tsx:42`
  还在读它，`tsc` 只报这一条错。照片这边是用 dev server（:3111）实际验的：
  中英文列表页、三个单辑页、缩略图/展示图 HTTP 200、caption 进了 alt、
  年份分组 2025/2024/2023 正确、⌘K 和页面里都没有 placeholder 残留。
  那条音乐错误修掉之后跑一次 `npm run build` 再提交。

> **⚠️ 更正（同日，2026-08-31）：上面那一节里的 `sport-2023-2024`「格斗与马拉松」已整辑删除。**
> 用户当天看过之后要求去掉，`content/photos/sport-2023-2024.json`、
> `public/images/photos/sport-2023-2024/`、`photos-src/sport-2023-2024/` 三处都清了
> （`photos-src/` 那份也要删，不然下次 `npm run photos` 会把整辑重新生出来）。
> **摄影板块现在是 3 辑 / 17 张：北京 7、青岛 4、上海 6。**
> 原始素材没动，还在 `~/摄影/个人网页上传的作品/个人成长照片/运动方面经历/`；
> 那两段 HEVC 视频和那篇格斗自述因此也一并**不在站上**，见 docs/素材清单.md 里的那条 ⬜。

---

### 开场页重做：外滩照片 + 视差 → 代码生成的「精密仪器」（2026-08-31）

站主拍板了一版新稿子：`design-v2/Main.dc.html`（1440×900 画板，**这轮开场页视觉的唯一依据**，
比 `docs/design/Intro.dc.html` 新）。上一版是上海外滩夜景照片 + 深度图 WebGL 视差
（`CityDepth.tsx`），这版换成纯代码生成的抽象仪器，不再有任何位图背景。

**删除**

- `src/components/intro/CityDepth.tsx`（WebGL 视差着色器组件）
- `public/images/intro/shanghai.webp`、`public/images/intro/shanghai-depth.jpg`（连带空目录）
- `scripts/generate-intro-assets.mjs`（只为生成上面两张图存在的一次性脚本）
- messages 里 `intro.backdropAlt`、`intro.nameLabel`、`intro.sinceLabel`、
  `intro.enterHintPrefix`、`intro.enterKey`、`intro.logoAlt`（画板新版排版里没有
  NAME/SINCE 这两块元信息，也没有进入按钮的文字提示，钟面上也不再叠 Logo）

**新背景**（画板定稿值，全部照搬）：底色 `#070707`（同步改了 `--color-void` token，
之前是 `#060606`）；主光 `radial-gradient(66% 58% at 78% 30%, …)`；回光
`radial-gradient(48% 44% at 12% 88%, …)`；颗粒**两层**叠加（`baseFrequency 1.4/numOctaves 2/
opacity .035/screen` + `baseFrequency 0.28/numOctaves 4/opacity .05/overlay`，单层会像电子噪点）；
暗角 `radial-gradient(130% 130% at 62% 40%, transparent 50%, rgba(0,0,0,.6) 100%)`。
`Grain.tsx` 加了 `numOctaves`/`blend` 两个可选 prop 来支持这两层不同参数，不传就还是
原来单层 screen 的默认行为，放松区背景（`SceneBackdrop.tsx`）那处调用没受影响。

**时钟 → 精密仪器**（`TimeDial.tsx` 整个重写，文件名没变，对外 props 去掉了不再需要的
`logoAlt`）：圆心 (1196,392) r=302，六层同心环、120 道外圈细密刻度（每 10/5 道加长加亮）、
60 道内圈刻度、12 个蚀刻方位数字（每 3 个亮一档）、右下 (1196,558) r=52 副盘、三层中心轴，
坐标/透明度/线宽全部照画板 `renderVals()` 的公式移植成模块级常量数组（`FINE_TICKS` /
`INNER_TICKS` / `NUMERALS` / `SUB_TICKS`），一次算好不必每帧重算。

**响应式缩放**：整个仪器是一个 `viewBox="0 0 1440 900"` 的 SVG，
`preserveAspectRatio="xMaxYMid slice"` 铺满容器（`absolute inset-0 h-full w-full`）——
不需要在 JS 里重算任何像素坐标，缩放和裁切全部交给 SVG 自己的视口计算。选 `xMax`（而不是
默认的 `xMid`）是关键：验证过如果用 `xMid`，窄屏裁出来的可视窗口会把整个仪器（在原始
1440 宽度里偏在 83% 处）直接裁没；`xMax` 把仪器所在的右侧贴住容器右边，构图缩小但「被右
边缘裁掉」的意图在任何宽高比下都保留。390×844 手机截图验证：仪器缩小、依然裁在右边，
构图没有塌。

**指针功能全部保留，只是换了个映射**：画板的静态示意图只画了「一组主指针」，但要保留旧版
三档转速的倒转效果（时针半圈/分针 6 圈/秒针 14 圈），所以做了取舍——
主指针（长线+尾巴，画板里那组）← 分针角度；对角短指针（十字准线下方那根斜线）← 时针角度；
右下副盘指针 ← 秒针角度（正好是钟表上「跑秒小表盘」的经典用法）。真实时间、
`performance.now()` 驱动的 p³ 缓动倒转、`prefers-reduced-motion` 降级成 20 秒对一次表且隐藏
副盘指针、SSR 首帧固定 10:09:36（避免 hydration mismatch）——这套逻辑原样保留，只是
`setAttribute` 写的目标从三根传统指针换成了三组新几何。倒转时的「发光」不再变暖色（旧版
是钟融进城市灯火那个色调，这版城市背景已经删了），改成纯白强度变化，没有引入任何新的
彩色，符合「全站黑白灰，例外只有开场页那一下上色」——而这一下上色本来就是绑定 CityDepth
的效果，CityDepth 删了，例外也就跟着没有了。

**排版按画板重排**（`IntroScreen.tsx` 大改）：左上 (92,56) 小标签、左下（left 92 / bottom
148）大标题「活出自己想要的人生」（Noto Serif SC **font-weight 200**，为此在
`src/app/[locale]/layout.tsx` 的 `Noto_Serif_SC` 配置里加了 `"200"` 这个字重，原来只有
300/400）+「加油」+ 英文小字，左下页脚。断点用 `sm:`/`lg:` 做渐进缩放（画板只给了 1440
桌面版数值，移动端自己推导，PLAN.md §5 允许）。语言切换和旧版一样保留（画板没画这个功能性
控件，但站是双语站，去掉会让直接落地页的英文访客没有退路），挪到右上角避免和新的左上角
小标签打架。

**去掉进入引导**：删掉了「穿梭时间 · TIME TRAVEL / 走进我的世界 · STEP INSIDE」那个带框
提示和「或按 Enter ↵」。整页仍然可点即进（`onClick` 挂在 `<main>`，点击冒泡到根节点，
不依赖具体点在哪个元素上）。**无障碍补了一个覆盖全页的 `<button>`**（`aria-label` 用
`intro.enter`，中文改成了更贴切的「进入网站」、英文「Enter site」，因为它现在是纯粹的
aria-label 而不是可见按钮文案），可以被 Tab 聚焦、Enter/Space 触发（原生 `<button>` 自带），
聚焦时有清晰的焦点环。

⚠️ **踩了一个 Tailwind v4 的坑，记一下**：一开始焦点环用 `outline-none focus-visible:outline-2
focus-visible:outline-offset-[-6px] focus-visible:outline-shell-ink`，CDP 实测 `:focus-visible`
确实匹配了，但 `getComputedStyle(el).outlineStyle` 死活是 `none`，环没画出来。原因是 Tailwind v4
的 `outline-none` 会把 `--tw-outline-style` 这个自定义属性**直接钉死成 `none`**（不是只在
未聚焦时生效），而 `focus-visible:outline-2` 只设置 `outline-width`，最终的
`outline-style: var(--tw-outline-style)` 解出来还是 `none`——两条规则的 specificity 高低根本
不影响这个变量的级联结果。**结论：`outline-none` 和 `focus-visible:outline*` 系列不要一起用**，
直接去掉 `outline-none`（默认初始值就是 `none`，不聚焦时本来就不会有环），问题解决，
CDP 复测 `outlineStyle` 变成 `solid`。

**自查（生产静态产物 `out/`，Python `http.server` 起静态服务 + headless Chrome CDP，
`--mute-audio`，手写 WebSocket/CDP 驱动，鼠标点击走 `Input.dispatchMouseEvent` 真实事件、
键盘走 `Input.dispatchKeyEvent`）**：
- 1440×900：截图逐项比对画板——六层同心环、120+60 道刻度疏密、12 个方位数字及其亮度分档、
  右下副盘、裁切位置、字号字距位置，均对上；`document.body` 背景色实测 `rgb(7,7,7)`。
- 390×844：仪器跟着缩小，裁切构图不塌，`scrollWidth === clientWidth`（无横向溢出），
  文字换行正常，footer 不被挤没。
- 中英文各测了一遍桌面 + 移动端，`taglineTail` 英文版是空字符串时补回了 `&&` 短路判断
  （不加的话会渲染一个空 `<span>`，虽然视觉上无害但不干净）。
- 键盘：真实 Tab 键把焦点带到那个全页 `<button>`（`aria-label` 正确），`:focus-visible`
  匹配、焦点环 `outline: solid 2px #ededed` 实测生效；真实 Enter 键 200ms 内触发
  `router.push`，`location.pathname` 变成 `/zh/home/`。
- 鼠标：真实 `mousePressed`/`mouseReleased`（不是 `el.click()`）点在空白区域，同样进站。
- `?replay=1`：设置过 `sessionStorage.intro-seen=1` 之后带这个参数访问，`location.pathname`
  还是开场页、标题正常渲染，没有被两道拦截（inline script + useEffect）打回主站。
- `prefers-reduced-motion: reduce`：页面照常渲染，副盘的秒针指针按设计隐藏（只留个圆点），
  不逐帧走动画。
- `Runtime.consoleAPICalled`（error/warning）、`Log.entryAdded`、`Runtime.exceptionThrown`
  三类事件在一次完整导航 + 2 秒观察窗口里全部是 0 条——没有 hydration mismatch，没有运行时报错。
- `npm run build` / `npx tsc --noEmit` / `npm run lint` 全过（lint 剩的 2 条警告是接手前
  就有的，跟这轮无关，见更早的「遗留问题」小节）。

**遗留问题（发现但没修）**
- 主指针到副盘指针的映射（分针→主指针、时针→对角短线、秒针→副盘）是这轮的取舍决定，
  画板本身的静态示意图并没有标注哪根线该对应哪个时间单位，如果站主对这个映射有不同想法
  （比如想要副盘走分钟而不是秒），调整只需要改 `TimeDial.tsx` 里三个 `<g ref=.../>` 分别
  接的是 `minuteRef`/`hourRef`/`secondRef` 中的哪一个，逻辑不用动。
- 语言切换控件是这轮自己加回去的判断（画板新稿没画这个功能位），不是照搬画板逐像素还原的
  产物，站主如果觉得开场页不需要它（比如以后想做纯粹的静态画面，语言切换留到进站后的侧栏
  就够），删掉 `IntroScreen.tsx` 里那个 `motion.div`（右上角那块）即可，不影响其余逻辑。
- 未在真实移动设备（非 CDP 模拟）上验证触摸手势进站（`touchmove` 监听器逻辑没动，
  沿用旧版，但这轮没有物理设备可测）。

- 专注区重做为「书桌」（2026-08-31，按 `design-v2/Focus.dc.html` 定稿稿重做）：
  - **大表盘拿掉**：番茄钟不再是走时的圆盘，改成纸上一行 128px 细衬线大字（`Noto Serif SC`
    weight 200）+ 一道 420px 宽 2px 高的进度线（底 `#d3d3d3`/`--color-desk-line-2`，走过
    部分 `#141414`，末端 8px 圆点游标）。数字和进度线仍是逐帧用 ref 写 DOM，不走 React
    重渲染，倒计时逻辑（`usePomodoro.ts` 的绝对结束时刻 `endsAt`、localStorage 持久化、
    后台/刷新照走）**完全没动**，只是 `PomodoroDial.tsx` 换了一套皮。
  - **底色改中灰**：新增 `focus/DeskBackdrop.tsx`（线性渐变 `#414141→#2a2a2a` + 一点来光
    径向渐变 + `feTurbulence` 颗粒，复用 `intro/Grain` 组件传 `blend="overlay"`），替掉
    原来 `SceneBackdrop.tsx` 的四套暗色场景。**左侧图标条**跟着调成压在桌面上的半透明黑
    （`rgba(0,0,0,0.18)` 底 + `rgba(0,0,0,0.24)` 右边线，图标描边 `rgba(237,237,237,0.16)`）。
    ⚠️ 中途踩过一个坑：`DeskBackdrop` 最初挂在内容列（`grow` 那个 div）里，没铺到图标条
    底下，图标条的半透明黑压在 body 的 `--color-paper`（`#fafafa`）上，合成出一条错误的
    浅灰 `#cdcdcd`；CDP 实测揪出来的，改成挂在最外层 `h-dvh w-full` 根容器上就好了。
  - **三层切换挪到纸夹标签**：新增 `focus/FocusPaper.tsx`，纸的上沿三个文件夹标签（当前
    层和纸同色 `#eaeaea`、稍高一截；另外两层深一号 `#b9b9b9`、矮一截），点哪个翻到哪份，
    不再是原来页面最底部离操作区四百多像素那种够不着的位置。三层功能（番茄钟/手记/时刻表）
    一个没丢。
  - **纸上配色整体翻转**（暗底浅字 → 中灰桌面 + 浅纸深字）：`globals.css` 新增一组
    `--color-desk-*` token（`paper #eaeaea`、`ink #141414`、`muted #333333`、
    `dim #555555`、`faint #8a8a8a`、`mute #999999`、`placeholder #b8b8b8`、
    `line/line-2/line-3` 分别 `#dcdcdc/#d3d3d3/#cfcfcf`），镜像原来暗侧 `--color-shell-*`
    的五级灰阶命名，但深浅顺序相反。`Notes.tsx`、`Departures.tsx`、`PomodoroDial.tsx`
    里所有 `text-shell-*`/`border-white/*` 类名换成对应的 `text-desk-*`/`border-black/*`。
  - **时刻表的站台钟**保留原样（真实时间指针、外链清单、App 出口逻辑都没动），只把颜色
    从「浅色描边画在暗底」翻成「深色描边画在纸上」——`dial.tsx` 的 `makeTicks`/`Hub` 加了
    可选颜色参数（不传保持原来暗底钟面的默认值，只有 `Departures.tsx` 传浅纸用的深色）。
  - **番茄钟时长编辑换了个位置**：原来是「设置时长」展开一个面板放三个 +/- 输入框；
    画板给的提示语是「点数字可以自己改时长」，改成直接点预设 chip 上的分钟数字就地编辑
    （`PomodoroDial.tsx` 的 `PresetChip`），逻辑仍是 `usePomodoro.setConf` + `clampConf`。
    长休间隔和恢复默认这两项画板没画、也不常用，收进一个默认收起的「更多设置」链接，
    展开还是原来的 +/- 输入框，**功能没削减**。

  **⚠️ 一次功能移除，需要站主知情（不是这轮"只动视觉"范围内的默认选项，是主动判断）**：
  - 删掉了 `site.config.ts` 里的 `focus.scenes` 数组（雨夜/海浪/篝火/深空四套可切换的纯
    CSS 背景）以及配套的 `SceneBackdrop.tsx` 组件、messages 里的 `focus.scenes.*` /
    `focus.sceneLabel`。这四套背景原本是更早「放松区」阶段留下的氛围场景（声音已在
    2026-08-29 先撤了，画面留着当全局背景、底部一排小字切换）。
  - **为什么判断和桌面隐喻不相容**：`design-v2/Focus.dc.html` 给的桌面背景是一张具体的、
    唯一的渐变（`linear-gradient(152deg, #414141→#2a2a2a)`），画板上也没有任何「换背景」
    的控件位——桌面隐喻的前提是"这是你自己那张唯一的书桌"，如果背景还能在雨夜/海浪/
    篝火/深空之间切换，书桌隐喻就站不住了（一张桌子不会随时切换成一片海）。
  - **如果站主想保留"可切换"这个功能**，比较自然的折中是把"场景"概念换成"桌面材质"：
    比如提供几种木纹/石材/皮革一类的中性桌面渐变＋颗粒供选择，同样在底部留一排切换小字，
    换的是桌子的材质而不是整个场景氛围，这样能保留个性化功能，又不违背"这是同一张桌子"
    的设定。这需要重新设计至少 2-3 套材质配色，画板没有覆盖，得站主拍板要不要做、
    要哪几种材质。
  - **这轮没有擅自加回去**——按代理规则，功能性移除必须让站主知情，由他决定要不要恢复
    或做上述折中方案。

  **自查（生产静态产物 `out/`，`npx serve out`（**不带 `-s`**——带 `-s` 会把所有路由
  SPA-fallback 到根 `index.html`，之前踩过一次坑：`serve out -s` 导致 `/zh/focus/`
  实际渲染的是开场页而不是专注页，CDP 截图和交互检查全部检查错了页面）+ headless Chrome
  CDP，`--force-prefers-reduced-motion` 截静止态，手写 WebSocket 走 `Target.createTarget`
  + `flatten` session + `Input.dispatchMouseEvent` 真实点击）：
  - 1440×900：桌面渐变、图标条（修复后）、三个纸夹标签、128px 大字、进度线、控件簇
    逐项比对 `design-v2/Focus.dc.html`，颜色实测对上（纸 `rgb(234,234,234)`＝`#eaeaea`、
    大字颜色 `rgb(20,20,20)`＝`#141414`、图标条 `rgba(0,0,0,0.18)`）；`scrollWidth ===
    clientWidth`，无横向溢出。
  - 390×844：纸能放下、三个标签不挤成一团、`scrollWidth === clientWidth`（三层都测了）。
  - **真实点击三个标签**：番茄钟→手记→时刻表→番茄钟，`aria-pressed` 状态跟着切，手记显示
    空态引导（"还没有便签"+"写第一条"，没有便签所以没有 textarea，属预期）、时刻表显示
    真实站台钟 + 3 条外链（对应 `site.config.ts` 的 `departures`），功能均未损坏。
  - **番茄钟真实点播放**：`Input.dispatchMouseEvent` 点圆形播放键，2.5 秒后大字从
    `25:00` 变成 `24:58`，localStorage `lounge-pomodoro` 写入 `endsAt` 时间戳；**新开一个
    同源 target 模拟刷新**，大字继续走到 `24:56`（同一个 `endsAt`，没被重置），确认
    「后台/刷新照走」没坏。
  - 中英各测一遍：英文版标签 `Pomodoro/Notes/Departures`，预设 `Focus 25/Short break 5/
    Long break 15`，提示语 `Click a number to change its length`，翻译齐全无硬编码单语。
  - `npm run build` / `npx tsc --noEmit` / `npm run lint` 全过（lint 剩的 2 条警告跟这轮
    无关，是接手前就有的）。

  **遗留问题（发现但没修）**
  - 移动端（390px）稀疏地方，头一行「专注 · FOCUS」和右边「已完成 N」之间的空隙会露出
    稿纸横线背景，看起来像一道短横贯穿这一行。这是稿纸横线本身的纹理（`opacity: 0.5`
    铺满整张纸），不是渲染错误，但视觉上稍微有点抢——如果站主觉得不舒服，简单的修法是
    给头一行套一层不透明的 `bg-desk-paper` 垫底，把稿纸横线单独挡掉。
  - 本机这台 headless Chrome 的 CDP 有个环境级怪癖：对已存在的页面 target 直接发
    `Page.navigate` 会报 "Not attached to an active page"，得改成每次都用
    `Target.createTarget` 新建 target 并配合 `Target.activateTarget` 才稳定截图——
    这是这台机器 / 这个 Chrome 版本的特性，和站点代码无关，写在这里免得下次又踩一遍。

### 首页重做 + 内容页排版规律定稿（2026-08-31，按 design-v2/Home.dc.html + ContentTemplate.dc.html）

站主拍板了两张新画板，这轮做三件事：全站背景加一层纸张颗粒解决"正文背景太单调"、
首页四块重排出密度节奏、把列表行/图片卡这两种模式抽成共用组件套到全站内容页。

**① 背景三层（全站一次性生效）**

- `src/components/shell/SiteShell.tsx`：`<main>` 加 `relative`，挂一层
  `<Grain id="site-paper-grain" opacity={0.035} baseFrequency={0.8} numOctaves={4} blend="multiply" />`——
  这一处是全站 `(site)` 路由组的公共骨架，所有内容页自动获得纹理，不用每页各写一份。
  **没加 `overflow-hidden`**：博客详情页的 sticky 目录用 `left-[calc(100%+56px)]` 探出
  700px 内容列，main 一旦裁剪就会把它切没——用"grain 用 `inset-0` 精确铺满 + 内容列自身
  `position:relative` 排在后面"的 DOM 顺序换正确的层叠顺序，不靠裁剪。
- `src/components/intro/Grain.tsx`：`blend` 联合类型加了 `"multiply"`；filter 里补了
  `<feColorMatrix type="saturate" values="0" />`——feTurbulence 默认吐彩色噪点（RGB 各自随机），
  不去饱和的话浅灰底上会有极淡的彩色麻点，违反"全站只用黑白灰"。这条对所有已有调用点
  （开场页、专注区背景）也生效，是全面收紧、不是新引入的行为。
- `globals.css` 新增 `--color-line-soft: #f0f0f0`（白卡内部行间分隔线，比 `--color-line`
  更轻一档）、`@utility card-face`（白底 + 1px 灰边 + 极轻投影）、`@utility tag-framed`
  （带框小标签，0.2em 字距）。
- **颗粒层验证**（不是做了个看不见的东西）：对 1440×1700 的首页截图取一块 150×150 的
  "空白"渐变区域采样，通道值在 248–254 之间跳动（不是恒定值），扫描线相邻像素约三分之二
  的位置有 ±1～2 的抖动（噪点特征，不是平滑渐变的特征）；同时每个像素的 R/G/B 三通道
  差值恒为 0（验证去饱和生效，没有彩色麻点）。

**② 首页重排（`src/app/[locale]/(site)/home/page.tsx`）**

四块层层收紧，不再是四段长得一样的左对齐文字：
- **块 A 引言**：`首页 / HOME` 小标签（10.5px / 0.36em）+ 34px（移动端 26px）细衬线大字，
  文案是从 `content/home/intro.zh.md` 现有正文里摘的原句"我不觉得会用 AI 是什么本事，
  真正难的是判断它给的东西对不对。"，英文摘自 `intro.en.md` 对应段落，**没有新编文案**。
- **块 B 关于**：`grid-cols-[104px_1fr]`，正文改用新增的 `.prose-about`
  （15.5px / line-height 1.85 / 段间距 15px，比默认 `.prose-bw` 的 16px/1.9 更紧）。
- **块 C 在做的**：`grid-cols-[52px_1fr_auto]`，序号 01/02/03 用 26px 细衬线 `text-line-strong`
  （#c9c9c9），每行上沿 `border-line`，右侧状态小标签。**状态文字是新数据**——
  `Project` 类型加了可选的 `status`/`status_en` 字段，`content/projects/projects.json`
  三个项目分别填了"进行中/在用/在线"（英文 In progress/In use/Live）。标题右侧的月份
  注记复用了 `content/now/now.zh.md` 的 `updated` 字段（`monthLabel` 格式化），
  不是新开一个日期字段。
- **块 D 最近写的**：`grid-cols-[92px_1fr]`，日期 + 带下划线的标题，比原来的版本去掉了
  类型徽章，只保留最必要的两列，这一块要最紧凑。
- **一个内容结构上的取舍，站主需要知情**：原来首页单独的"现在是"板块（`content/now/*.md`
  的正文，讲知识库和 Claude-Anki 的近况）**从首页整个拿掉了**——画板的四块定义里本来就
  没有这一块，且它的文字内容和新的"在做的"高度重叠（同样在讲这两个项目的近况）。
  `content/now/` 的 md 文件和 `getNow()` 都没删，只是现在只取它的 `updated` 日期用在
  块 C 标题旁，正文不再渲染在任何页面上。如果站主觉得这块内容还有必要单独出现，
  可以再议放回哪里（比如 /about 页）。
- `messages/{zh,en}.json` 的 `home` 组：新增 `eyebrow`/`aboutLabel`；`buildingTitle`
  从"我在做的"改成"在做的"、`writingTitle` 从"我写的"改成"最近写的"（跟画板文案对齐）；
  删掉了不再用到的 `projectSeparator`/`stackSuffix`（原来拼接"标题 —— 描述 · 技术栈。"
  这句话的分隔符，新排版里标题和描述分成两行，不再需要）。

**③ 内容页排版规律：两个共用组件 + 套到六个页面**

新增 `src/components/ui/ListRow.tsx`（`ListRowGroup` 白卡容器 + `ListRow` 单行，
104px/1fr/auto 三栏，`#f0f0f0` 细线分隔，右侧 `tag-framed` 标签）和
`src/components/ui/MediaCard.tsx`（白卡 + 顶部图片区可配 `aspect-ratio` + 底部信息区，
`footer` 插槽放外链一类的次要行）。套用情况：

- **列表行**：`components/blog/BlogList.tsx`（同年份的条目揉进一张白卡，年份分隔行留在
  卡外面）、`components/library/LibraryShelf.tsx`（日期左列、类型标签右列）、
  `app/[locale]/(site)/projects/page.tsx`（年份左列，技术栈徽章和源码链接挪进 `footer`）、
  `app/[locale]/(site)/news/page.tsx`（世界新闻按日期分组，每组一张白卡；"其余更新"
  列表也套了白卡+细线，但没有强套 `ListRow` 组件本身——它是来源/标题/日期三列内联，
  跟 `ListRow` 的"标题+描述换行"语义不吻合，硬套反而别扭，只借了白卡外壳和分隔线颜色）。
- **图片卡**：`components/media/VideoCard.tsx`（播放按钮、懒加载 iframe 这些交互原样搬进
  `MediaCard` 的 `media` 插槽，没有改逻辑）、`app/[locale]/(site)/photos/page.tsx`
  （"专题"单栏大卡 + "档案"改成三栏图片卡网格，原来是纯文字清单，现在每辑露出封面）。
- **没套的，以及为什么**：唱片页（`/records`）——它的核心是一台真播放的黑胶唱机和
  一份可点击装到唱机上的曲目表，不是"一组图片"，硬套图片卡反而破坏交互，只让它跟着
  `SiteShell` 的全局颗粒层自动获得纹理。工具页（`/tools`）——`ToolCard` 已经是画板认可
  的定稿样式，且图标 hover 亮品牌色是 CLAUDE.md 明确的黑白例外，重新套模式风险大于收益，
  没有动。新闻页 AI 解读那几篇（`digest`）——是标题 + 多段正文的小型文章预览，语义上更接近
  博客摘要而不是列表行，维持原样。

**硬约束核对**：功能一律没动（BlogList 的筛选/年份分隔、LibraryShelf 的筛选、
Turntable/Chart 的播放、VideoCard 的懒加载、Photos 的灯箱、⌘K、giscus 全部原样）；
双语全部走 messages，没有新增硬编码单语文案；内容图片（视频封面、照片、专辑封面）
保持原色，包着它们的卡片边框/标签/按钮仍是黑白；`prefers-reduced-motion` 沿用全局兜底，
这轮没加新动效。

**顺手挖出并修掉一个跟这轮无关的潜在 bug**：给 `SiteShell` 的 `<main>` 加 `relative`
之后用 CDP 真实模拟 390px 手机做全站横向溢出扫描（`document.documentElement.scrollWidth`
对比 `clientWidth`），发现 `/blog/[slug]` 文章详情页在有长代码行时整页横向溢出到 748px——
根因是 `main` 自己是外层 flex 行（侧栏 + main）里的一个 flex 子项，其内容列又是 `main`
自身 flex 布局里的另一个子项，两层 flex 子项都用了默认的 `min-width: auto`，代码块一行
很长就会把两层都撑宽，连累 `<pre>` 自己的 `overflow-x: auto` 失效。**在这轮改的
`SiteShell.tsx` 里顺手补了 `min-w-0`**（main 和内容列两处都要加，少一层不够）。
这是个显示层的正确性修复，不是新功能，摄影/唱片/工具/新闻等页面本来就没有长代码行，
不会有任何视觉差异；用 CDP 在全部页面（含三篇文章详情页）复测过，`scrollWidth` 修复前后
分别是 748→390，其余页面修复前后都是 390，没有引入新问题。

**自查方法说明（这次踩了个截图工具的坑，记一下）**：一开始用
`chrome --headless --window-size=390,844 --screenshot=out.png url` 截手机图，
结果文字看起来像是在硬边界被截断而不是正常换行——一度以为是横向溢出的真 bug。
用 CDP 的 `Emulation.setDeviceMetricsOverride({mobile:true, width:390, ...})` 真实模拟
移动视口后复测，`scrollWidth === clientWidth === 390`，证明是**截图工具的假象**：
不带 `mobile:true` 的 `--window-size` 只是设置窗口尺寸，不会让页面按真实的移动视口
（含 meta viewport 的窄屏解析规则）布局，Chrome 会退回接近桌面宽度的虚拟视口再把截图
硬裁到 390px 宽，文字因此看起来像是被"切"了。**以后测手机响应式，必须用 CDP 的
`Emulation.setDeviceMetricsOverride` 显式传 `mobile: true`，不能只信任
`--headless --window-size` 命令行截图**——这条计入通用经验，不止这轮用得上。
（本机没有 puppeteer/playwright，也没装 `websockets` 这类 Python 库；这轮改用 Node 24
自带的全局 `WebSocket` 客户端手写了个几十行的 CDP 驱动脚本，够用，没有引入新依赖。）

**自查结果汇总**：`npm run build` / `npx tsc --noEmit` / `npm run lint` 全过（lint 剩的
2 条警告跟这轮无关，是接手前就有的）。1440×900 对着两张画板逐项比对了首页和四个内容页
（博客列表、摄影、书影音、项目），390×844（CDP 真实移动模拟）复核了首页、博客列表 +
三篇详情页、摄影列表 + 一个相册详情页、唱片、书影音、新闻、视频、项目、工具、关于、
联系、留言板、开场页，全部 `scrollWidth === clientWidth`，无横向溢出。中英文各截了一遍
首页和内容页，翻译齐全。

**发现但没修的问题（跟这轮改动无关，留给站主/下一轮判断）**：
- `content/home/intro.zh.md` 和 `intro.en.md` 结尾那句"最近在忙什么在 [现在](/now)"
  链接指向的 `/now` 路由**从来就不存在**（PLAN.md 从阶段 0 起就定了"Now 不单独成页"），
  这是这轮改动之前就有的死链，不是本轮引入的——本轮只是让它更显眼了一点（因为首页也不再
  展示对应内容）。要修的话最简单是把这两处链接文字改成指向 `/home`（首页本身）或者
  干脆去掉这个分句。
- `messages.library.empty`（"还没有条目。清单在 content/library/library.json 里。"）
  和它的英文版一样，是会被访客看到的开发者话术（暴露仓库内部文件路径），跟之前放松区
  修过的 `musicEmpty`/`podcastEmpty` 是同一类问题，这次没顺手改，因为它跟"背景/首页/
  内容页排版"这个任务范围没关系。
- `docs/如何更新内容.md` 没有为 `projects.json` 写字段说明（只有 `videos.json` 有），
  这轮给 `Project` 加了 `status`/`status_en` 两个新字段，站主自己加项目时如果照抄现有
  条目基本能猜到怎么填，但没有像 videos 那样有一段"照着抄"的示例，算是文档欠账。

### 开场页四项修改：宽屏构图 bug、时钟五层质感、极淡星点、语言切换挪位置 + 修 bug（2026-08-31）

站主上线后提的四条，用过 `design-taste-frontend` 判定为 redesign-preserve（保留现有构图/排版，
只做这四项）。

**1. 宽屏构图 bug（必修）**：`TimeDial.tsx` 原来是整页 `viewBox="0 0 1440 900"` +
`preserveAspectRatio="xMaxYMid slice"`，slice 的缩放比例取 `max(容器宽/1440, 容器高/900)`，
2000×1120 这类比 1440×900 更宽的屏算出来比例反而更大，仪器被放大又被上下左右一起裁。
**改成仪器彻底脱离页面 viewBox，自己是独立定位/独立尺寸的正方形**：
`width: clamp(520px, min(76vh, 58vw), 900px)`（只跟视口较小的一维走，构图恒定）+
`right: calc(-0.17 * var(--dial-size))` 水平锚右边缘探出视口外 + `top:50%/translateY(-50%)`
垂直居中；内部 SVG 换成正方形 `viewBox="0 0 672 672"`，圆心从旧的 `(1196,392)` 平移到新的
`(336,336)`——因为原代码所有几何早就是相对 `CX/CY` 变量写的（只有十字准线和副盘中心两处是
硬编码字面量，这次顺手改成了 `CX±偏移` 的相对写法），平移不用重算任何半径/角度。
手机竖屏单独给一档尺寸（`max-sm:[--dial-size:clamp(300px,68vw,460px)]`）——按公式算过，
390 宽屏下 `min(76vh,58vw)=226px` 会被桌面那档的下限 520px 兜住，反而变成比屏幕还宽的巨物，
不是"偏小"是"爆屏"，所以窄屏必须单独给一档，不能指望同一个 clamp。
用 CDP（非 `--virtual-time-budget`，会挂 rAF）截了 1440×900 / 1920×1080 / 2000×1120（站主
那块屏）四种比例，仪器在画面里的占比、被右边缘裁掉的比例肉眼一致，bug 已修。

**2. 时钟五层质感**：在原来的线框（同心圆+刻度）上叠五层，全程黑白灰、不做外发光：
① **表圈**——`BEZEL_OUTER=326`/`BEZEL_INNER=304` 之间一圈 `stroke` 粗环，填
`linearGradient`（方向 `16%,78%→80%,24%`，对应背景主光 `78% 30%` 那个轴），背光侧
`rgba(237,237,237,0.03)` 到受光侧 `rgba(237,237,237,0.22)`，数值原样照抄任务书；
② **盘面下沉**——新增 `radialGradient#dial-sink`（中心透明→边缘 `rgba(0,0,0,0.45)`）叠在
原有左上高光渐变之上；③ **指针改锥形实体**——原来的 `<line>` 全部换成 `<polygon>`（根宽
2.6px、尖端 0.6px），分针另加尾部配重（小圆+梯形）和一条沿一侧的 0.5px 高光线；
④ **表镜反光**——一道 `opacity:0.05` 的斜向（`rotate(-30deg)`）光带，`clipPath` 裁在盘内；
⑤ **数字浅浮雕**——原有蚀刻数字下面先画一份 `x+0.4,y+0.9`、`fill="#000000"`、
`fillOpacity` 减半的暗色副本，再盖原来那层。用 CDP 截图放大裁切核对过，五层都清晰可辨、
没有一处是"发光/霓虹"（没用 `drop-shadow` 之外的任何 glow 手法，`drop-shadow` 那两个常量
是旧版就有的，没加新的）。

**3. 极淡星点（站主选的方向：天文感）**：`Starfield.tsx` 整个重写。三条防"壁纸感"的约束：
- 数量/亮度分层：44 颗里 34 颗 opacity 0.05–0.12、8 颗 0.15–0.28、2 颗固定 0.42（"亮星"）。
- **和仪器有物理关系**：候选点按到 `TimeDial.tsx` 导出的 `DIAL_ANCHOR`（仪器圆心的近似
  百分比坐标，`{xPct:82,yPct:50}`）的距离做拒绝采样——`keepProb = clamp(dist/42, 0.05, 1)`，
  越近仪器越容易被丢弃；保留下来的点再按距离从远到近排序分层，**亮星必然是离仪器最远的
  那两颗**。坐标用固定种子（20260831）的线性同余生成器算，服务端/客户端一致，不会
  hydration mismatch。用 `filter:brightness(6)` 截图验证过：星点确实是离仪器越近越稀疏，
  不是随手撒的均匀壁纸。
- 动效：只有 2 颗亮星 + 4 颗中等星呼吸（周期 6–11s、随机相位错开，振幅 ≤ opacity+0.06，
  新增全局 keyframe `dcStarBreathe`，靠每颗星自己的 `--o0/--o1` 变量驱动），其余 38 颗完全
  静止；`prefers-reduced-motion` 沿用 globals.css 里已有的全局兜底（所有动画 duration 钉到
  0.01ms），没有另写判断。
  用 PIL 在渲染截图上实测取色：亮星灰度 88、中等 53–55、暗淡 28–30（背景基线 13），
  确认真的渲染出来了，且足够淡。

**4. 语言切换挪位置 + 修"点它会连带进站"的 bug（必修）**：
- **位置**：右上角挪到左下，`left-5/sm:left-9/lg:left-[92px]`（和页脚同一条基线），
  纵向按"页脚上边缘和大字文案下边缘之间居中"手算了三档（`bottom-12/16/[92px]`），
  截图确认不挤footer也不撞大字块。
- **z 层结构**：进站的整页 `<button>` 显式钉 `z-0`，语言切换的 `<motion.div>` 是它的
  **兄弟节点**（本来就不是嵌套关系）、`z-20`；顺手把 `<motion.main>` 上冗余的
  `onClick={enter}` 整个删掉——原来点哪都进站是靠 main 的 onClick 和这个全屏 button 双重
  实现的，删掉 main 那份之后进站的入口只剩这一个 `<button>`，不再需要靠"事件冒泡谁先谁后"
  去赌语言按钮点不点得中。语言链接的 `onClick` 保留 `stopPropagation` 当兜底。
  用 CDP 真实点击测过：点语言按钮 `finalUrl` 停在 `/en/`（没有变成 `/en/home/`）；点背景
  空白处 `finalUrl` 变成 `/zh/home/`；Tab 一次聚焦到 `aria-label="进入网站"`（键盘可达性没退化），
  再按 Enter 也进站。
- **不重放入场动画**：新增 `sessionStorage` 短时效标记 `intro-locale-switch`——语言链接
  onClick 里先 `setItem`，新页面 `IntroScreen` 用 `useState` 惰性初始化只读一次，读到就让
  `rise()` 返回 `initial:false`（Framer Motion 直接从 animate 目标值起render，不经过渡），
  一个 `useEffect` 读完立刻 `removeItem`。用 CDP 分别测了两条路径：从语言切换跳过去的页面，
  点击后只等 150ms 截图，文字已经是**完全体**；直接访问 `/en/` 的对照组，同样等 150ms 截图，
  文字还在渐显中途——证明"跳过入场"确实只在切语言这条路径生效，没有误伤正常首访。
  焦点环没有写 `outline-none`（CLAUDE.md 踩过的坑），语言链接补了
  `focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2`。

**改动文件**：`src/components/intro/TimeDial.tsx`（构图重做 + 五层质感，整份重写）、
`src/components/intro/Starfield.tsx`（星点算法整份重写，从未使用状态变成被引用）、
`src/components/intro/IntroScreen.tsx`（引入 Starfield、语言切换位置/z 层/skip-entrance 逻辑、
删 main 冗余 onClick）、`src/app/globals.css`（新增 `dcStarBreathe` keyframe）。
没有改任何 messages 字典（没引入新文案）、没有改 `page.tsx`（`intro-seen` 那套重定向逻辑
跟这轮的 `intro-locale-switch` 是两套独立的短时效标记，互不干扰）。

**自查**：`npm run build` / `npx tsc --noEmit` / `npm run lint` 全过（lint 剩的 2 条警告跟
这轮无关，是接手前就有的）。用 Node 24 原生 `WebSocket` 手写的 CDP 截图脚本（本机没装
puppeteer/playwright，沿用上一轮"不能只信任 `--headless --window-size` 命令行截图"的经验，
但这次连命令行 `--screenshot` 本身都不够用——它不等入场动画播完就拍，所以额外写了
"CDP 打开页面→按需 sleep→`Page.captureScreenshot`"这条路，能控制在动画播完之后再拍，
也能在拍之前先派发真实的点击/键盘事件）截了 1440×900、1920×1080、2000×1120、390×844
四种必查比例，另外顺手抽查了 640×900、768×1024、820×1180 三个中间尺寸确认没有断档。

**发现但没修的问题**：任务书原文把现有时钟描述成"纯线框（同心圆+刻度）"，但实际代码在
上一轮"六处调整"里已经加了 120 道细密外圈刻度、60 道内圈刻度、蚀刻数字、右下副盘——
比任务书设想的更复杂。这轮五层质感是叠在**实际代码**（而不是任务书描述的简化版）上做的，
没有借机简化或删掉那些既有细节。

### 开场页：语言切换挪到左上角 + 时钟改为「悬浮玻璃」（2026-08-31）

**语言切换**：从左下角挪到**左上角**，和小标签「个人网站 · PERSONAL SITE」同一行、
共用 `left-[92px]` 这条左基线（页脚、文案块也是这条线）。小标签是 `pointer-events-none`，
不会挡住切换按钮；切换按钮 z-20，明确高于铺满整页的进站按钮 z-0。

**时钟从「金属仪表盘」改成「悬浮玻璃」**（用户选的方向）。两个原始问题：
① **外发光让它浮在页面之上** —— 发光的物理含义是「它自己在发亮」；
② 圆心推出视口 17%，像挂在右边缘上。

改法：
1. **投影取代外发光**（`SHADOW_REST`）。投影 = 有厚度、压在平面上。
   光源是背景主光那片 radial（78% 30%，右上），所以投影一律朝左下。
   表盘那一带背景约 `#1c1c1c` 不是纯黑，投影读得出来 —— 换背景色时要重新确认这一点。
2. **折射亮边**（`rim-refract` / `rim-dark` / `rim-spill`）：沿光轴从背光侧暗棱渐变到受光侧亮边，
   最外沿再压一条细亮线。**一圈亮边是「玻璃」最主要的信号**，比阴影管用。
3. 盘面边缘内暗 0.45 → **0.2**。⚠️ 压太狠会立刻变回金属，别调回去。
4. **高光弧**（`SPEC_WIDE`/`SPEC_HOT`/`SPEC_BACK`）经 `feGaussianBlur` 糊开，画在**最上层** ——
   指针在镜片下面、反光在镜片上面。背光侧也给一小段回光，只有一侧会显得是贴片。
5. **指针投影走 filter（feDropShadow）**，不是画偏移副本。
   ⚠️ filter 在屏幕空间生效，指针转到任何角度投影方向都不变；
   副本会跟着指针一起转，某些角度必穿帮。**别改成副本写法。**
6. 裁切 `right: -0.17 × 尺寸` → **-0.06**，整个棱基本进画面、只被右边缘轻切一下。
   ⚠️ 同时把 `DIAL_ANCHOR.xPct` 从 82 改到 **74** —— Starfield 按它算「离仪器多远」的疏密，
   改了裁切不改这个就会错位。

⚠️ **这个仓库今天有两个会话在改**（上一次提交 ce14bf7 是另一个窗口做的开场页四项修改）。
CLAUDE.md 里写了「不要并行开两个终端改同一个项目」，后续注意串行。

### 首页重做：开场页下线，换成「躺在草坪上」那一屏（2026-09-01）

用户要求：推翻开场页的设计，换成一个男孩躺在草坪上手枕脑后的画面；
去掉「点一下进站」的跳转；首页不必拘在黑白灰。

**用户在提问里定的三件事**：① 画面用**代码画剪影**（我无法生成图片，另两个选项是他给照片、
去免费图库找）；② 光**跟真实时间变**（早/中/晚/夜四档）；③ 配色要**鲜亮的草绿蓝天**，不压饱和。

**开场页整套下线**：删了 `[locale]/page.tsx`、`IntroScreen.tsx`、`TimeDial.tsx`、
`Starfield.tsx`（星点层只服务于那只时钟）。`Grain.tsx` 是全站共用的
（SiteShell + 书桌背景），从 `components/intro/` 挪到 `components/ui/`，
`components/intro/` 目录已清空 —— 别再往那儿加东西。

**路由搬家**：`/{locale}/home/` → `/{locale}/`。
- `NAV_HOME.path` 从 `"/home"` 改成 `""`（`localePath` 里 `""` 拼出来正好是 `/zh/`）
- ⚠️ 侧栏两处和 FocusRail 一处是**硬编码** `localePath(locale, "/home")` 的，
  只改 NAV_HOME 不够，构建产物里会留下死链。已改成引用 `NAV_HOME.path`。
  以后再动首页路径，记得 `grep -rn '"/home"' src/`。
- sitemap 原来单列了一条 `"/"`，而 NAV_ITEMS 第一项现在也是首页，会出两条同样的 URL，已去重。
- `.next` 里的路由类型缓存会记住已删的页面，删页面之后必须 `rm -rf .next` 再构建，
  否则报 `TS2307: Cannot find module '.../home/page.js'`。

**MeadowHero（`components/home/MeadowHero.tsx`）几个不能改坏的点**：
1. ⚠️ **SVG 的 presentation attribute 不解析 `var()`** —— `fill="var(--x)"` 在任何浏览器里都是死的。
   所有跟时段有关的颜色都写在 globals.css 的 `.meadow .mh-*` 类里，JSX 只挂类名。
2. ⚠️ **`cx`/`cy`/`r` 这些几何属性同样吃不进 `var()`**。日/月因此画在原点，
   靠 CSS `transform: translate(...) scale(...)` 挪过去，并且必须显式写
   `transform-box: view-box; transform-origin: 0 0` —— 否则 scale 会绕 viewBox 中心缩放，直接飞出画面。
3. 时段色由 effect 在挂载后 `setProperty` 写到根节点，**不经过 React state**：
   服务端不知道访客几点，首帧一律 day，既不会 hydration mismatch 也不违反
   React 19「不要在 effect 里 setState」。每 5 分钟对一次，跨过日落会自己变。
4. 草叶/云/星点的位置全部由**定种子**的 mulberry32 生成。
   ⚠️ **不要换成 `Math.random()`**，那样服务端和客户端的 DOM 对不上。
5. 人物剪影用「粗描边 + 圆线帽」画四肢 —— 圆线帽天然就是关节，不用描闭合轮廓，不会画崩。
   整组 `translate(0 46)`：FIG 那套坐标算出来身体在 545~660，而地平线在 622，
   不下移的话大半个身子在天空里，像躺在山脊上。**调姿势改 FIG，调高度改这个 translate。**

**遗留**：
- 这一屏靠负边距（`-mx-6 sm:-mx-10 -mt-[88px] lg:-mt-[104px]`）顶掉 main 的内边距，
  但内容列本身只有 700px，所以**不是整块 main 的全出血**。真要全出血得改 SiteShell 的结构。
- `intro.*` 那组文案（eyebrow / tagline / taglineTail / taglineSub）**保留**，
  现在是这一屏的标题文字，命名空间没跟着改名。
- CLAUDE.md 和 PLAN.md 里还写着「五个画板（含 Intro）」，Intro 那块已经不对应任何页面了。

### 首页那一屏改成单色海报（2026-09-01，同日第二轮）

上一轮做的「躺在草坪上」剪影，用户看过实际效果后要求换掉，改成**按 mono-color skill
（github.com/yanliudesign/mono-color-skill）的设计系统，把他自己那张「背影走在空旷马路上」
的照片做成编辑式海报**。

⚠️ **那个 skill 的默认交付是「生成提示词 + 生成一张位图」，而我没有图像生成能力。**
按 skill 自己的规则，这种情况停在提示词；但站上这一屏必须真的变，所以改成
**按它的 design-system 手写 SVG** —— 配方照它的目录走，只是渲染器换成浏览器。
顺带保住了这个站「一张位图都不加载」的底线。以后要位图版，用提交信息里那段提示词去生成。

**配方（SKILL.md 要求的 manifest，取自 design-system/ 目录，目录优先于散文）**：
- substrate `#FAFAF7` Neutral White —— **和内容区的 `#fafafa` 几乎同色**，
  所以海报像印在页面上，不像贴上去的一块图。这也顺手解决了上一版彩色 hero 和
  黑白正文之间的跳变（上一版得靠底部渐隐硬接）。
- palette `Ultramarine + Safety Orange` `#263E99` + `#E55D2B` ——
  目录里这一对写明用于 movement / youth culture / **active urban subjects**。
- plate roles：群青 = 结构与节奏（路面、影子、人、楼、树）；
  **橙只画中线这一个识别锚点** —— skill 明令 accent 不许当装饰均匀撒开。
- identity anchors（缩略图尺寸下至少两个还能认出来）：①背影+书包 ②被拉长的影子
  ③公路与黄色中线 ④远处的楼与细树。
- focal event：**异常尺度关系** —— 影子放大到横贯整页、在左边缘裁断，远比人本身大。
- layout `image field`：标题**穿过**影子，不做「标题一边、照片一边」的安全分栏。

**两个实现要点**：
1. **反白（纸把图咬开）**：印刷里反白字是同一版上的镂空。做法是标题画两遍 ——
   先群青画一遍，再画墨块盖住，最后同一行用**纸色**画一遍并 `clip-path` 到墨块里，
   落在墨上的那半截就自动反白。**别改成加白色描边**，那是描边不是镂空。
2. **套准偏移**：橙版整体 `translate(1.5 -1)` 且 `mix-blend-mode: multiply` ——
   双色印刷的套准误差 + 叠印变深，是 skill 允许的两处可控瑕疵之一（另一处是路面网点）。
   ⚠️ 这个偏移是**故意的**，不是 bug，别去对齐。

`MeadowHero.tsx` 和 globals.css 里那段 `.meadow` 已删。

### 首页海报改用真照片（2026-09-01，同日第三轮）

上一轮我把那张背影照**手画成了矢量**，用户要求「按整个仓库的流程来，按原图来改，不是 SVG」。
改对了：skill 的 faithful reproduction 本来就允许「裁切、放大、简化、**转网点**」，
而转双色调只需要图像处理，不需要我没有的生成能力。

**新增管线 `npm run hero`（scripts/build-hero.mjs）**，完全照 build-photos.mjs 的约定：
`hero-src/road.png`（原图，已加进 .gitignore）→ 双色调 → `public/images/hero/road.webp`（进仓库）。
sharp 借用 next 的传递依赖，**没有为它新增 package.json 依赖**。

**脚本里三个不能改坏的决定**：
1. **副版是选出来的，不是铺上去的**。`isAccent()` 按「红绿高、蓝明显低」在原图里挑出
   路面黄线那些像素单独上橙墨，实测占 0.77% 像素。
   ⚠️ skill 明令 accent 不许当装饰均匀撒开 —— 别改成整体叠色或色相旋转。
2. **副版要单独限最低密度**（`Math.min(t, 0.42)`）。黄线在原图里本来就亮，
   照亮度映射会被洗成纸色，等于没有副版。这一条是实测出来的，别删。
3. **高光硬剪到纸色**（`clipHi = 0.8`）。于是照片顶部的天空直接融进页面，
   看不到图片的矩形上边 ——「纸把图咬开」是影调本身带来的，不用额外画遮罩。

**版式**：照片占约 70% 并在右边缘出血；标题从纸上横穿进照片，落进照片的那半截**反白**。
反白是真镂空：标题画两遍，先群青一遍、画照片盖住、再用纸色画一遍 clip 到照片矩形里。
⚠️ 别改成给文字加白描边，那是描边不是镂空。

⚠️ **React 19 又撞了一条新规矩**：`react-hooks/static-components` 不允许在渲染函数里
创建组件。标题那个复用组件因此必须放模块级。（这个项目已经踩过 set-state-in-effect、
refs-during-render，加上这条是第三条。）

**遗留**：用户给的是**带黑边的手机截图**，脚本自动去掉黑边后真正的照片只有 **900×546**，
成品放大到 1400 宽会偏软（双色调网点能盖掉一部分，盖不全）。
**拿到相机原图替换 `hero-src/road.png` 重跑一次就会明显更实。**

### 项目页加「用到的仓库」；mono-color 装成项目 skill（2026-09-01，同日第四轮）

**mono-color skill 落地**：之前做首页海报时只把上游的 SKILL.md 和 design-system
下载到 `/tmp` 读了一遍，**规则本身没进仓库** —— 下一个会话不知道这套东西。
现在装在 `.claude/skills/mono-color/`（frontmatter 的 name 就是 `mono-color`，可直接调用）。
上游 MIT，所以 SKILL.md 和六个 design-system 目录原样收录；
⚠️ **`examples/` 里的示例图没有拷、也不要拷** —— 那些不在 MIT 范围内，
上游 ASSET-LICENSE 写明未经书面许可不得复制或再分发。
⚠️ **不要手改 `.claude/skills/mono-color/SKILL.md` 和 design-system/**，
那是上游的，本地改动会在下次同步时丢掉；项目自己的取舍写在同目录 NOTICE.md。

**项目页拆成两块**（用户要求：自己的项目留原处，另起一块放用到的 GitHub 仓库）：
- 上「我做的」← `content/projects/projects.json`（站主自己的作品，左列是年份）
- 下「用到的仓库」← `content/projects/repos.json`（12 个开源仓库，左列是它在这个站里干什么）
⚠️ **两块必须分开**：混在一张清单里会让人以为这些开源项目都是他写的。
类型也分开（`Project` vs `UsedRepo`），不要为了省事合并。

repos.json 里那 12 条是**从实际依赖里取的**（`node_modules/*/package.json` 的
repository 字段），不是凭印象写的；另外三条不在 package.json 里但确实用了：
giscus（评论）、simple-icons（工具页图标来源）、mono-color-skill（首页海报的版式）。
以后加删依赖记得同步这个文件 —— 它不会自己更新。

### 项目页改成两个筛选（2026-09-01，同日第五轮）

用户在导语那一行画了两个红框，要的是**在那个位置放两个筛选**：一个放自己的项目，
一个放用到的 GitHub 开源项目。上一轮做成了上下两个小节，这一轮改成筛选。

- 抽出 `components/ui/SegmentedTabs.tsx`（通用），项目页和新闻页共用。
- ⚠️ **新闻页一行没动**：`NewsTabs` 改成了 SegmentedTabs 的一层薄封装。
  一开始想直接改新闻页的调用处，那两块内容嵌套很深，用脚本替换把 JSX 改坏了
  （TS17014 / TS2657），已回退。**以后要动新闻页那段 JSX，手动改，别用字符串替换。**
- 原来那句固定导语（"做过的东西，一个个列在这里…"）拆成了两条，各挂在自己那个筛选下面。
- `repos.json` 加了 `mine` 字段：站主自己找来的仓库排最前、带「自己找的」标记，
  和「构建依赖」区分开。目前只有 mono-color-skill 一个。

**未选中的那一块只挂 `hidden`，仍在 DOM 里** —— ⌘K 搜索、浏览器页内查找和爬虫都拿得到，
不要改成条件渲染。

**待办**：用户说「主要放我发给你的那几个 GitHub 仓库」，但这轮对话里他只发过
mono-color-skill 一个地址。其余仓库等他补，补的时候记得把 `mine: true` 一起加上。

---

## 导航搬到顶部 + 板块合并（2026-09-08）

站主给了三张图：一张在侧边栏上画了红箭头指向页面顶部（"功能选项跟正常网站一样放到上边，
下面的页面是完整的整张的"），一张圈住侧栏底部的社交链接（"那几个跳转也要放到上边顶部靠右"），
一张是参考排版（暗色顶栏：左边导航平铺，右边 ZH/EN + 社交图标）。同一轮里还要求合并板块、
删留言板、首页不放照片。**这一轮改的是结构，内容文件一个都没动。**

### 做了什么

**① 侧栏 → 顶栏**（`components/shell/Sidebar.tsx` 删，`TopNav.tsx` 新）
- 64px 高的暗色横条 fixed 在最上面：左边 Logo + 名字 + 八项平铺导航（当前项下面一道细线，
  横排里反色底块太重），右边 中/EN · 分隔线 · 六个社交图标 · ⌘K。
- 颜色仍是原来那套 `shell-*` 暗侧灰阶 —— 那块暗色只是从一竖条变成一横条，全站黑白关系没变。
- 断点：`<lg` 汉堡 + 全屏抽屉；`lg` 导航平铺、社交先收起（八项 + 名字已经占满）；`xl` 社交一起出来。
- ⚠️ 抽屉是**点一下就收**（`onClick` 挂在抽屉的 `<nav>` 上），不要改成在 effect 里盯 `pathname` ——
  eslint 的 `react-hooks/set-state-in-effect` 会报错，而且软跳转时点击本来就发生在跳转之前。
- 侧栏时代的四组分组（作品/写作/嗜好/关于）和 `icons/NavIcon.tsx` 一起删了：顶栏是平铺的一行。

**② 内容区改成整幅宽度**（`SiteShell.tsx`、`globals.css`）
- 内容列不再有 `max-width`，只留左右 20/40px 边距。`--spacing-sidebar` 和 `--spacing-rail` 删掉，
  换成 `--spacing-topnav`。
- **唯一还限宽的是文章正文**：`.prose-bw` 自己带 `max-width: var(--spacing-column)`（700px），
  文章详情页外层也加了 `max-w-column`（侧边目录的 `left-[calc(100%+56px)]` 是相对这一列算的，
  容器一铺满目录就飞到屏幕外了）。一行文字横穿两千像素没人读得下去。
- 顺手把工具页（2→3→4 列）、视频页（1→2 列）、摄影专题（1→2 列）、档案（3→4 列）
  在宽屏上铺开；联系页和 PageHeader 的引导语给了个软上限，不然拉成一长条。

**③ 博客 = 文章 + 新闻**（`/news` 删）
- `/blog` 页内三个筛选平铺成一排：文章 / 世界新闻 / AI 更新。新闻原来是独立页、页内自己
  还有两个筛选；这里**没有套成两层**，两层筛选读起来是两个决定，一层只是一个。
- 新闻那两块拆进 `components/news/NewsSections.tsx`（`WorldNews` / `AiNews`，都是服务端组件），
  `NewsTabs.tsx` 删。上一轮那条"别用字符串替换改新闻页 JSX"的教训这次是整块搬走，没有原地改。
- ⚠️ **三块必须分开**：自己写的文章和抓来的新闻标题混在一张清单里，会让人以为新闻也是他写的。

**④ 爱好 = 摄影 + 唱片 + 书影音**（`/photos` 索引、`/records`、`/library` 删）
- `/hobbies` 页内三个筛选。三块分别抽成 `PhotosSection` / `RecordsSection` /（直接用现成的
  `LibraryShelf`），都在服务端渲染好再当 props 交给 `SegmentedTabs`。
- **单辑详情页 `/photos/<slug>/` 没动**，只是没有 `/photos/` 这个索引页了；页内的"回到摄影"
  指向 `/hobbies#photos`。

**⑤ 首页那张海报搬到摄影**
- `PosterHero` 从 `components/home/` 挪到 `components/photos/`，现在是爱好页摄影栏的开场大图
  （负边距出血到窗口两侧）。首页顶上换成一块**报头**：Logo + 名字 + 一句话定位，纯排版没有图。
- 换照片的办法没变：替换 `hero-src/road.png` 重跑 `npm run hero`。

**⑥ 专注页下线，番茄钟和手记进项目页的「小工具」**（`/focus`、`/lounge` 删）
- 站主原话："将专注这个里的番茄钟和笔记功能带入到项目里面，里面命名为小工具。时刻表不需要了。"
- `components/focus/` → `components/gadgets/`，新增 `Gadgets.tsx`（客户端，持有 `usePomodoro`
  状态，两栏之间切）。项目页因此有三个筛选：我做的 / 小工具 / 用到的开源。
- **`PomodoroDial` 和 `Notes` 一行没改**：它们用的 `desk-*` 是**浅色**灰阶（desk-paper #eaeaea、
  desk-ink #141414），本来就画在浅色纸上，直接摆进内容区的白卡里就成立。
- 跟着退场的：`Departures`（时刻表，站主说不要了）、`DeskBackdrop`、`FocusPaper`、`FocusRail`、
  `FocusStage`、`dial.tsx`，以及 `site.config.ts` 的 `focus.departures`。
  `site.config.focus` 改名成 `site.config.gadgets`（只剩 `pomodoro`）。**时刻表别再加回来。**
- 两栏都留在 DOM 里只挂 `hidden`：切到手记时番茄钟不会被卸载，计时才不断。

**⑦ 留言板下线**（`/guestbook` 删）
- 文章底部的 giscus 评论**保留**，`giscus` 配置和命名空间都没动。
- 联系页页脚那句"不想发邮件？去留言板写一句"改成指向博客。

### 几个要记住的点

- **`SegmentedTabs` 认 hash 了**：地址带 `#<tab key>` 直接落到那一栏。合并掉的五个旧板块
  （摄影/唱片/书影音/新闻/小工具）在 `lib/nav.ts` 的 `NAV_ALIASES` 里，只进 ⌘K、**不进 sitemap**
  （它们是带 hash 的同一个页面，进了会变成重复 URL）。`localePath()` 会把尾斜杠补在 hash 前面。
- **旧地址没有留跳转页**：静态导出发不了 301。`/lounge` 那张跳转页也一起删了
  （它指向的 `/focus` 本身已经不存在）。站上线才十来天，外链风险低；真需要的话
  照 `lounge/page.tsx` 原来那个 meta-refresh 的样子补回来就行（见 git 历史）。
- `messages/{zh,en}.json`：`focus` 命名空间改名 `gadgets`（去掉 departures/moved/整屏那几条），
  新增 `hobbies`，`guestbook` 删除，`nav.blog` 从"文章"改成"博客"。两份键结构已脚本校对一致。

### 遗留 / 下一轮注意

- **`docs/design/` 那五张画板里的左侧栏已经作废**（Main/BlogContact/Lounge/Tools 都画的是侧栏版）。
  灰阶、字号、间距这些 token 仍然有效，**但布局别再照着画板还原了**，以 PLAN.md §4 改版后那段为准。
- 首页那块报头是新写的，没有画板依据。站主要是不满意，那儿是最容易调的一块。
- `components/records/RecordShelf.tsx` 和 `getRecords()` 仍然留着没删（2026-08-31 起就不渲染了），
  这轮也没动。

### 同日第二轮：导航居中、版心收回、爱好并进关于（2026-09-08）

站主看过上一轮的成品后提了四件事，**其中两件是推翻上一轮的决定**：

1. 顶栏导航"再稍微靠中间一些"（截图上把导航那一组框了出来）→ 做成**绝对居中**
2. "内容区域看着有些分散太靠左边了" → 上一轮那个"完全铺满整幅"**收回**，改成 **1240px 居中版心**
3. "将爱好结合到关于的板块里面" → `/hobbies` 撤销，三个爱好**变回独立页**，入口挪到关于页
4. "设计一个交互动态，爱好相互连接的互动的感觉效果，可以点击跳转" → 同心轨道图

#### 做了什么

**顶栏导航绝对居中**（`TopNav.tsx`）
容器从 `flex gap-8` 改成 `relative`，导航那一段是 `absolute left-1/2 -translate-x-1/2`。
⚠️ **别改回 flex 里的一项**：中英文名字长度不一样、社交图标在 xl 才出现，
flex 布局下这三种情况导航会落在三个不同的位置，不是"正中"。七项约 380px，
lg（1024px）下两侧留白也够，不会压到 Logo 或右边那组。

**1240px 居中版心**（`SiteShell.tsx`、`--spacing-page`）
上一轮那个"完全铺满"是站主自己选的，看到实物之后推翻了。三层嵌套现在是：
视口 → 1240px 版心（`max-w-page`）→ 700px 阅读列（`.prose-bw`，只给 markdown 正文）。

**爱好拆回三个独立页**（`/hobbies` 删）
`/photos`、`/records`、`/library` 都回来了，但页面本身只是薄薄一层壳 ——
内容仍在 `PhotosSection` / `RecordsSection` / `LibraryShelf` 里。
**这就是上午把内容抽成 Section 组件的价值**：一天之内合并又拆开，两次都只是换外壳。
以后再有这种反复，照这个办：内容进组件，page.tsx 只管路由和 metadata。

**关于页的同心轨道图**（`components/about/HobbyOrbit.tsx` + globals.css 末尾一大段）
- 圆心是站主的 Logo（不动），三个爱好在同一条轨道上 60 秒转一圈，各有一根辐条连回圆心。
  想说的话是「三样东西从同一个人身上长出来」—— 这也是选轨道而不是散点连线的理由。
  圆形语言跟唱片页那张黑胶、番茄钟原来那只表盘是同一套，不是另起一个视觉。
- 鼠标进舞台（或键盘 focus 到某个节点）→ 停住，停在哪儿是哪儿；停在某个节点上 →
  它反白、它那根辐条变深、另外两根淡到 0.34；点一下进那一页。
- **服务端组件，零客户端 JS**：数字（3 辑 / 20 首 / 4 条）构建时数好写进 HTML，
  转和 hover 全是 CSS。三个节点是真 `<Link>`（右键新标签、Tab + 回车、爬虫都成立），
  **别改成 onClick 跳转**。下面还有一份普通的文字链接清单兜底。
- ⚠️ **两处反向旋转别混**：`.orbit-node` 上的 `rotate(calc(-1 * var(--a)))` 抵消的是
  静止时辐条的角度；`.orbit-face` 上的 `orbitCounter` 动画抵消的是转盘正在转的那份。
  少一个字就是歪的。
- ⚠️ `.orbit-arm` 必须是 **0×0**：它的 transform-origin 默认在自己正中，0×0 的正中
  就是圆心本身，`rotate` 才会绕圆心转。给它宽高就绕错地方了。
- 尺寸有个隐含约束：辐条**露出来**的那一截 = `--orbit-r` − 半个圆心 − 半个节点。
  第一版手机上只剩 31px，"连着"的感觉没了，收小节点之后现在是手机 46px / 桌面 81px。
  改这三个尺寸前先算这个差。
- 「减少动态效果」下：globals.css 第 236 行那条全局兜底把两条动画掐成 0.01ms 跑一次，
  转盘停在 0°、字是正的，三个节点静止地摆在轨道上，功能一点不少。

#### 顺带

- `nav.ts`：`NAV_TOP` 七项（爱好去掉），新增 `NAV_EXTRA`（三个爱好页，进 ⌘K 和 sitemap，
  不进顶栏）。`NAV_ALIASES` 只剩「新闻 → /blog#world」「小工具 → /projects#gadgets」两条。
- messages：`hobbies` 命名空间删除，`about.orbit.*` 新增，`nav` 里补回 photos/records/library。
- MiniPlayer 的 `HIDE_ON` 和单辑详情页的返回链接都指回 `/records`、`/photos`。

#### 遗留

- 轨道图目前是**纯文字节点**（圆里写"摄影 / 3 辑"）。想更"有内容"的话，
  节点里可以放真实缩略图（最新一张照片 / 唱片封面 / 书封）——那会引入内容图片，
  按 CLAUDE.md 的规矩它们该是**原色**的，包着它的界面仍然只有黑白灰。没做，等站主发话。
- `--orbit-spin: 60s` 这个转速没经过站主确认，觉得晃眼就调大。

### 同日第三轮：关于页正文重写 + 版式居中 + 轨道图加厚（2026-09-08）

站主指了隔壁 `项目文件存放处/网站设计/`（另一个 Next.js 站，他自己那版）说：
按那边的个人介绍和「关于我」重写，**推翻原来的文字**；内容再居中一些、别别扭；
轨道图「再加一点流转的速度，再加点交互效果，再美观一些」。

#### 正文推翻重写

`content/about/about.{zh,en}.md` **整篇换掉**，来源是 `网站设计/lib/content.ts`
里的 `aboutSections`（那边写得比这边实在得多）。四段：

  开头     21 岁、上海、计算机网络、2026.07 毕业；这一年只学一件事，
           判断标准是「每学一样得做出一个真在跑的东西」，现在有三个
  我是怎么学的   不是程序员，第一件事不是学框架是搭知识库
  客服那一年教会我的  「客户着急的时候，第一件要给他的不是解释，是一个确定的东西」
  接下来    要找的不一定是写代码的岗；**缺口也写在明面上**（React 倒着学的、
           没碰过后端接口、TS 类型不规范）

⚠️ 最后那段「缺口」是这篇的骨头，别因为「显得不自信」把它删了 ——
一份把自己不会的写出来的自我介绍，比一句「熟练使用 AI 工具」可信得多。

新增 `content/about/timeline.json`（同样来自那边的 `timeline`）+
`components/about/Timeline.tsx`：一条竖线串五格，**最后一格的点是实心的**
（= 现在停在这儿），竖线只画到末点为止，所以是「到此为止」不是「还有下文」。
读取函数 `getTimeline()` 在 lib/content.ts，**不排序**，顺序照 json。

`site.config.ts` 的 `tagline`/`taglineEn` 跟着换了（原来那句「技术支持新人」和新正文里
「不一定是写代码的岗位」是拧着的）。**`taglineShort` 没动** —— 它是分享图 og.png 的
文案，改了要重跑 `npm run og`，那句本身也还成立。

#### 版式：从「贴着左边」改成「一条居中的脊」

站主原话「内容居中一些，看着不别扭观感流畅」。上一轮做了 1240px 版心，但正文列
（700px）仍然贴在版心左边，右边空掉五百多像素，整页是歪的。这一轮：

- **关于页**自己套一层 `mx-auto max-w-[820px]`（比全站版心窄），标题、正文、履历、
  轨道图共用同一根中轴线。`.prose-bw` 在里面再 `mx-auto` 一次。别改回全宽。
- **文章详情页**的阅读列改成 `mx-auto max-w-column`。侧边目录跟着落进右边留白：
  700+56+190 = 946 < 1240，正好把那块空白用掉，左右就对称了。

#### 轨道图加厚

- 转速 `--orbit-spin` 60s → **42s**；新增 `--orbit-ticks-spin: 150s`。
- **内圈 60 道刻度**（`.orbit-ticks`）：conic-gradient 画满一圈 + radial 遮罩留一细环，
  反着慢慢转。和唱片沟槽、番茄钟表盘同一套刻度语言。
  ⚠️ 遮罩必须写 `circle closest-side`：radial-gradient 默认按 farthest-corner 算，
  正方形里 100% 会变成对角线的一半，整个环跑到方框外只在四个角露一点。踩过一次。
  半径用 `--orbit-r * 0.47`（辐条露出那段的中点），**不要写成「轨道半径减固定像素」**——
  手机和桌面两套尺寸差得多，减同一个数在小屏上会顶到节点上。
- 节点反白时加**一圈光晕 + 一道投影**（不然只是变黑，没有被拿到手上的感觉）。
- 停在某个节点上时：**整圈轨道提亮一档**（「它们都在同一条轨道上」这句话，正是在你
  注意其中一个的时候最该说出来）、**圆心 Logo 让位**淡到 0.28。
- 图下面加了**一行会换的说明**：四条（默认 + 三样）叠在同一个 grid 格子里交叉淡入，
  靠 `:has(.orbit-arm[data-k=…]:hover)` 选。所以**整块仍然零客户端 JS**，
  而且四条占同一格，换哪条页面都不会被顶动。

#### 遗留

- 首页那段 `content/home/intro.{zh,en}.md` **没动** —— 站主这次指的是「关于我」。
  那段和新的关于页有重叠（都讲了用 AI 读/做/啃英语），要不要一起收一遍，等他发话。
- 节点里仍是纯文字。想放真实缩略图（最新照片 / 唱片封面 / 书封）的话，
  按 CLAUDE.md 那些内容图该是**原色**的，圆环辐条标签仍然只有黑白灰。

### 同日第四轮：履历挪到右栏、首页介绍重写、项目/视频/联系三页重排（2026-09-08）

#### 履历挪到关于页右栏

站主原话「我希望这个放到关于我的右边，一起，像刚才网站的排版一样，一边介绍一边显示经历」。
照隔壁 `网站设计` 那个站的关于页排：`lg:grid-cols-[minmax(0,1fr)_300px]`，
右栏 `sticky top-[88px]`（让开 64px 顶栏）。页面容器从 820px 放宽到 1080px。

**为什么右栏要 sticky**：读到「客服那一年」的时候，右边正好停着 2025.07 出港客服那一格，
两边互相印证。正文滚起来履历不滚走，这一条是这个排法的全部意义，别改成普通的静态右栏。

`Timeline.tsx` 的字号跟着窄栏重调了（年份 11px / 标题 14.5px / 说明 12.5px），
顶上的 `SectionTitle` 换成一道细线 + 小标签，和左边正文第一行齐平。
**这套字号是按 300px 调的**，搬到宽处用要重调。

#### 首页介绍重写

`content/home/intro.{zh,en}.md` 整篇换掉，同样取自隔壁那个站的 `home.subtitle`
和 `subtitleAlt`，再接上关于页那条主线。顺带修掉一个**死链**：旧版最后一段里的
`[现在](/now)` —— `/now` 那一页从来没做出来过（阶段 0 就并进首页了），
挂了这么久没人发现。

首页块 A 那句大字引言从 page.tsx 的硬编码搬进 `messages` 的 `home.quote`
（按 CLAUDE.md「用户可见文案走字典」），句子也换了 —— 旧那句引的是**已经被整篇
换掉的**旧关于页，现在是「每学一样，得做出一个真在跑的东西」，
这是新自我介绍里的判断标准，也是整个站的论点。

#### 项目 / 视频 / 联系三页重排

站主说这三页「排版很不舒服」。查下来是同一个根子：**内容量配不上 1240px 的版心**，
但三页的解法不一样（各自征求过站主意见，选的都是推荐项）。

**项目页** —— 整页收进 1000px 居中版心；「我做的」从列表行换成**两列编号大卡**
（新组件 `components/projects/ProjectCard`）。
理由：站主自己的项目只有三个，是整个站最该被看见的东西，被压成和「用到的开源」
那 12 条一模一样的行，等于主角和配角排成一样大。**「用到的开源」仍然是列表** ——
那本来就是一份清单。所以现在两块不只是内容分开，**形状也分开**了。
卡面右上角那个巨大编号是淡出去的底纹（line-strong/45，hover 加深到 /80），
和首页「在做的」那三行的 01/02/03 是同一个手势。

**视频页** —— 现在只有 1 条视频，上一轮却被我改成了两列，右半边全空。
改成**按条数自适应**：`videos.length <= 2` 时单列大卡 + 960px 居中，
三条以上才铺两列。阈值写在页面里而不是写死列数，以后多发几条不用回来改代码。

**联系页** —— 改成**左右两栏**（左：衬线大字邮箱 + 写邮件/复制；右：社交清单竖排），
容器 1080px 居中。原来是上下两块各限 860px 左对齐，一个邮箱加六个链接摊在 1240px 里，
右边空一大片。社交从双列改单列：右栏只有 320px，双列会把 @handle 挤断行；
handle 那一格加了 `truncate`，宁可截断也不让它把行撑成两行。

#### 现在全站的宽度约定（一张表，改之前先看这个）

| 页面 | 容器 | 说明 |
|---|---|---|
| 全站默认 | 1240px（`--spacing-page`） | 列表、网格、照片墙铺满它 |
| 首页 | 1080px | 报头 / 引言 / 关于，下面「在做的」\| 「最近写的」两栏 |
| 关于 | 1080px | 正文 \| 履历 两栏 |
| 联系 | 1080px | 邮箱 \| 社交 两栏 |
| 项目 | 1000px | 三个筛选共用同一左右边界，切换不跳 |
| 视频 | 960px（≤2 条时） | 三条以上回到 1240 铺两列 |
| 文章正文 | 700px（`--spacing-column`）居中 | `.prose-bw` 自带，侧边目录落在右边留白 |

**行宽（一行字最多多长）2026-09-09 收敛成三档**，新代码不许再写裸值，挑一档用：

| token | 值 | 给谁用 |
|---|---|---|
| `--spacing-display` | 860px | 大字引言那一档（衬线 26–34px） |
| `--spacing-column` | 700px | 正文和引导语 —— markdown 正文也用它 |
| `--spacing-note` | 420px | 小注记、说明、一句话补充 |

原来散落着 860/760/720/620/400/340px 和 60ch/46ch/30ch **九种写法、三种单位**。
剩下的裸值都是**组件几何**（560px 面板、420px 进度条、46%/80% 比例、36em 容器查询），
不是文字量度，不归这张表管。

**规律**：内容量少的页面就把版心收窄并居中，不要靠留白硬撑 1240。
新加页面时照这个表挑一档，别再随手写 `max-w-[860px]` 然后左对齐 —— 那正是这一轮在修的毛病。

### 同日第五轮：首页重排（2026-09-08）

站主：「首页的排版也有些别扭。」是同一个毛病的最后一处 —— 摊在 1240px 上，
眉标「首页 / HOME」被甩到一千二百像素外、引言右边空一大片、
「在做的」那几行描述拉成一千像素长的一句话。

- 整页收进 **1080px 居中版心**（和关于页、联系页同一档）。
- 报头那句一句话定位加了 `max-w-[46ch]` —— 换成新文案之后它比原来长，
  不限宽会一直伸到右边眉标底下。
- **块 C「在做的」和块 D「最近写的」在 lg 以上并排**。首页是门口，这两样本来就该
  一眼同时看见；并排之后每行的字数也回到正常。窄屏仍是上下排，间距沿用原来的 68px。
- 块 C 的行内结构跟着改了：状态标签原来是单独一列右对齐，并排后这一栏只剩五百来像素，
  再挤一列会把项目名压断行，所以**状态跟在名字后面同一行**。
- 两栏底部那句「更多…」用 `mt-auto` 对齐，左右长短不一时下沿仍然是平的。

至此四轮版式返工收尾。**规律写在上面那张宽度表里**：内容量少的页面就把版心收窄并居中，
不要靠留白硬撑 1240，也不要「限个宽然后左对齐」。

### 代码审查与修复（2026-09-09）

改版五轮做完之后做了一次三维度代码审查（正确性 / 架构性能 / 内容双语文档）。
**构建和 lint 全绿，但审出三个真实逻辑错误** —— 全在这几轮新写的交互代码里，
而且其中两个的文件注释还宣称功能是好的。这条教训值得记住：
**注释里写「这个交互能做到 X」的时候，必须当场在浏览器里点一遍 X。**

#### 修掉的（严重）

**1. `#hash` 深链在同页场景下完全不工作**（`ui/SegmentedTabs.tsx`）
⌘K 里「新闻」「小工具」这类别名是 `/blog#world` 这种带 hash 的**同页**地址。
同路由导航时 Next 只调 `history.pushState`，而 **pushState 按规范不触发
`hashchange`**，组件也不重挂载 —— 表现是地址栏变了、页面纹丝不动。
修法：`CommandPalette` 的 `go()` 里把目标 hash 用 `TAB_HASH_EVENT`
（`segmented-tabs:select`）广播出去，SegmentedTabs 三个入口都认
（挂载读一次 / `hashchange` / 这个自定义事件）。
**不依赖 `location.hash` 何时被写入，所以没有时序竞态** —— 别改成 setTimeout 里补发。

**2. hash 会静默改写用户记住的筛选项**（同上）
原来 hash 直接调 `setTab`，而那个 setter 会写 localStorage。从 ⌘K 点一次「新闻」，
`blog-tab` 就被永久改成 world，之后裸访问 `/zh/blog/` 默认落在新闻而不是文章 ——
一次临时跳转悄悄改掉了长期偏好。
修法：选中项拆成两个来源，**用户手点 → 落盘（偏好）；hash → 只在本次浏览生效**。
`pick()` 里先 `setLinked(null)` 再落盘，顺序别反。

**3. 点当前语言会被踢回首页**（`shell/TopNav.tsx`）
高亮那一侧的 href 被写成了 `homeHref`。在 `/zh/blog/` 上点「中」直接跳首页。
搬顶栏时改坏的（旧 Sidebar 这里是 `"#"`）。已改回 `"#"`。

#### 修掉的（主要）

**4. 四条已上线地址裸 404** —— 新增 `components/shell/Moved.tsx` +
`[locale]/{news,guestbook,focus,lounge}/page.tsx` 四张跳转页：
`meta http-equiv=refresh` + `robots: noindex, follow` + 一句人话和手动链接。
去向：news→`/blog#world`、focus/lounge→`/projects#gadgets`、guestbook→`/contact`。
文案在 messages 的 `moved.*`。**它们靠 NAV_ITEMS 生成的 sitemap 天然不会被收录**（已验证）。
⚠️ **以后再删或改任何已上线的地址，先来这儿加一条。** 这套写法本来就有
（2026-08-30 的 /lounge 改名页），改版时被我连着删了。

**5. 全站 meta description 已过时** —— 仍自称「技术支持新人……用来发呆的专注区」，
而关于页早改成「不一定是写代码的岗位」、`/focus` 也删了。这是搜索结果和分享卡片
直接读的文案，`tagline` 改了却漏了它。已重写，产物全站零残留（已验证）。

**6. 番茄钟在隐藏面板里仍以 60fps 重绘** —— 切到「手记」栏只是 `display:none`，
rAF 照样对不可见节点写 `style.width`。`PomodoroDial` 加了 `active` prop，
不可见时降级成每秒一跳（和 `prefers-reduced-motion` 同一条分支）。

#### 顺手清掉的（次要）

- 孤儿 key：`news.title/lead/footerNote`（/news 页删后由 `blog.*` 接管）、`projects.visit`
- `--spacing-topnav` 定义了零使用 → 顶栏高度、main 上边距、关于页 sticky 三处
  现在都从这个 token 算，改一处会联动
- `Timeline` 的 `key` 加上序号（同一 `YYYY.MM` 两条会撞）
- 首页块 C 补空态兜底（`projects.json` 一条 featured 都没有时原来只剩空壳）
- `Gadgets` 内层筛选改用 `useStoredState`，和外层 SegmentedTabs 对称（原来刷新即丢）
- 死代码：`--color-desk-muted`、`.prose-tight`
- `timeline.json` 的「四个自动化脚本」数字对不上（实际 7 条）→ 改成「一批」
- `docs/如何更新内容.md` 里残留的「爱好页」（同一份文档两处自相矛盾，会误导站主）

#### 明知没修的（下一轮）

- **`/blog` 单页 HTML 从 67.6 KB 涨到 187.4 KB**（gzip 19.0 → 40.5，**+21.5 KB**）——
  只想读文章的人完整扛了一份新闻页。根子是 SegmentedTabs「未选中也留在 DOM 里」
  这个约定撞上了「一页塞三份内容」。修法是让新闻两栏按需取 `content/news/*.json`，
  改动不小，单独一轮做。
- **番茄钟的完成事件会迟到** —— `endsAt` 是绝对时间戳所以数字不丢，但 `complete()`
  挂在组件 effect 上，离开项目页期间不执行，提示音要等回来那一刻才补放。
  要修得把计时提到全局，性价比不高。
- **限行宽有六种写法**（`720px`/`860px`/`760px`/`620px`/`60ch`/`46ch`）——
  容器档位守住了宽度表，但行宽是失控的第三种量度，该收敛成 token。
- 老死代码 `RecordShelf` / `getRecords()` / `records.json` / `fetch-record-covers.mjs` 仍在。

### 补完上一轮明知没修的三条（2026-09-09）

**1. `/blog` 单页体积 —— 已解决，而且比合并之前还轻**

| | 合并前 | 合并后（问题） | 现在 |
|---|---|---|---|
| HTML raw | 67,626 | 187,425 | **61,968** |
| HTML gzip | 18,986 | 40,509 | **18,079** |
| flight | — | 107,463 | **38,107** |

做法：新闻两栏改成**客户端按需加载**。`scripts/build-news-data.mjs` 在构建时把
`content/news/` 翻成 `public/data/news.json`（31 KB / gz 12.7 KB），
`components/news/NewsPanel` 在**切到那一栏时**才 fetch，两栏共用一次请求。
挂在 package.json 的 `prebuild` 和 `dev` 上，`npm run build` / `npm run dev`
会自动跑，站主不用记；产物不进 git（由 content/ 推导，进仓库只会和每天的新闻提交打架）。

⚠️ **这是对「未选中也留在 DOM 里」那条约定的有意例外，只针对新闻。**
那些标题是别人家报社的，本来就不该作为这个站的正文被搜索引擎收录 ——
所以「不进 HTML」这件事在这里是**收益不是代价**。
**自己写的文章仍然全部服务端渲染，一个字没少，别把这条例外推广到文章上。**

技术上踩了一个坑：一开始想把 `content` 改成 `(active) => ReactNode` 的函数形式，
构建直接报 *Functions cannot be passed directly to Client Components* ——
panel 内容是服务端组件渲染好再传进来的，函数跨不了 RSC 边界。
改成 `TabActiveContext`（provider 在 SegmentedTabs 这个客户端组件里）就通了。
`getNews()` 随之退场。

**2. 番茄钟完成事件迟到 —— 改成不迟到就不响**
`complete()` 加了 `withChime` 参数；effect 里判断「第一次 arm 时就已经过期」
（= 这一段是在组件没挂着的时候走完的），这种情况静默结算。
那一声的意思是「时间到了」，迟到几分钟再响就是在错误的时间响，不如不响 —— 数字仍然是对的。

**3. 行宽九种写法 —— 收敛成三档 token**（见上面那张表）。
顺带把出现四次的 1080px 版心也给了个 `--spacing-page-narrow`。
个别宽度有小幅变动：引导语 720→700、AI 解读 760→700、小工具说明 620→700、
项目页说明 60ch(≈400)→420、手记提示 400→420、番茄钟设置说明 340→420。都在 ±80px 内。

**同一轮还删掉了报头那句里的「在上海。」**（站主指的）——
`site.config.ts` 的 `tagline`/`taglineEn`。首页介绍正文里那句「目前在上海求职，
随时可以到岗」**没动**，那是另一处、也该留着。

---

## 全站视觉改版：黑白 → 雾蓝 + 蓝/橙（2026-09-17）

站主拿来一份新的设计交付包（`design_handoff_weiliang_redesign/`），要求
**把之前那套黑白设计系统整个覆盖和删除**，全站换成「雾蓝底 + 蓝/橙双主色 +
玻璃卡面 + 手写标题」。信息架构、内容管线、路由、部署方式一律没动 ——
这一轮改的全是视觉层。

规格现在在 `docs/design/改版规格.md` + `docs/design/tokens.css`（已从交付包拷进仓库），
效果图是同目录的 `weiliang-redesign-reference.html`（26 MB 单文件，**在 .gitignore 里**）。
**老的五个黑白画板（docs/design/*.dc.html）和整个 design-v2/ 已经 `git rm` 删掉了**
—— 本文件上面所有提到它们的段落都是历史记录，不是现行规矩。

### 改了什么

**token（src/app/globals.css）**
- `@theme` → **`@theme static`**。Tailwind 4 默认只输出「被某个工具类用到」的 token，
  第一次构建时 `--color-accent-700` 和 `--font-hand` 直接被摇没了 —— 这套系统里大量 token
  是组件通过 `var(--color-accent-700)` 直接引用的，摇掉就静默失效。**别把 static 去掉。**
- 老名字（ink/body/muted/faint/line/card/paper/shell-*/desk-*）**全部保留，只换值**，
  所以组件层引用这些名字的地方一行没改。新增 bg/surface/accent-*/accent-2-*/neutral-*/radius-*/shadow-*。
- **间距**：设计稿要 Organic 1.10× 密度（4.4/8.8/13.2/17.6/26.4/35.2px）。
  tokens.css 原本是逐个覆盖 `--spacing-1…8`，那样只有这六档变，`p-5`/`p-10` 还留在 4px 基数上，
  一把尺子两种刻度。改成一行 `--spacing: 0.275rem`（4.4px）—— 六档值和设计稿一字不差，
  其余档位等比缩放。**这一行影响全站所有数字型间距/尺寸类**。
- 三层体感的基础层：`.glass` / `.glass-soft` / `.glass-tag` / `.btn-primary-glow` /
  `.bg-texture` / `.bg-blob-a|b` / `.scroll-progress` / `[data-wl-rv]` / `.page-enter` +
  全套 `wl*` keyframes。**放在 `@layer components` 里**，不是裸写在最外层 ——
  裸写的规则优先级高过 Tailwind 的 utilities 层，`class="glass rounded-lg"` 里的
  rounded-lg 会被 .glass 自己的圆角吃掉。
- `card-face` / `tag-framed` / `link-underline` 三个 @utility 重写成新语言，
  所以套它们的组件（ListRow / MediaCard / NewsPanel / Gadgets / Giscus…）自动跟着换。
- `.prose-bw img` 原来强制 `grayscale(1)` —— 老黑白系统的规矩，**已取消**，改成 washed
  （`saturate(.82) brightness(1.04)`），hover 恢复原色。h2 换成手写体。
- 清掉已经没人用的 `bg-content` / `bg-rule` / `--spacing-topnav`。
- 全站统一焦点环 `:focus-visible`（handoff §7）。

**字体（src/app/[locale]/layout.tsx）**
Inter 换成 **Figtree**；新增 **Noto Sans SC**（原来中文靠系统 PingFang SC 兜底，
Windows 上没有）、**Caveat**、**Ma Shan Zheng**。全部 `next/font/google` 自托管，
构建出来 301 个 woff2 分片（CJK 按 unicode-range 切的）。
⚠️ `--font-hand` 的栈序 Caveat → Ma Shan Zheng **不能反**：Caveat 没有 CJK 字形，
反过来写拉丁字母也会被 Ma Shan Zheng 接走。

**外壳**
- `SiteShell`：底纹层 + 两颗光斑 + 滚动进度条 + 1180px 版心 + 全站页脚。
  光斑包在一个 `fixed inset-0 overflow-hidden` 里 —— 直径 680px、定位是负值，不裁会撑出横向滚动条。
  **main 绝对不能 overflow-hidden**（博客详情页的 sticky 目录会被切没）。
  原来那层 Grain（SVG 噪点纸纹）连组件一起删了。
- `TopNav`：完全透明，无边框无阴影无毛玻璃。**因此也不再 fixed** ——
  一条没有底的横条钉在视窗上，内容滚到它下面就糊成一团。品牌标记改成双色错版
  （同一张 logo 用 CSS mask 印两遍，橙压左下、蓝压右上，错 5px）。
- `SiteFooter`（新）：neutral-900 底、顶部 56px 圆角，手写大字 + 主按钮 + 一排圆形社交。
  **版权行搬到这里了**，`ContentFooter` 不再印第二遍。

**动效**
- `Reveal` 从 Framer Motion 换成 IntersectionObserver + CSS：左右交替的错位滑入，
  强度 `--wl-k: 1.6`，错峰 `(index % 4) * 110ms`。**API 从 `delay` 改成 `index`**，
  所有调用点都改过了。卡片网格是一张张分别进，所以每张卡各包一个 `<Reveal index={i}>`。
  **1600ms 的兜底定时器不能删** —— 图没加载完时观察器可能一直不响，页面就是空的。
- `PageFade` 换成 `.page-enter`（wlPgSlide，从右 44px 滑入），key 跟着 pathname。

**逐页**：首页（Hero + 拱门头像视差 / 在做的三张卡 / 最近写的+最近拍的 / 爱好胶囊）、
关于（正文 + 「现在」玻璃大卡 + 履历 + 轨道图）、项目（单列大卡，64px 手写序号）、
博客（58px 日期块的卡片列表）、视频（880px 单列，74px 播放键）、摄影（4/3 figure + washed）、
工具（胶囊卡）、联系（主色实底大卡 + 社交胶囊行）。⌘K、迷你播放器、番茄钟、手记、
唱机、书架、新闻、留言全部只换皮，逻辑一行没动。

### 留下的几件事

1. **首页那块「关于」玻璃卡不在设计稿的四块里**，是实现时补回来的：
   `content/home/intro.{locale}.md` 是站主自己写的一段自我介绍，设计稿的首页没给它位置，
   照着做等于把这段文字从站上删掉。不想要就删 page.tsx 里那个 section。
2. **首页头像是占位**：用的是 `/images/hero/road.webp`（那张双色调背影照）。
   真头像到位后换 `components/home/HeroAvatar.tsx` 里那一个 `src` 就行。
3. **PosterHero 的纸色换成了 `--color-bg`**，但照片本身那层双色调是
   `scripts/build-hero.mjs` 按旧纸色 `#FAFAF7` 印出来的，两者之间会有一丝极淡的色差。
   要完全消掉得改那个脚本的 substrate 再重跑 `npm run hero` —— 这轮没动 scripts/。
4. **messages/ 只新增、没改动任何现有文案**：新增的是设计稿要求的装饰性文案
   `home.hero*/cta*/avatarBadge/photosTitle/hobbiesTitle`、`about.nowLabel/card*`、
   `footer.line1/line2/cta`。页头那些大写英文标签（ABOUT/PROJECTS/…）是版式记号不是文案，
   由调用方传字面值，不走字典。
5. 设计稿里说图标用 Lucide，**没有装这个包**（技术栈锁死）——
   首页爱好那三个图标按 Lucide 的画法把路径直接写进 `components/home/HobbyPills.tsx` 了。

## 改版第二轮 · 第一阶段：精简范围（2026-09-17）

设计交付包换了一版（`design_handoff_weiliang_redesign/`，不进 git）：除了视觉，还要**精简功能范围**、**全量换文案**、**全量换图片**。
规格/token/文案已同步进 `docs/design/`（`改版规格.md` / `tokens.css` / `文案定稿.md`）。
新定稿和上一轮换皮的差别：**无底纹网格、无顶部滚动进度条、首页头像是拍立得、顶栏左右分立**。

### 做了什么
- 下线（路由 + 组件 + 内容 + 脚本）：书影音/书架、专注区（番茄钟、便签）、放松区、留言板、文章评论 Giscus、
  新闻抓取、⌘K 命令面板、关于页轨道图与时间轴、`Moved.tsx` 及四个旧地址跳转页。
- 清单外顺带删：`.github/workflows/news.yml`（每日跑新闻脚本）、`content/about/timeline.json`、
  `repos.json` 的 giscus 条目、globals.css 里只给轨道图/放松区用的 `.orbit*` 与 `dcDriftX/dcFlicker/dcRain`。
- `nav.ts`：顶栏八项 首页/项目/视频/博客/摄影/工具/关于/联系；`/records` 只在 NAV_EXTRA；`NAV_ALIASES` 删除。
- messages 删掉 library/giscus/palette/news/gadgets/moved 等键组，中英键集合一致；
  三处介绍已删功能的文案只删了半句（第二阶段整体换文案）。
- `package.json`：`dev` 不再先跑新闻脚本，删 `prebuild` / `news`。
- `npm run build` 通过（39 页静态导出）；Haiku 代理独立验收：通过。

### 决定（站主授权主会话代为决定）
- 旧地址 /library /focus /lounge /guestbook /news **接受 404，不配重定向**（旧数据不重要）。
- `site.config.ts` 里已无人读取的 `gadgets` / `giscus` 配置删除（随第二阶段一起做）。

### 下一步：第二阶段（视觉 + 内容）
按 `docs/design/改版规格.md` §十 第 1–6 步。英文内容本次不提供，`en` 路由复用中文内容。
上一轮换皮留下的 `ScrollProgress`、`.bg-texture` 网格、拱门头像要按新定稿去掉/替换。

## 改版第二轮 · 第二阶段：视觉 + 内容（2026-09-17）

按 `docs/design/改版规格.md` §十 第 1–6 步做完；Opus 实现，Haiku 独立验收「通过」，主会话抽查图片与交互逻辑无误。

### 做了什么
- **外壳**：删 `ScrollProgress` 与网格底纹，只留两颗光斑；`SiteShell` 最外层 `overflow-x: clip`
  （从右滑入的块初始位移会在 400px 宽下撑出横向滚动；clip 不生成滚动容器，文章页 sticky 目录不受影响，**别换成 overflow-hidden**）。
  `TopNav` 左右分立 + `.nav-split` 2px 渐变下法线；页脚按定稿两栏。
- **globals.css**：删 shell-* / void / desk-* token、`.bg-texture` / `.scroll-progress` / `tag-framed` 与一批无人用的 keyframes；
  新增 `.washed`（saturate .6 / contrast .85 / brightness 1.1）、`.glass-tag-2`、`.glass-tag-neutral`。
- **逐页**：首页（拍立得 `HeroAvatar`、在做的、最近写的/拍的、爱好两行）、关于（正文 + 大卡）、项目、博客、视频、摄影、工具、联系；
  `/records` 与迷你播放器只换皮，`PlayerProvider` 的 localStorage 逻辑未动，视频点击才载入 iframe 未动。
- **删除组件**：SegmentedTabs、ListRow、MediaCard、EmailActions、PosterHero（及 `intro` 字典）。
- **文案**：全量照 `docs/design/文案定稿.md`；长内容进 `content/`，短文案进 `messages/`。工具清单与四篇博客正文沿用旧的。
  en：`content/` 读取回退到 zh（不复制 .en 文件）；`messages/en.json` 为直译，键与 zh 一致。SEO 的 pageMeta 值也换成定稿引导语。
- **图片**：交付包 `assets/` 按 §九 拷入 `public/`；删旧摄影目录 34 个文件、`hero-src/`、`photos-src/`、`build-hero.mjs`、`build-photos.mjs`。
  摄影辑改为 `content/photos/{shanghai,beijing,qingdao}.json`，地址 `/photos/shanghai/` 等。
- `site.config.ts` 删掉无人读取的 `gadgets` / `giscus`。

### 主会话代站主做的决定
- **邮箱沿用 site.config 的 `weiliang99520@gmail.com`**，没用定稿里的 `hi@weiliang.dev`（后者是否能收信未确认）。
  要换就只改 site.config 的 `email`。
- PosterHero 下线（定稿摄影页没有它，它上面的字是旧文案）。
- 所有卡片统一玻璃化（CLAUDE.md 规则），参考稿里个别实色白卡未照搬。

### 遗留 / 下一轮注意
1. ~~`public/images/hero/poster.png` 3.2 MB~~ —— **已处理**：用 sharp 转成 `public/images/hero/poster.webp`，1086×1448 → 800×1067，quality 75，约 195 KB（199,178 字节）；原 png 已删，引用已全部改名。
2. 迷你播放器的 `HIDE_ON` 仍包含首页（开场页时代的规则），首页不显示播放器；要显示就去掉那一项。
3. 三篇占位博客（如「为什么我把网站做成黑白的」）正文仍是旧占位，开头有「这是一篇占位…」引用。
4. ~~英文内容待站主提供~~ —— **已补译**（2026-09-17，待站主过目）：
   - markdown：`content/home/intro.en.md`、`content/about/about.en.md`；四篇文章各加 `content/posts/<slug>.en.md`
     （front-matter 只写 title / summary / tags，正文英文；同一 slug、同一 URL）。
   - json：`projects.json` 加 `name_en / status_en / desc_en / summary_en`；`videos.json` 加 `title_en / desc_en / summary_en`；
     `photos/*.json` 加 `titleEn / captionEn`；`music/resident.json` 加 `creditEn`。歌名/歌手不译；`tools.json` 本来就有 `desc_en`。
   - 读取逻辑：`getPosts()` 跳过 `*.en.md`，把译本并进 `title_en / summary_en / tags_en / body_en / minutes_en`（不单独成篇，RSS/sitemap 不变）；
     博客列表按语言取标签与时长，文章页在 en 下读 `body_en`、有译本时不再显示「原文为中文」；唱机常驻出处读 `creditEn`。
   - ⚠️ 以后改中文内容，对应的 `.en.md` / `_en` 字段要一起改，否则英文站是旧的（已写进 `docs/如何更新内容.md`）。
5. `.claude/skills/mono-color/NOTICE.md` 仍提到已删的 build-hero / PosterHero（许可说明文件，未动）。
6. `pageMeta.blog.description` 里「共 4 篇」是照定稿写死的（SEO 描述不走 `{count}` 插值），增删文章时 zh/en 两处要手动改。

## 2026-09-17 清理无用文件

站主确认后删掉的东西（逐项 grep 复核过没有在用的功能）：
- **旧「我听的」专辑墙**（2026-08-31 起就不渲染）：`src/components/records/RecordShelf.tsx`、`content.ts` 的 `getRecords()`、
  `types.ts` 的 `RecordItem`、`content/music/records.json`、`scripts/fetch-record-covers.mjs` 与 `package.json` 的 `records` 脚本，
  以及只被 records.json 引用的 6 张封面 `public/images/records/{zhao-chuan,jay-chou,chang-chen-yue,all-for-you,red-right-hand,not-for-me}.webp`
  （`chart-*.webp` 是「按心情听」榜单在用的，保留）。`RecordsSection.tsx` / `types.ts` 里「保留不删方便回退」的注释一并去掉。
- **零散**：`public/images/hero/road.webp`（无引用）；无人调用的 `getPost`（content.ts）、`getAlbum`（photos.ts）、
  `monthLabel` / `yearOf`（format.ts）；`messages/{zh,en}.json` 的 `common.language`；`public/images/.gitkeep`、`content/projects/.gitkeep`。
- **过时文档**：`docs/分工提示词.md`、`docs/review/2026-08-25-审核报告.md`、`docs/deploy/README.md`、`docs/图片存放处/logo.png`（三个目录随之清空删除）。
- 连带更新：`docs/素材清单.md`、`docs/如何加歌.md` 里提到专辑墙「保留不删」的说法；`.gitignore` 里交付包相关注释改成「包已从本机删除」。
- 上面历史记录里提到这些文件的地方是当时的状态，不再改写。
