# 素材清单（你需要准备的东西）

> 素材没到位不阻塞开发：没给的地方跑的是占位内容，你把东西发给当班终端、或自己按下面的位置放进去即可。
> 每项到位后把 ⬜ 改成 ✅。**方括号 `[...]` 包起来的都是占位，全站搜一下就能找齐。**
>
> 最近一次更新：2026-08-25（身份信息、四个社交链接、自我介绍、三个项目已填入；
> 工具页图标换成官方图标、B 站视频已录入 1 条）

## 一、最优先（填了整站就像你的了）

- ✅ **Logo**：`public/logo/wl-logo.png`（暗底上反色显示）。已同时用作 favicon（`src/app/icon.png`）。
- ✅ **开场页背景**（2026-08-30）：上海外滩夜景 → `public/images/intro/shanghai.webp`
  （1672px / 136KB，你给的那张 png 压过的，原图没进仓库）。
  ⬜ **配套的深度图 `shanghai-depth.jpg` 现在是「近似版」**：不是真的深度估计，
  是 `scripts/generate-intro-assets.mjs` 按画面构图**程序画出来**的分层灰度图
  （天空最远 → 楼群 → 江面 → 堤岸 → 木板路最近，另加男孩和两根路灯杆两处局部）。
  够用，但层次是猜的。**想升级**：把原图发给能生成 depth map 的 AI，
  要一张灰度深度图（**近处白、远处黑**），存成同名文件覆盖
  `public/images/intro/shanghai-depth.jpg` 即可，**代码不用改，构建一次就生效**。
  换背景照片同理：新图压成 `shanghai.webp` + 配一张同名深度图，
  再把 `CityDepth.tsx` 里的 `IMG_ASPECT` 改成新图的宽高比。
- ✅ **身份信息** → 已填进 `site.config.ts` 顶部 + `messages/{zh,en}.json` 的 `meta` / `intro`：
  - 名字：伟良 / Weiliang
  - 一句话定位：「一个喜欢把问题查到根上、再把答案写下来给下一个人的技术支持新人。」
    英文版：*A support newcomer who chases problems down to the root, then writes the answer down for the next person.*
    ⚠️ 侧边栏那一行和分享图只有一行的位置，整句放不下，所以另给了个**短版**
    （`taglineShort` / `taglineShortEn`：「把问题查到根上，再写下来」/ *Root causes, written down*）。
    这两句想换随时说。
  - 公开邮箱：weiliang99520@gmail.com（已确认）
- ⬜ **六个社交主页链接** → `site.config.ts` 的 `socials`（还差两个）：
  - ✅ X：https://x.com/WeiliangF27854 （@WeiliangF27854）
  - ✅ GitHub：https://github.com/leomfu （@leomfu）
  - ✅ 哔哩哔哩：https://space.bilibili.com/3546677612907455 （已去掉 `?spm_id_from=` 跟踪参数）
  - ✅ 小红书：https://www.xiaohongshu.com/user/profile/63057ac300000000120001cf （已去掉 `?tab=` 参数）
  - ✅ YouTube：https://www.youtube.com/@WEILIANGFU-s7q （2026-08-25 补齐，handle @WEILIANGFU-s7q）
  - ⬜ **抖音**：你给的是 https://www.douyin.com/user/self （这个地址只对登录后的你本人生效，别人打不开）。
    需要**别人也能打开的主页地址**：在抖音 App 里「我」→ 右上角「…」→ 分享主页 → 复制链接，
    粘出来形如 `https://www.douyin.com/user/MS4wLjAB...`。同样，给不了就留空。
  - 备注：哔哩哔哩和小红书那两条的 `handle` 现在显示「主页」（这个字段全站只有一份，
    英文页上也会显示中文的「主页」）。想显示别的（比如昵称）说一声就换。
- ✅ **自我介绍**（中文原文由你提供，英文由我翻译，**请过目**）：
  - 首页那两段 → `content/home/intro.zh.md` / `.en.md`
  - 关于页整篇 → `content/about/about.zh.md` / `.en.md`
  - 首页「现在是」→ `content/now/now.zh.md` / `.en.md`（`updated` 已改成 2026-08-25，
    以后内容有变化记得同步改这个日期，首页那一行会显示「YYYY 年 M 月」）

## 二、内容（可以持续补充）

- ✅ **项目列表** → `content/projects/projects.json`，现在是三个真实项目（占位已删）：
  ① AI 知识库（Claude Code + Obsidian）② Claude-Anki ③ 本站。
  三个都标了 `featured`，所以首页「我在做的」三个全显示。
  ⬜ 差的是 **①② 的链接**：有 GitHub 仓库或线上地址就补进对应的 `repo` / `link` 字段，
  补了之后首页和项目页的标题会变成外链。③ 的 `repo` 已填本站仓库。
- ⬜ **文章** → `content/posts/*.md`（现在是 3 篇标了 `[示例]` 的占位，**你的第一篇真文章进来后就把它们删掉**）。
  front-matter 字段见 `content/README.md`。
- ✅ **视频作品** → `content/videos.json`：占位已删，现在是 **1 条真实视频**——
  B 站 `BV1WHup6fEyA`《Claude-Anki 使用介绍》。⚠️ 日期填的是 **2026-08-01**（你没给具体日子，
  按"2026 年 8 月"取了个合理值），要改直接改 `date` 字段。以后再发视频，照这条的格式往数组里加即可
  （B 站给 BV 号，YouTube 给视频 id，各配一句中英文介绍）。
- ⬜ **工具清单** → `content/tools.json`：现在是画板里那 8 个占位，按你实际在用的增删。
  每条要 名称 / 一句话 / 官网 / 图标名 / 品牌色（hover 时亮的那个颜色）。
  📌 **图标已换成官方图标**（来自 [Simple Icons](https://simpleicons.org)，CC0 授权），
  path 数据硬拷进 `src/components/icons/ToolIcon.tsx`，**没有引入 npm 依赖**；
  `brandColor` 也同步成了各家官方色。要加新图标：去 simpleicons.org 搜到图标，
  把它的 24×24 path 贴进 ToolIcon.tsx 的 `ICON_PATHS` 表，再在 tools.json 里写上同名 `icon` 和官方 hex。
- ⬜ **书影音** → `content/library/library.json`：类型（book/movie/album）、标题、作者、评分 1–5、一句短评。
- ✅ **摄影照片**（2026-09-17 改版全量替换）→ 现在站上是 **3 辑 / 6 张**，图来自设计交付包：
  - `content/photos/shanghai.json` 上海 2025：shanghai-01 陆家嘴天际线 / shanghai-02 武康大楼
  - `content/photos/beijing.json` 北京 2023：beijing-01 天安门夜色 / beijing-03 保和殿
  - `content/photos/qingdao.json` 青岛 2024：qingdao-01 五月的风 / qingdao-04 蓝调时刻
  图片在 `public/images/photos/`。压图脚本（`npm run photos`）已下线，加照片改成**手动放图 + 改 json**，
  完整步骤在 **`docs/如何添加照片.md`**。
- ⬜ **成长素材（格斗 + 马拉松）整批不在站上**。
  2026-08-31 先并成过摄影板块的 `sport-2023-2024` 一辑，**当天用户要求去掉**，已整辑删除。
  原文件都还在 `~/摄影/个人网页上传的作品/个人成长照片/运动方面经历/`：
  - 格斗：TWF 竞武决合影 1 张 + 两段竖屏 **HEVC** 视频（13 秒 / 6 秒）+ 一篇自述 `2026-08-30.md`（`type: blog`）
  - 马拉松：2024 郑开半马的赛前地铁、冲线、赛后成绩短信共 3 张
  以后想上站得先定去处：摄影板块只放图片、`/videos` 是 B 站嵌入位放不了自托管文件，
  所以视频那两段最省事的是写成一篇博客内联进去（**上之前先转 H.264**，
  HEVC 在 Chrome/Firefox 上多半播不了）。

## 三、放松区

- ❌ **氛围音四个文件** → 已于 2026-08-29 **整个删除**（用户要求去掉氛围音）。
  `public/audio/ambient/` 和生成脚本 `scripts/generate-ambient.sh` 都不在了。
  四套**背景画面**留着，下放成放松区的全局背景（纯 CSS，不需要素材）。别再补这四个音频。
  是用 `scripts/generate-ambient.sh`（ffmpeg）**合成**的，不是下载的素材：授权干净、能无缝循环、
  每个 1.2MB，四段统一压到 −23 LUFS，切场景不会忽大忽小。
  **想换成真实录音**：同名 mp3 覆盖进去即可，代码不用改（挑素材的要求见该目录 README）。
- ✅ **音乐层**（2026-08-25 重做成时间盘播放器，不再用网易云的 iframe）。两组曲目：
  - **常驻**：4 首肖邦，公共领域录音（Musopen / CC0），文件在 `public/audio/music/`。
    想换别的：把 mp3 放进那个目录，再改 `content/music/resident.json`。
  - **我在听**：来自你的网易云歌单，但**只能收录外链放得出声的**。
    改歌单方式：编辑 `scripts/netease-songs.txt`（一行一首，写歌曲 id 或直接写歌名），
    然后跑 `npm run music`，脚本会自动查 id、查时长、逐首验证，能放的写进
    `content/music/netease.json`，放不了的会在终端里告诉你。
    ⚠️ 实测**约六成的歌放不出来**（版权限制，华语流行几乎全锁）；能放的里面不少是
    **翻唱/独立版本**而不是原唱。这是网易云的限制，绕不开。
    ⚠️ 你最早给的 `music.163.com/#/user/home?id=6289139566` 是用户主页 id，不是歌单 id。
- ✅ **「按心情听」歌单**（2026-08-30 新加，`content/music/chart.json`，音源换成 Apple 官方
  30 秒预览，`scripts/fetch-music-chart.mjs` / `npm run chart` 生成，步骤见
  `docs/如何加歌.md`）。分 4 个场景（深度专注/通勤路上/番茄钟间隙/深夜放空——**场景名是代拟
  的，不是你的原话**），共 16 首：
  - **确凿的 10 首**：张震岳《走慢一点点》《等我有一天》（你点名的两首）+ 你网易云歌单里
    的赵传两首、Shallow、Hold On、Not For Me、All For You、Red Right Hand、我相信。
  - **代选的 6 首**（`placeholder: true`）：周杰伦四首（七里香/晴天/稻香/告白气球）、张震岳
    两首（爱的初体验/分手吧）——因为拿不到你 Spotify 歌单
    （`4mPvpGE5wk2B3jO6wciAYW`）的实际曲目，凭代表作代选，**等你换成自己真正想放的**。
  - 网易云歌单里的《Sunk》（artist 写的是「WAV」）iTunes 上核实不了对应歌手，没收进来。
  - ⚠️ **未验证点**：`previewUrl` 指向 `audio-ssl.itunes.apple.com`，本机（挂代理）实测能通，
    但没条件验证国内网络裸连是否可达，上线前建议站主自己测一遍。
  - 2026-08-31 起**已经接进唱片页 UI**：按 4 个场景分组的「按心情听」榜单（封面 + 歌名 +
    艺人 + 时长 + 去 Apple Music 外链，整行可点=装到唱机上试听）。原来那面"我听的"专辑墙
    （`content/music/records.json` + `RecordShelf` 组件）已经从页面撤下——新榜单覆盖了它的
    作用且更好，数据文件和 `npm run records` 脚本保留不删，想回退随时能接回来。
    `netease.json`（旧的"我在听"分组）同样保留但页面不读它。
- ⬜ **播客** → 小宇宙节目页或 YouTube 播放列表的**嵌入地址**，填进 `lounge.podcastEmbeds`。
  没填时放松区**不显示「播客」这一层**（不会给访客看到「还没配」这种话），所以不填也不难看。

## 四、需要你去点几下的（评论 / 部署）

- ✅ **GitHub 仓库**：https://github.com/leomfu/WL---
  本地已配好 `origin` 并推送（远程原先停在阶段 1 的那次提交，是本地历史的祖先，
  所以是干净的快进推送，没有覆盖任何东西）。
  ⚠️ 评论和留言板用 giscus，仓库必须是 **public**——顺手去 Settings 确认一下。
- ✅ **giscus（文章评论 + 留言板）**：2026-08-25 全部配好。仓库 public + Discussions 已开 +
  giscus app 已装，四个值都在 `site.config.ts`：repo / repoId `R_kgDOUCPA3g` /
  category `Announcements` / categoryId `DIC_kwDOUCPA3s4DEK0W`。
  留言方案已定：**giscus（有 GitHub 账号的公开留言）+ 邮件兜底**，不引第三方评论后端。
  评论区是**点一下才加载**的，不点不向 GitHub 发请求。
  ⚠️ 以后换自定义域名，记得同步改 `giscus.theme` 里那个 giscus.css 的绝对地址。
- ✅ **部署平台**：**Vercel**，项目 `weiliang`，已接仓库 `main` 分支，线上地址
  **https://weiliang.dev**（域名 2026-08-28 启用；站点本身 2026-08-25 上线，
  旧的 weiliang-blush.vercel.app 仍可访问）。日常流程：改完 markdown → `git push` → 自动构建上线。
  `site.config.ts` 的 `url` 已换成正式域名，分享图也重生成过了。
  ⚠️ 顺手去 Vercel 删掉多余的项目 **`weiliang-site`**（重复建的，同样连着这个仓库，
  留着会每次 push 多构建一遍）：项目 → Settings → 最下方 Delete Project。
- ✅ **域名**：**weiliang.dev**，2026-08-28 **在 Vercel 买的**（不是原来设想的 Cloudflare），
  到期 **2027-08-28**。DNS 用 Vercel 自家的 `ns1/ns2.vercel-dns.com`，域名和项目在同一个账号下，
  所以绑定时不用手配任何 A / CNAME 记录。裸域 `weiliang.dev` 是主地址，
  `www.weiliang.dev` 308 永久跳回裸域。`.dev` 在浏览器 HSTS 预加载名单里，强制 HTTPS，没有 http 兜底。
  ⚠️ Vercel 账号里「付款方式的账单地址不完整」那个警告要去补全，否则明年续费可能出问题。
- ⬜ **访问统计**（可选）：注册 https://cloud.umami.is（免费版够用），把它给的脚本地址和 website id
  填进 `site.config.ts` 的 `analytics`。不填就完全不加载统计脚本。

---

## 2026-09-17 改版新增的占位

- ✅ **首页头像（拍立得）** —— 2026-09-17 改版第二阶段换成设计交付包里的头像，现为 `public/images/hero/poster.webp`（原 png 3.2 MB，已转 800×1067 webp ≈195 KB）。
  以后换照片同名覆盖这个文件即可（3/4 竖幅，人脸大约在上三分之一处）。
